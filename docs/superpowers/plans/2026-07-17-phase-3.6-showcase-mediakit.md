# Fase 3.6 — Showcase UX v2 + media-kit 0.5 + componentes de impacto — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** media-kit `0.4.0 → 0.5.0` (fix mouse-follow en fullscreen, tooltips con delay, `compareMode`, `SpotlightReveal`, `FilterGallery`, `VideoScrubPreview`), showcase con UX v2 (índice que filtra, tabs sin salto, radii/estados coherentes, colorización real, demos nuevas) y 4 componentes de impacto en la app (`AnimatedMetric`, `TiltCard`, `ApiRequestPlayer`, `HeroCanvas`).

**Architecture:** Bloque A = paquete (todo aditivo, props opcionales, 0 deps nuevas). Bloque B = app showcase (un client container nuevo para el filtrado; Tabs pasa a paneles apilados por grid). Bloque C = app (4 client components ligeros con gating de `prefers-reduced-motion`/pointer). Los assets de demo (foto paisaje + clip de vídeo) los genera el ORQUESTADOR con IA antes de despachar B.

**Tech Stack:** React 18+ peer (paquete), TS strict, tsup, Vitest+jsdom+Testing Library; app Next.js 16 App Router (export estático), Tailwind v4 tokens, next-intl.

**Spec:** `docs/superpowers/specs/2026-07-16-phase-3.6-showcase-mediakit-design.md` (leer ANTES de cada tarea; las decisiones de comportamiento viven ahí).

## Global Constraints

- Rama: `feature/phase-3.6-showcase-mediakit` desde `main`.
- TDD estricto; gate por tarea `pnpm run lint && pnpm run typecheck && pnpm run test` en verde antes de cada commit. Commits convencionales.
- media-kit: cero breaking (props nuevas opcionales con default retro-compatible); tests existentes NO se modifican (añadir `describe` nuevos al final), con UNA excepción documentada: los tests de `Tabs` de la app cuyo contrato cambia (Task 12).
- Colores hardcodeados SOLO en `packages/media-kit/src/styles.css`, `apps/web/src/app/globals.css` y assets de demo. En la app, tokens semánticos siempre.
- `prefers-reduced-motion` según la matriz de la spec (por componente). WCAG AA en los 8 combos skin×tema.
- Assets: imagen ≤ 200 kB WebP con `width`/`height` explícitos; vídeo ≤ 1.5 MB MP4 sin audio.
- i18n: toda label nueva en `apps/web/messages/{es,en}.json` (el test `messages-parity` vigila la paridad). En el paquete: defaults en inglés vía props/`labels`.
- NUNCA `git add apps/web/content/` ni `git add -A` desde la raíz — añadir por ruta explícita.
- Subagentes: leer skills `component-patterns`, `accessibility`, `code-conventions` antes de escribir componentes; `tailwind-tokens` para lo de la app; `performance` para assets/canvas.
- El polyfill de `PointerEvent` y stubs de pointer-capture ya existen en `packages/media-kit/vitest.setup.ts` — usarlos, no redefinirlos.

## Mapa de ficheros

```
packages/media-kit/src/
├── media-lightbox/media-lightbox.tsx        # T1 foco fullscreen · T3 tooltips en close
├── media-lightbox/media-lightbox.test.tsx   # T1, T3 (describes nuevos al final)
├── media-lightbox/use-zoom-pan.ts(+test)    # T2 re-clamp resize/fullscreenchange
├── media-lightbox/lightbox-controls.tsx(+test) # T3 data-mk-tooltip en toolbar
├── compare-slider/compare-slider.tsx(+test) # T4 compareMode onion/side-by-side · T5 blink
├── internal/prefers-reduced-motion.ts       # T5 (CREATE, util compartida)
├── spotlight-reveal/spotlight-reveal.tsx(+test)      # T6 CREATE
├── filter-gallery/filter-gallery.tsx(+test)          # T7 CREATE
├── video-scrub-preview/video-scrub-preview.tsx(+test) # T8 CREATE
├── styles.css                                # T3, T4/5, T6, T7, T8 (bloques nuevos)
└── index.ts · package.json · README.md · CHANGELOG.md # T9 (0.5.0)
apps/web/public/demo/landscape.webp · scrub.mp4       # T10 (ORQUESTADOR)
apps/web/src/components/showcase/
├── media-kit-demo.tsx(+test)                 # T11 colorización real
├── compare-modes-demo.tsx(+test)             # T15 CREATE
├── spotlight-demo.tsx(+test)                 # T15 CREATE
├── filter-gallery-demo.tsx(+test)            # T16 CREATE
├── video-scrub-demo.tsx(+test)               # T16 CREATE
├── showcase-view.tsx(+test)                  # T13 CREATE (filtro client)
└── showcase-index.tsx(+test)                 # T13 (onSelect → filtro)
apps/web/src/components/ui/tabs/tabs.tsx(+test)       # T12 paneles apilados
apps/web/src/components/ui/input/input.tsx · select/select.tsx # T14 hover/transition
apps/web/src/app/globals.css                  # T14 prose code radius
apps/web/src/app/[locale]/showcase/page.tsx   # T11-T16 (secciones/TOC/labels)
apps/web/src/components/ui/animated-metric/…  # T17 CREATE
apps/web/src/components/projects/project-card.tsx(+test) # T17 animateMetrics opt-in
apps/web/src/components/ui/tilt-card/…        # T18 CREATE
apps/web/src/components/home/featured-projects.tsx(+test) # T18 aplicar TiltCard
apps/web/src/data/api-demo.ts                 # T19 CREATE
apps/web/src/components/showcase/api-request-player.tsx(+test) # T19 CREATE
apps/web/src/components/home/hero-canvas.tsx(+test)   # T20 CREATE
apps/web/src/components/home/hero.tsx         # T20 integrar canvas
apps/web/messages/{es,en}.json                # T13, T15, T16, T19 (keys nuevas)
docs/superpowers/plans/2026-07-10-portfolio-roadmap.md · STATUS.md # T21
```

---

## Bloque A — media-kit 0.5

### Task 1: foco al dialog tras alternar fullscreen (fix A1-causa 1)

**Files:** Modify `packages/media-kit/src/media-lightbox/media-lightbox.tsx` · Test `media-lightbox.test.tsx` (describe nuevo al final).

**Interfaces:** Consumes `useFullscreen(dialogRef)` existente (`fullscreen.active` se actualiza con `fullscreenchange`). Produces: al cambiar `fullscreen.active`, el foco vuelve al root del dialog → Espacio (space-pan / mouse-follow) funciona inmediatamente tras entrar o salir de fullscreen.

- [ ] **Step 1 — test en rojo** (describe `fullscreen focus (v0.5)`):

```tsx
it('devuelve el foco al dialog al entrar en fullscreen', () => {
  render(
    <MediaLightbox open onClose={noop} label="Viewer">
      <img alt="" src="/x.png" />
    </MediaLightbox>,
  );
  const dialog = screen.getByRole('dialog');
  screen.getByRole('button', { name: 'Full screen' }).focus();
  Object.defineProperty(document, 'fullscreenElement', { configurable: true, value: dialog });
  fireEvent(document, new Event('fullscreenchange'));
  expect(dialog).toHaveFocus();
});

it('devuelve el foco al dialog al salir de fullscreen', () => {
  render(
    <MediaLightbox open onClose={noop} label="Viewer">
      <img alt="" src="/x.png" />
    </MediaLightbox>,
  );
  const dialog = screen.getByRole('dialog');
  Object.defineProperty(document, 'fullscreenElement', { configurable: true, value: dialog });
  fireEvent(document, new Event('fullscreenchange'));
  Object.defineProperty(document, 'fullscreenElement', { configurable: true, value: null });
  fireEvent(document, new Event('fullscreenchange'));
  expect(dialog).toHaveFocus();
});
```

(Si el label del botón difiere, usar la clave real de `MediaLightboxLabels.fullscreen*`; restaurar `fullscreenElement` a `null` en `afterEach` como hacen los tests fullscreen existentes.)

- [ ] **Step 2 — FAIL** (`pnpm --filter @nicobehm/media-kit test -- media-lightbox`).
- [ ] **Step 3 — implementación** en `media-lightbox.tsx` (junto a los demás efectos):

```tsx
// Al alternar fullscreen el foco queda en el botón ⤢, y Espacio sobre un botón
// no activa el space-pan (:354). Devolver el foco al root restablece el follow.
const fullscreenWasActive = useRef(fullscreen.active);
useEffect(() => {
  if (fullscreenWasActive.current === fullscreen.active) return;
  fullscreenWasActive.current = fullscreen.active;
  dialogRef.current?.focus();
}, [fullscreen.active]);
```

Verificar que el root del dialog tiene `tabIndex={-1}` (añadirlo si falta — necesario para `.focus()` programático).

- [ ] **Step 4 — PASS + suite del paquete completa** (cero regresiones en los 43 casos existentes).
- [ ] **Step 5 — commit** `fix(media-kit): return focus to dialog after fullscreen toggle so space-pan works`

### Task 2: re-clamp del pan al cambiar el viewport (fix A1-causa 2)

**Files:** Modify `packages/media-kit/src/media-lightbox/use-zoom-pan.ts` · Test `use-zoom-pan.test.ts` (describe nuevo).

**Interfaces:** Produces: con el hook montado, `resize` (window) y `fullscreenchange` (document) re-clampan `{scale,tx,ty}` a los límites vivos y recalculan `canPan`.

- [ ] **Step 1 — test en rojo** (patrón `renderHook` + `fakeElement()` existente):

```ts
it('re-clampa el pan cuando el viewport cambia de tamaño', () => {
  const viewport = fakeElement(400, 300);
  const content = fakeElement(800, 600);
  const { result } = renderHook(() => useZoomPan({ viewportRef: refOf(viewport), contentRef: refOf(content) }));
  act(() => {
    result.current.zoomTo(2);
    result.current.panBy(10_000, 10_000); // clampa al máximo actual
  });
  const before = { ...result.current };
  // el viewport crece (entrar en fullscreen): el desborde se reduce a 0
  viewport.clientWidth = 1600;
  viewport.clientHeight = 1200;
  act(() => {
    window.dispatchEvent(new Event('resize'));
  });
  expect(Math.abs(result.current.style.tx ?? 0)).toBeLessThan(Math.abs(before.style.tx ?? 1));
});
```

(Adaptar los nombres a la forma real del hook — el estado clampado se observa vía `style`/`canPan`; si `style` expone `transform` string, asertar sobre él. `refOf(el)` = `{ current: el }`.)

- [ ] **Step 2 — FAIL** → **Step 3 — implementación** dentro del `useEffect` de listeners nativos (mismo effect, mismos cleanups):

```ts
function onViewportResize() {
  setState((current) => clampState(current));
}
window.addEventListener('resize', onViewportResize);
document.addEventListener('fullscreenchange', onViewportResize);
// cleanup:
window.removeEventListener('resize', onViewportResize);
document.removeEventListener('fullscreenchange', onViewportResize);
```

- [ ] **Step 4 — PASS + suite completa** → **Step 5 — commit** `fix(media-kit): re-clamp pan state on viewport resize and fullscreen change`

### Task 3: tooltips con delay en toolbar y close

**Files:** Modify `packages/media-kit/src/styles.css` (generalizar bloque tooltip) · `lightbox-controls.tsx` · `media-lightbox.tsx` (botón close) · Tests `lightbox-controls.test.tsx` y `media-lightbox.test.tsx` (describes nuevos).

**Interfaces:** Produces: todo botón del lightbox con `data-mk-tooltip` muestra tooltip CSS con delay 600 ms al hover (sin delay con foco); `data-mk-tooltip-pos="above"` lo posiciona encima (toolbar). Cero claves i18n nuevas (reutiliza los `aria-label`).

- [ ] **Step 1 — tests en rojo**:

```tsx
// lightbox-controls.test.tsx (describe 'tooltips (v0.5)')
it.each(['zoomOut', 'zoomIn', 'reset', 'fit', 'fullscreen'] as const)(
  'el botón %s lleva tooltip con su mismo label, posicionado arriba',
  (key) => {
    render(<LightboxControls {...baseProps} />);
    const button = screen.getByRole('button', { name: expectedLabel(key) });
    expect(button).toHaveAttribute('data-mk-tooltip', expectedLabel(key));
    expect(button).toHaveAttribute('data-mk-tooltip-pos', 'above');
  },
);
// media-lightbox.test.tsx (describe 'tooltips (v0.5)')
it('el botón close lleva tooltip', () => {
  render(<MediaLightbox open onClose={noop} label="V"><img alt="" src="/x.png" /></MediaLightbox>);
  const close = screen.getByRole('button', { name: 'Close' });
  expect(close).toHaveAttribute('data-mk-tooltip', 'Close');
});
```

(`expectedLabel(key)` = el default en inglés del label correspondiente, con `template()` resuelto donde aplique — copiar el patrón de aserciones de labels ya usado en ese archivo.)

- [ ] **Step 2 — FAIL** → **Step 3 — implementación**:
  - `lightbox-controls.tsx`: cada botón añade `data-mk-tooltip={<su aria-label ya computado>}` y `data-mk-tooltip-pos="above"`.
  - `media-lightbox.tsx`: botón close añade `data-mk-tooltip={labels.close}`.
  - `styles.css`: sustituir el bloque acotado a `.mk-lightbox__corner button[data-mk-tooltip]` (líneas ~360-390) por:

```css
.mk-lightbox [data-mk-tooltip] {
  position: relative;
}
.mk-lightbox [data-mk-tooltip]::after {
  content: attr(data-mk-tooltip);
  position: absolute;
  left: 50%;
  top: calc(100% + 8px);
  transform: translateX(-50%);
  padding: 4px 8px;
  border-radius: 4px;
  background: var(--mk-control-bg);
  color: var(--mk-control-color);
  font-size: 12px;
  line-height: 1.2;
  white-space: nowrap;
  opacity: 0;
  pointer-events: none;
  transition: opacity 120ms ease-out;
  transition-delay: 0ms;
}
.mk-lightbox [data-mk-tooltip][data-mk-tooltip-pos='above']::after {
  top: auto;
  bottom: calc(100% + 8px);
}
.mk-lightbox [data-mk-tooltip]:hover::after {
  opacity: 1;
  transition-delay: 600ms;
}
.mk-lightbox [data-mk-tooltip]:focus-visible::after {
  opacity: 1;
  transition-delay: 0ms;
}
@media (prefers-reduced-motion: reduce) {
  .mk-lightbox [data-mk-tooltip]::after {
    transition-duration: 0ms;
  }
}
```

  - Comprobar que los dos botones de esquina existentes (ayuda `?` y ojo) siguen funcionando con el selector generalizado (sus posiciones: default `below`).
- [ ] **Step 4 — PASS + suite completa** → **Step 5 — commit** `feat(media-kit): delayed tooltips on all lightbox controls`

### Task 4: `compareMode` — `onion` y `side-by-side`

**Files:** Modify `packages/media-kit/src/compare-slider/compare-slider.tsx` · `styles.css` · `media-lightbox.tsx` (passthrough en `compare`) · Test `compare-slider.test.tsx` (describe nuevo).

**Interfaces:** Produces: prop `compareMode?: 'wipe' | 'onion' | 'blink' | 'side-by-side'` (default `'wipe'`; `blink` se implementa en Task 5 — aquí el tipo ya lo incluye) + `MediaLightboxProps['compare']` gana `compareMode?`. Root `.mk-compare` emite `data-compare-mode`.

- [ ] **Step 1 — tests en rojo** (describe `compareMode (v0.5)`):

```tsx
it('onion: sin divisor, la posición gobierna la opacidad y el handle anuncia aria-valuetext', () => {
  render(<CompareSlider before={<img alt="" src="/a.png" />} after={<img alt="" src="/b.png" />} compareMode="onion" initialPosition={30} label="C" />);
  const root = screen.getByRole('slider').closest('.mk-compare')!;
  expect(root).toHaveAttribute('data-compare-mode', 'onion');
  expect(screen.getByRole('slider')).toHaveAttribute('aria-valuetext', '30% after');
});

it('side-by-side: sin slider ni handle, ambos lados visibles', () => {
  render(<CompareSlider before={<img alt="a" src="/a.png" />} after={<img alt="b" src="/b.png" />} compareMode="side-by-side" label="C" />);
  expect(screen.queryByRole('slider')).toBeNull();
  expect(screen.getByRole('img', { name: 'a' })).toBeInTheDocument();
  expect(screen.getByRole('img', { name: 'b' })).toBeInTheDocument();
});

it('default wipe: comportamiento actual intacto', () => {
  render(<CompareSlider before={<img alt="" src="/a.png" />} after={<img alt="" src="/b.png" />} label="C" />);
  expect(screen.getByRole('slider').closest('.mk-compare')).toHaveAttribute('data-compare-mode', 'wipe');
});
```

- [ ] **Step 2 — FAIL** → **Step 3 — implementación**:
  - Tipo: `export type CompareSliderMode = 'wipe' | 'onion' | 'blink' | 'side-by-side';` y prop `compareMode?: CompareSliderMode` (default `'wipe'`).
  - Root: `data-compare-mode={compareMode}`.
  - `const hasSlider = compareMode === 'wipe' || compareMode === 'onion';` — con `!hasSlider`: no renderizar divisor ni handle, y los handlers de puntero/teclado del surface hacen early-return.
  - Handle en onion: añadir `aria-valuetext={`${Math.round(position)}% after`}`.
  - CSS (bloque nuevo al final de la sección CompareSlider):

```css
.mk-compare[data-compare-mode='onion'] .mk-compare__after {
  clip-path: none;
  opacity: calc(var(--mk-compare-pos) / 100);
}
.mk-compare[data-compare-mode='onion'] .mk-compare__divider {
  display: none;
}
.mk-compare[data-compare-mode='side-by-side'] {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 2px;
}
.mk-compare[data-compare-mode='side-by-side'][data-orientation='vertical'] {
  grid-template-columns: 1fr;
  grid-template-rows: 1fr 1fr;
}
.mk-compare[data-compare-mode='side-by-side'] .mk-compare__after {
  position: static;
  clip-path: none;
}
.mk-compare[data-compare-mode='side-by-side'] .mk-compare__divider,
.mk-compare[data-compare-mode='side-by-side'] .mk-compare__handle {
  display: none;
}
```

  - `media-lightbox.tsx`: el tipo del prop `compare` gana `compareMode?: CompareSliderMode` y se pasa al `<CompareSlider>` embebido (side-by-side dentro del lightbox hereda el zoom/pan del visor — zoom sincronizado, ver spec A3).
- [ ] **Step 4 — PASS + suite completa** → **Step 5 — commit** `feat(media-kit): compareMode onion and side-by-side`

### Task 5: `compareMode="blink"`

**Files:** Modify `compare-slider.tsx` · `styles.css` · Create `packages/media-kit/src/internal/prefers-reduced-motion.ts` · Test `compare-slider.test.tsx`.

**Interfaces:** Produces: modo blink (alterna lados cada 800 ms, botón `role="switch"` pausa/reanuda reutilizando `pauseLabel`/`resumeLabel`); util interna `prefersReducedMotion()` (reutilizada por T7/T17/T18/T20 en sus propios módulos de la app — en la app se duplica como helper local `lib/`, ver T17).

- [ ] **Step 1 — tests en rojo** (fake timers):

```tsx
it('blink alterna los lados cada 800ms y el switch pausa', () => {
  vi.useFakeTimers();
  render(<CompareSlider before={<img alt="" src="/a.png" />} after={<img alt="" src="/b.png" />} compareMode="blink" label="C" />);
  const root = document.querySelector('.mk-compare')!;
  expect(root).toHaveAttribute('data-blink-side', 'before');
  act(() => vi.advanceTimersByTime(800));
  expect(root).toHaveAttribute('data-blink-side', 'after');
  fireEvent.click(screen.getByRole('switch'));
  act(() => vi.advanceTimersByTime(1600));
  expect(root).toHaveAttribute('data-blink-side', 'after'); // pausado
  vi.useRealTimers();
});

it('blink arranca pausado con prefers-reduced-motion', () => {
  stubReducedMotion(true); // helper del test: window.matchMedia mock
  render(<CompareSlider before={<img alt="" src="/a.png" />} after={<img alt="" src="/b.png" />} compareMode="blink" label="C" />);
  expect(screen.getByRole('switch')).toHaveAttribute('aria-checked', 'false');
});
```

- [ ] **Step 2 — FAIL** → **Step 3 — implementación**:
  - `internal/prefers-reduced-motion.ts`:

```ts
export function prefersReducedMotion(): boolean {
  return typeof window !== 'undefined' && !!window.matchMedia?.('(prefers-reduced-motion: reduce)').matches;
}
```

  - En `CompareSlider`: `const BLINK_INTERVAL_MS = 800;`

```tsx
const [blinkShowsAfter, setBlinkShowsAfter] = useState(false);
const [blinkRunning, setBlinkRunning] = useState(() => compareMode === 'blink' && !prefersReducedMotion());
useEffect(() => {
  if (compareMode !== 'blink' || !blinkRunning) return;
  const id = window.setInterval(() => setBlinkShowsAfter((v) => !v), BLINK_INTERVAL_MS);
  return () => window.clearInterval(id);
}, [compareMode, blinkRunning]);
```

  - Root en blink: `data-blink-side={blinkShowsAfter ? 'after' : 'before'}`; sin handle/divisor (extiende `hasSlider` de T4). Botón:

```tsx
{compareMode === 'blink' && (
  <button
    type="button"
    role="switch"
    aria-checked={blinkRunning}
    className="mk-compare__blink-toggle"
    onClick={() => setBlinkRunning((v) => !v)}
  >
    {blinkRunning ? pauseLabel : resumeLabel}
  </button>
)}
```

  - CSS: `.mk-compare[data-compare-mode='blink'] .mk-compare__after { clip-path: none; opacity: 0; }` · `.mk-compare[data-compare-mode='blink'][data-blink-side='after'] .mk-compare__after { opacity: 1; }` · divisor oculto · `.mk-compare__blink-toggle` con estilos `--mk-control-*` (mismo look que el toggle de pausa existente).
- [ ] **Step 4 — PASS + suite completa** → **Step 5 — commit** `feat(media-kit): compareMode blink with pause switch and reduced-motion default`

### Task 6: `SpotlightReveal`

**Files:** Create `packages/media-kit/src/spotlight-reveal/spotlight-reveal.tsx` + `spotlight-reveal.test.tsx` · Modify `styles.css`.

**Interfaces:** Produces el componente de la spec A4 (API exacta allí). Export en Task 9.

- [ ] **Step 1 — tests en rojo** (casos mínimos):

```tsx
it('renderiza lados MediaSource como imágenes y expone el aria-label', () => {
  render(<SpotlightReveal base={{ src: '/a.png', alt: 'base' }} reveal={{ src: '/b.png', alt: '' }} label="Spotlight demo" />);
  expect(screen.getByLabelText('Spotlight demo')).toBeInTheDocument();
  expect(screen.getByRole('img', { name: 'base' })).toBeInTheDocument();
});

it('las flechas mueven la lente y Escape la oculta', () => {
  render(<SpotlightReveal base={<div />} reveal={<div />} label="S" />);
  const root = screen.getByLabelText('S');
  root.focus();
  expect(root).toHaveAttribute('data-active');
  fireEvent.keyDown(root, { key: 'ArrowRight' });
  expect(root.style.getPropertyValue('--mk-spot-x')).toBe('55%');
  fireEvent.keyDown(root, { key: 'Escape' });
  expect(root).not.toHaveAttribute('data-active');
});

it('el puntero posiciona la lente', () => {
  render(<SpotlightReveal base={<div />} reveal={<div />} label="S" />);
  const root = screen.getByLabelText('S');
  vi.spyOn(root, 'getBoundingClientRect').mockReturnValue({ left: 0, top: 0, width: 200, height: 100 } as DOMRect);
  fireEvent.pointerMove(root, { clientX: 100, clientY: 25 });
  expect(root.style.getPropertyValue('--mk-spot-x')).toBe('50%');
  expect(root.style.getPropertyValue('--mk-spot-y')).toBe('25%');
});
```

- [ ] **Step 2 — FAIL** → **Step 3 — implementación completa**:

```tsx
import { useRef, useState, type CSSProperties, type KeyboardEvent, type PointerEvent, type ReactNode } from 'react';
import { isMediaSource, type MediaSource } from '../media-source/media-source';

export type SpotlightRevealProps = {
  base: ReactNode | MediaSource;
  reveal: ReactNode | MediaSource;
  label: string;
  radius?: number;
  defaultPosition?: { x: number; y: number };
  overlayLabels?: { base?: string; reveal?: string };
  className?: string;
};

const STEP = 5;
const FINE_STEP = 1;

function clampPct(value: number): number {
  return Math.min(100, Math.max(0, value));
}

function renderSide(side: ReactNode | MediaSource): ReactNode {
  if (isMediaSource(side)) {
    return <img src={side.src} alt={side.alt ?? ''} draggable={false} />;
  }
  return side;
}

/** Lente que revela `reveal` sobre `base` bajo el puntero; flechas la mueven con teclado. */
export function SpotlightReveal({
  base,
  reveal,
  label,
  radius = 110,
  defaultPosition = { x: 50, y: 50 },
  overlayLabels,
  className,
}: SpotlightRevealProps) {
  const rootRef = useRef<HTMLDivElement>(null);
  const [position, setPosition] = useState(defaultPosition);
  const [active, setActive] = useState(false);

  function onPointerMove(event: PointerEvent<HTMLDivElement>) {
    const rect = rootRef.current?.getBoundingClientRect();
    if (!rect || rect.width === 0 || rect.height === 0) return;
    setPosition({
      x: clampPct(((event.clientX - rect.left) / rect.width) * 100),
      y: clampPct(((event.clientY - rect.top) / rect.height) * 100),
    });
    setActive(true);
  }

  function onKeyDown(event: KeyboardEvent<HTMLDivElement>) {
    const step = event.shiftKey ? FINE_STEP : STEP;
    const moves: Record<string, { dx: number; dy: number }> = {
      ArrowRight: { dx: step, dy: 0 },
      ArrowLeft: { dx: -step, dy: 0 },
      ArrowDown: { dx: 0, dy: step },
      ArrowUp: { dx: 0, dy: -step },
    };
    if (event.key === 'Escape') {
      setActive(false);
      return;
    }
    if (event.key === 'Home') {
      event.preventDefault();
      setPosition({ x: 50, y: 50 });
      setActive(true);
      return;
    }
    const move = moves[event.key];
    if (!move) return;
    event.preventDefault();
    setPosition((p) => ({ x: clampPct(p.x + move.dx), y: clampPct(p.y + move.dy) }));
    setActive(true);
  }

  const style = {
    '--mk-spot-x': `${position.x}%`,
    '--mk-spot-y': `${position.y}%`,
    '--mk-spot-radius': `${radius}px`,
  } as CSSProperties;

  return (
    <div
      ref={rootRef}
      className={['mk-spotlight', className].filter(Boolean).join(' ')}
      tabIndex={0}
      aria-label={label}
      aria-roledescription="spotlight"
      data-active={active || undefined}
      style={style}
      onPointerMove={onPointerMove}
      onPointerLeave={() => setActive(false)}
      onFocus={() => setActive(true)}
      onBlur={() => setActive(false)}
      onKeyDown={onKeyDown}
    >
      <div className="mk-spotlight__base">{renderSide(base)}</div>
      <div className="mk-spotlight__reveal" aria-hidden>
        {renderSide(reveal)}
      </div>
      <div className="mk-spotlight__lens" aria-hidden />
      {overlayLabels?.base && (
        <span className="mk-spotlight__badge" data-side="base" aria-hidden>
          {overlayLabels.base}
        </span>
      )}
      {overlayLabels?.reveal && (
        <span className="mk-spotlight__badge" data-side="reveal" aria-hidden>
          {overlayLabels.reveal}
        </span>
      )}
    </div>
  );
}
```

(Ajustar la ruta/forma real de `isMediaSource`/`MediaSource` al módulo existente. `alt` del lado `reveal` siempre `''` no es necesario — respeta el `alt` del MediaSource como hace CompareSlider.)

  - CSS `styles.css`, bloque nuevo `/* SpotlightReveal */`:

```css
.mk-spotlight {
  position: relative;
  overflow: hidden;
  border-radius: var(--mk-radius);
  touch-action: none;
  cursor: crosshair;
}
.mk-spotlight:focus-visible {
  outline: 2px solid var(--mk-focus-ring);
  outline-offset: 2px;
}
.mk-spotlight__base img,
.mk-spotlight__reveal img {
  display: block;
  width: 100%;
  height: auto;
}
.mk-spotlight__reveal {
  position: absolute;
  inset: 0;
  clip-path: circle(0px at var(--mk-spot-x) var(--mk-spot-y));
  transition: clip-path 160ms ease-out;
}
.mk-spotlight[data-active] .mk-spotlight__reveal {
  clip-path: circle(var(--mk-spot-radius) at var(--mk-spot-x) var(--mk-spot-y));
}
.mk-spotlight__lens {
  position: absolute;
  left: var(--mk-spot-x);
  top: var(--mk-spot-y);
  width: calc(var(--mk-spot-radius) * 2);
  height: calc(var(--mk-spot-radius) * 2);
  transform: translate(-50%, -50%);
  border: 2px solid var(--mk-spot-ring, rgba(255, 255, 255, 0.9));
  border-radius: 50%;
  opacity: 0;
  pointer-events: none;
  transition: opacity 120ms ease-out;
}
.mk-spotlight[data-active] .mk-spotlight__lens {
  opacity: 1;
}
.mk-spotlight__badge {
  position: absolute;
  top: 8px;
  padding: 2px 8px;
  border-radius: 4px;
  background: var(--mk-overlay-bg);
  color: var(--mk-control-color);
  font-size: 12px;
  pointer-events: none;
}
.mk-spotlight__badge[data-side='base'] {
  left: 8px;
}
.mk-spotlight__badge[data-side='reveal'] {
  right: 8px;
}
@media (prefers-reduced-motion: reduce) {
  .mk-spotlight__reveal,
  .mk-spotlight__lens {
    transition: none;
  }
}
```

- [ ] **Step 4 — PASS + suite completa** → **Step 5 — commit** `feat(media-kit): SpotlightReveal component`

### Task 7: `FilterGallery`

**Files:** Create `packages/media-kit/src/filter-gallery/filter-gallery.tsx` + `filter-gallery.test.tsx` · Modify `styles.css`.

**Interfaces:** Produces el componente de la spec A5 (API exacta allí; salida de items inmediata en v1). Usa `prefersReducedMotion()` de T5 y `element.animate` con guard (jsdom no lo implementa).

- [ ] **Step 1 — tests en rojo**:

```tsx
const ITEMS = [
  { id: 'a', categories: ['image'], node: <span>A</span> },
  { id: 'b', categories: ['video'], node: <span>B</span> },
  { id: 'c', categories: ['image', 'video'], node: <span>C</span> },
];

it('sin filtro muestra todo; filtrar por categoría oculta el resto (no controlado)', () => {
  render(<FilterGallery items={ITEMS} categories={[{ id: 'image', label: 'Image' }, { id: 'video', label: 'Video' }]} label="Gallery" allLabel="All" />);
  expect(screen.getAllByRole('listitem')).toHaveLength(3);
  fireEvent.click(screen.getByRole('button', { name: 'Video' }));
  expect(screen.getAllByRole('listitem')).toHaveLength(2);
  expect(screen.getByRole('button', { name: 'Video' })).toHaveAttribute('aria-pressed', 'true');
  expect(screen.getByRole('button', { name: 'All' })).toHaveAttribute('aria-pressed', 'false');
});

it('modo controlado: filter + onFilterChange', () => {
  const onFilterChange = vi.fn();
  render(<FilterGallery items={ITEMS} categories={[{ id: 'image', label: 'Image' }]} filter="image" onFilterChange={onFilterChange} label="G" allLabel="All" />);
  expect(screen.getAllByRole('listitem')).toHaveLength(2);
  fireEvent.click(screen.getByRole('button', { name: 'All' }));
  expect(onFilterChange).toHaveBeenCalledWith(null);
  expect(screen.getAllByRole('listitem')).toHaveLength(2); // controlado: no cambia solo
});

it('anima con FLIP al filtrar (spy en element.animate) y no anima con reduced-motion', () => {
  const animate = vi.fn();
  Object.defineProperty(HTMLElement.prototype, 'animate', { configurable: true, value: animate });
  const { rerender } = render(<FilterGallery items={ITEMS} filter={null} label="G" allLabel="All" />);
  rerender(<FilterGallery items={ITEMS} filter="image" label="G" allLabel="All" />);
  expect(animate).toHaveBeenCalled();
});
```

(El caso reduced-motion: con `stubReducedMotion(true)`, mismo rerender, `animate` NO llamado. En modo controlado el FLIP se dispara desde el cambio de `filter` — la medición First ocurre en un `useLayoutEffect` previo vía ref del valor anterior, ver Step 3.)

- [ ] **Step 2 — FAIL** → **Step 3 — implementación completa**:

```tsx
import { useLayoutEffect, useRef, useState, type ReactNode } from 'react';
import { prefersReducedMotion } from '../internal/prefers-reduced-motion';

export type FilterGalleryItem = { id: string; categories: readonly string[]; node: ReactNode };
export type FilterGalleryCategory = { id: string; label: string };

export type FilterGalleryProps = {
  items: readonly FilterGalleryItem[];
  filter?: string | null;
  defaultFilter?: string | null;
  onFilterChange?: (filter: string | null) => void;
  categories?: readonly FilterGalleryCategory[];
  allLabel?: string;
  label: string;
  duration?: number;
  className?: string;
};

/** Grid filtrable con recolocación FLIP (WAAPI); reduced-motion = cambio instantáneo. */
export function FilterGallery({
  items,
  filter,
  defaultFilter = null,
  onFilterChange,
  categories,
  allLabel = 'All',
  label,
  duration = 240,
  className,
}: FilterGalleryProps) {
  const [internalFilter, setInternalFilter] = useState(defaultFilter);
  const activeFilter = filter !== undefined ? filter : internalFilter;
  const gridRef = useRef<HTMLUListElement>(null);
  const previousRectsRef = useRef<Map<string, DOMRect>>(new Map());
  const previousFilterRef = useRef(activeFilter);

  const visible =
    activeFilter == null ? items : items.filter((item) => item.categories.includes(activeFilter));

  // First: si el filtro cambió, medir ANTES de pintar el layout nuevo es imposible aquí
  // (ya se re-renderizó), así que las medidas se toman en cada render y se comparan
  // en el layout effect solo cuando el filtro cambia.
  useLayoutEffect(() => {
    const grid = gridRef.current;
    const filterChanged = previousFilterRef.current !== activeFilter;
    previousFilterRef.current = activeFilter;
    if (!grid) return;
    if (filterChanged && !prefersReducedMotion()) {
      for (const el of grid.querySelectorAll<HTMLElement>('[data-fg-id]')) {
        const id = el.dataset.fgId;
        const before = id ? previousRectsRef.current.get(id) : undefined;
        const after = el.getBoundingClientRect();
        if (typeof el.animate !== 'function') continue;
        if (before) {
          const dx = before.left - after.left;
          const dy = before.top - after.top;
          if (dx !== 0 || dy !== 0) {
            el.animate(
              [{ transform: `translate(${dx}px, ${dy}px)` }, { transform: 'translate(0, 0)' }],
              { duration, easing: 'ease-out' },
            );
          }
        } else {
          el.animate(
            [
              { opacity: 0, transform: 'scale(0.96)' },
              { opacity: 1, transform: 'scale(1)' },
            ],
            { duration, easing: 'ease-out' },
          );
        }
      }
    }
    previousRectsRef.current = new Map(
      [...grid.querySelectorAll<HTMLElement>('[data-fg-id]')].map((el) => [
        el.dataset.fgId ?? '',
        el.getBoundingClientRect(),
      ]),
    );
  });

  function select(next: string | null) {
    onFilterChange?.(next);
    if (filter === undefined) setInternalFilter(next);
  }

  return (
    <div className={['mk-filter-gallery', className].filter(Boolean).join(' ')}>
      {categories && (
        <div role="group" aria-label={label} className="mk-filter-gallery__filters">
          <button
            type="button"
            aria-pressed={activeFilter == null}
            onClick={() => select(null)}
          >
            {allLabel}
          </button>
          {categories.map((category) => (
            <button
              key={category.id}
              type="button"
              aria-pressed={activeFilter === category.id}
              onClick={() => select(category.id)}
            >
              {category.label}
            </button>
          ))}
        </div>
      )}
      <ul aria-label={label} className="mk-filter-gallery__grid" ref={gridRef}>
        {visible.map((item) => (
          <li key={item.id} data-fg-id={item.id}>
            {item.node}
          </li>
        ))}
      </ul>
    </div>
  );
}
```

  - CSS (`/* FilterGallery */`): `.mk-filter-gallery__filters` flex+gap con botones `--mk-control-*` (aria-pressed=true → fondo `--mk-handle-color`); `.mk-filter-gallery__grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(120px, 1fr)); gap: 8px; list-style: none; margin: 0; padding: 0; }`.
- [ ] **Step 4 — PASS + suite completa** → **Step 5 — commit** `feat(media-kit): FilterGallery with FLIP reflow`

### Task 8: `VideoScrubPreview`

**Files:** Create `packages/media-kit/src/video-scrub-preview/video-scrub-preview.tsx` + `video-scrub-preview.test.tsx` · Modify `styles.css`.

**Interfaces:** Produces el componente de la spec A6.

- [ ] **Step 1 — tests en rojo**:

```tsx
function setupVideo(duration = 10) {
  const { container } = render(<VideoScrubPreview src="/demo.mp4" label="Preview" />);
  const root = screen.getByLabelText('Preview');
  const video = container.querySelector('video')!;
  Object.defineProperty(video, 'duration', { configurable: true, value: duration });
  vi.spyOn(root, 'getBoundingClientRect').mockReturnValue({ left: 0, top: 0, width: 200, height: 100 } as DOMRect);
  return { root, video };
}

it('el puntero hace scrub proporcional', () => {
  const { root, video } = setupVideo(10);
  fireEvent.pointerMove(root, { clientX: 100, clientY: 10 });
  expect(video.currentTime).toBeCloseTo(5);
});

it('al salir vuelve al inicio', () => {
  const { root, video } = setupVideo(10);
  fireEvent.pointerMove(root, { clientX: 150, clientY: 10 });
  fireEvent.pointerLeave(root);
  expect(video.currentTime).toBe(0);
});

it('teclado: flechas ±5%', () => {
  const { root, video } = setupVideo(10);
  root.focus();
  fireEvent.keyDown(root, { key: 'ArrowRight' });
  expect(video.currentTime).toBeCloseTo(0.5);
});

it('sin metadata (duration NaN) no rompe', () => {
  const { root, video } = setupVideo(Number.NaN);
  fireEvent.pointerMove(root, { clientX: 100, clientY: 10 });
  expect(video.currentTime).toBe(0);
});
```

(Nota jsdom: el scrub usa asignación directa síncrona a `currentTime` — el throttle rAF envuelve solo el caso puntero; en el test, stub de `requestAnimationFrame` que ejecuta el callback inmediato, patrón ya usado en tests del paquete si existe; si no, `vi.stubGlobal('requestAnimationFrame', (cb) => { cb(0); return 0; })`.)

- [ ] **Step 2 — FAIL** → **Step 3 — implementación completa**:

```tsx
import { useRef, useState, type KeyboardEvent, type PointerEvent } from 'react';

export type VideoScrubPreviewProps = {
  src: string;
  poster?: string;
  label: string;
  scrubOnFocus?: boolean;
  className?: string;
};

const KEY_STEP = 0.05;

/** Vídeo que se recorre al mover el puntero (estilo miniaturas de YouTube). */
export function VideoScrubPreview({ src, poster, label, scrubOnFocus = true, className }: VideoScrubPreviewProps) {
  const rootRef = useRef<HTMLDivElement>(null);
  const videoRef = useRef<HTMLVideoElement>(null);
  const frameRef = useRef<number | null>(null);
  const [progress, setProgress] = useState(0);

  function scrubTo(fraction: number) {
    const video = videoRef.current;
    if (!video || !Number.isFinite(video.duration) || video.duration <= 0) return;
    const clamped = Math.min(1, Math.max(0, fraction));
    video.currentTime = clamped * video.duration;
    setProgress(clamped * 100);
  }

  function onPointerMove(event: PointerEvent<HTMLDivElement>) {
    const rect = rootRef.current?.getBoundingClientRect();
    if (!rect || rect.width === 0) return;
    const fraction = (event.clientX - rect.left) / rect.width;
    if (frameRef.current != null) cancelAnimationFrame(frameRef.current);
    frameRef.current = requestAnimationFrame(() => {
      frameRef.current = null;
      scrubTo(fraction);
    });
  }

  function onPointerLeave() {
    if (frameRef.current != null) cancelAnimationFrame(frameRef.current);
    frameRef.current = null;
    const video = videoRef.current;
    if (video) video.currentTime = 0;
    setProgress(0);
  }

  function onKeyDown(event: KeyboardEvent<HTMLDivElement>) {
    if (!scrubOnFocus) return;
    const fraction = progress / 100;
    if (event.key === 'ArrowRight') scrubTo(fraction + KEY_STEP);
    else if (event.key === 'ArrowLeft') scrubTo(fraction - KEY_STEP);
    else if (event.key === 'Home') scrubTo(0);
    else if (event.key === 'End') scrubTo(1);
    else return;
    event.preventDefault();
  }

  return (
    <div
      ref={rootRef}
      className={['mk-scrub', className].filter(Boolean).join(' ')}
      tabIndex={scrubOnFocus ? 0 : undefined}
      aria-label={label}
      style={{ '--mk-scrub-pos': `${progress}%` } as React.CSSProperties}
      onPointerMove={onPointerMove}
      onPointerLeave={onPointerLeave}
      onKeyDown={onKeyDown}
    >
      <video ref={videoRef} src={src} poster={poster} muted playsInline preload="metadata" aria-hidden />
      <div className="mk-scrub__bar" aria-hidden />
    </div>
  );
}
```

  - CSS (`/* VideoScrubPreview */`): root `position: relative; overflow: hidden; border-radius: var(--mk-radius); cursor: ew-resize;` + focus-visible ring `--mk-focus-ring`; `video { display:block; width:100%; height:auto; }`; `.mk-scrub__bar { position:absolute; left:0; bottom:0; height:3px; width: var(--mk-scrub-pos); background: var(--mk-handle-color); transition: width 60ms linear; }`.
- [ ] **Step 4 — PASS + suite completa** → **Step 5 — commit** `feat(media-kit): VideoScrubPreview component`

### Task 9: media-kit 0.5.0 — exports, README, CHANGELOG

**Files:** Modify `packages/media-kit/src/index.ts` · `package.json` (`0.5.0`) · `README.md` · `CHANGELOG.md`.

- [ ] **Step 1:** `index.ts` añade:

```ts
export { SpotlightReveal } from './spotlight-reveal/spotlight-reveal';
export type { SpotlightRevealProps } from './spotlight-reveal/spotlight-reveal';
export { FilterGallery } from './filter-gallery/filter-gallery';
export type { FilterGalleryProps, FilterGalleryItem, FilterGalleryCategory } from './filter-gallery/filter-gallery';
export { VideoScrubPreview } from './video-scrub-preview/video-scrub-preview';
export type { VideoScrubPreviewProps } from './video-scrub-preview/video-scrub-preview';
export type { CompareSliderMode } from './compare-slider/compare-slider';
```

- [ ] **Step 2:** README: tabla de props por componente nuevo + `compareMode` en la tabla de CompareSlider + variables nuevas (`--mk-spot-ring`, `--mk-scrub-pos` interna, tooltips `data-mk-tooltip[-pos]`).
- [ ] **Step 3:** CHANGELOG `## 0.5.0 — <fecha>` con Added/Fixed (patrón Keep a Changelog del repo, negrita por componente, nota final "no breaking changes; every new prop is optional…").
- [ ] **Step 4:** `pnpm --filter @nicobehm/media-kit build` verde (tsup + dts) + gate completo → **Step 5 — commit** `feat(media-kit): release 0.5.0 — new components, compare modes, fullscreen fixes`

## Bloque B — Showcase UX v2

### Task 10 (ORQUESTADOR, no subagente): assets de demo

- [ ] Generar con Magnific MCP: (a) **paisaje** fotográfico vívido (montañas/atardecer, colores saturados) → recodificar a `apps/web/public/demo/landscape.webp` ≤ 200 kB ~1600×900; (b) **clip de vídeo** 3-5 s (paneo suave de paisaje/olas) → `apps/web/public/demo/scrub.mp4` H.264 sin audio ≤ 1.5 MB, más `scrub-poster.webp` (primer frame, ≤ 60 kB).
- [ ] Verificar pesos (`ls -la`) y dimensiones; commit `feat(web): AI-generated demo assets for showcase (landscape, scrub clip)`.
- [ ] Si la generación no está disponible en la sesión: BLOQUEA B (T11, T15, T16 parcial) — escalar al usuario, no inventar assets.

### Task 11: colorización real en los 2 primeros ejemplos

**Files:** Modify `apps/web/src/components/showcase/media-kit-demo.tsx` (+test) · Delete `apps/web/public/demo/photo-before.svg` y `photo-after.svg`.

**Interfaces:** Consumes `landscape.webp` (T10). Produces: los ejemplos drag y hover usan UNA imagen real; el "antes" se deriva con filtro CSS (patrón del retrato).

- [ ] **Step 1 — test en rojo:** en `media-kit-demo.test.tsx`, los dos primeros CompareSlider renderizan `img` con `src="/demo/landscape.webp"` en ambos lados y el lado before lleva `style` con `filter` que contiene `saturate`.
- [ ] **Step 2 — FAIL** → **Step 3:** en ambos ejemplos:

```tsx
before={
  <img
    src="/demo/landscape.webp"
    alt=""
    width={1600}
    height={900}
    style={{ filter: 'saturate(0.12) contrast(0.92) brightness(0.96)' }}
  />
}
after={<img src="/demo/landscape.webp" alt="" width={1600} height={900} />}
```

- [ ] **Step 4:** borrar los SVG antiguos + `grep -rn "photo-before\|photo-after" apps/web` limpio; gate → **Step 5 — commit** `feat(web): real colorization demo from a single AI photo`

### Task 12: Tabs sin desplazamiento (paneles apilados)

**Files:** Modify `apps/web/src/components/ui/tabs/tabs.tsx` · `tabs.test.tsx` (contrato nuevo, cambio documentado).

**Interfaces:** Produces: los `TabPanel` comparten celda de grid (`grid-area` 1/1 de la fila de paneles); el inactivo conserva su caja (`invisible` = `visibility:hidden`) → la altura del bloque = la del panel más alto → cero salto. Fade de 150 ms del activo (`motion-reduce:transition-none`).

- [ ] **Step 1 — tests:** actualizar el describe de 2.6: el panel inactivo ya NO tiene atributo `hidden`; en su lugar `aria-hidden="true"` + clase `invisible`; el activo `opacity-100` y accesible. Añadir: ambos paneles presentes en el DOM; el inactivo no es focusable (`tabIndex` solo en el activo).
- [ ] **Step 2 — FAIL** → **Step 3 — implementación:** root de `Tabs` pasa a `grid` con `grid-template-rows: auto 1fr` implícito por clases: `TabList` fila 1; cada `TabPanel`: `col-start-1 row-start-2` + base `transition-opacity duration-150 motion-reduce:transition-none` + estado `selected ? 'opacity-100' : 'invisible opacity-0'` + `aria-hidden={!selected}` + `tabIndex={selected ? 0 : undefined}`; eliminar `hidden={!selected}`. Actualizar el comentario que prometía "cero layout-shift" con la explicación real (visibility conserva la caja).
- [ ] **Step 4 — PASS + gate** → **Step 5 — commit** `fix(web): tabs reserve panel height — no layout shift on switch`

### Task 13: el índice del showcase filtra

**Files:** Create `apps/web/src/components/showcase/showcase-view.tsx` (+test) · Modify `showcase-index.tsx` (+test) · `apps/web/src/app/[locale]/showcase/page.tsx` · `apps/web/messages/{es,en}.json`.

**Interfaces:** Produces: client component `ShowcaseView({ toc, labels, sections })` donde `sections: { id: string; node: ReactNode }[]` (los `<Section>` server-rendered viajan como props). Estado `filter: string | null`; `ShowcaseIndex` gana prop `onSelect(id: string | null)` (la entrada "Todas", id sintético `__all`, va primera) y deja de tocar `window.location.hash` para scroll — el hash refleja el filtro (deep-link).

- [ ] **Step 1 — tests en rojo** (`showcase-view.test.tsx`):

```tsx
const SECTIONS = [
  { id: 'button', node: <section aria-label="Button">B</section> },
  { id: 'card', node: <section aria-label="Card">C</section> },
];

it('por defecto muestra todas las secciones', () => {
  renderView(); // helper con NextIntlClientProvider
  expect(screen.getByLabelText('Button')).toBeInTheDocument();
  expect(screen.getByLabelText('Card')).toBeInTheDocument();
});

it('seleccionar una sección en el índice muestra solo esa y lo anuncia', () => {
  renderView();
  fireEvent.click(screen.getByRole('option', { name: /card/i }));
  expect(screen.queryByLabelText('Button')).toBeNull();
  expect(screen.getByLabelText('Card')).toBeInTheDocument();
  expect(screen.getByRole('status')).toHaveTextContent(/card/i);
});

it('la opción "Todas" restaura todo', () => {
  renderView();
  fireEvent.click(screen.getByRole('option', { name: /card/i }));
  fireEvent.click(screen.getByRole('option', { name: /todas/i }));
  expect(screen.getByLabelText('Button')).toBeInTheDocument();
});
```

- [ ] **Step 2 — FAIL** → **Step 3 — implementación:**
  - `showcase-view.tsx` (client): estado `filter`; efecto de montaje que lee `location.hash` inicial (si coincide con un id, filtra) y efecto que escribe `history.replaceState(null, '', filter ? '#' + filter : location.pathname)`; render: `<ShowcaseIndex items={[all, ...toc]} onSelect={...}/>` + `sections.filter(s => !filter || s.id === filter).map(s => <div key={s.id} className="transition-opacity duration-150 motion-reduce:transition-none">{s.node}</div>)` + `<p role="status" className="sr-only">{filter ? labels.showing.replace('{section}', title) : labels.showingAll}</p>`.
  - `showcase-index.tsx`: `onSelect` prop en lugar del hash-scroll (líneas 25-27); la entrada `__all` primera con `selectedId` marcando el filtro activo (reutiliza T10-F3.5).
  - `page.tsx`: construir `sections` (los 6+nuevos `<Section>` actuales) y `toc` y pasarlos a `<ShowcaseView>`; keys nuevas `showcase.index.all` ("Todas"/"All"), `showcase.index.showing` ("Mostrando: {section}"/"Showing: {section}"), `showcase.index.showingAll` ("Mostrando todas las secciones"/"Showing all sections") en ambos locales.
- [ ] **Step 4 — PASS + gate** → **Step 5 — commit** `feat(web): showcase index filters sections instead of scrolling`

### Task 14: radii y estados coherentes

**Files:** Modify `apps/web/src/app/[locale]/showcase/page.tsx` (pre → `rounded-control`) · `apps/web/src/app/globals.css` (`.prose-portfolio code` radius) · `apps/web/src/components/ui/input/input.tsx` · `select/select.tsx` (+tests si asertan className).

- [ ] **Step 1:** `<pre>` del panel api: `rounded-card` → `rounded-control`.
- [ ] **Step 2:** `.prose-portfolio code { border-radius: 0.25rem }` → `border-radius: calc(var(--radius-control) / 2);`.
- [ ] **Step 3:** `Input` y `Select`: añadir `transition-colors hover:border-fg-muted` (mismo par que Button secondary). Tests de clase si existen: actualizar.
- [ ] **Step 4 — gate** → **Step 5 — commit** `fix(web): coherent radii and hover states across showcase controls`

### Task 15: demos — `compareMode` y `SpotlightReveal`

**Files:** Create `apps/web/src/components/showcase/compare-modes-demo.tsx` (+test) y `spotlight-demo.tsx` (+test) · Modify `media-kit-demo.tsx` o `page.tsx` (montaje) · `messages/{es,en}.json`.

**Interfaces:** Consumes media-kit 0.5 (T4-T6) y `landscape.webp`. Produces: dos sub-bloques nuevos en la sección media-kit, con intro y labels i18n (`showcase.mediaKit.modes*`, `showcase.mediaKit.spotlight*`).

- [ ] **Step 1 — tests en rojo:** `compare-modes-demo`: renderiza un grupo de botones (wipe/onion/blink/side-by-side, labels por props) y al pulsar "Onion" el `.mk-compare` tiene `data-compare-mode="onion"`. `spotlight-demo`: renderiza `SpotlightReveal` con `aria-roledescription="spotlight"` y la imagen del paisaje.
- [ ] **Step 2 — FAIL** → **Step 3 — implementación:**
  - `compare-modes-demo.tsx` (client): estado `mode: CompareSliderMode`; botonera `role="group"` con `aria-pressed` (labels por props desde messages); `<CompareSlider compareMode={mode} before={…filtro desaturado…} after={…} label={label} />` (misma pareja de imgs que T11).
  - `spotlight-demo.tsx` (client): `<SpotlightReveal base={<img … style={{ filter: 'grayscale(1)' }} />} reveal={<img … />} label={label} overlayLabels={{ base: labels.base, reveal: labels.reveal }} />`.
  - Montaje en la sección media-kit con `<figure>/<figcaption>` (patrón demos 2.6) + keys es/en.
- [ ] **Step 4 — PASS + gate** → **Step 5 — commit** `feat(web): showcase demos for compare modes and SpotlightReveal`

### Task 16: demos — `FilterGallery` y `VideoScrubPreview` + TOC

**Files:** Create `filter-gallery-demo.tsx` (+test) y `video-scrub-demo.tsx` (+test) · Modify sección media-kit + TOC (`page.tsx`) · `messages/{es,en}.json`.

- [ ] **Step 1 — tests en rojo:** `filter-gallery-demo`: 8 tiles (SVG inline de categorías image/video/audio con `role="img"` + title), filtrar por "Video" reduce los `listitem`. `video-scrub-demo`: renderiza `VideoScrubPreview` con `src="/demo/scrub.mp4"` y `poster="/demo/scrub-poster.webp"`.
- [ ] **Step 2 — FAIL** → **Step 3:** implementar ambos demos (client, labels por messages); los tiles del gallery-demo son SVGs inline pequeños token-neutros (grises/acento vía `currentColor` — NO colores hardcodeados en el TSX). Añadir al TOC del índice las entradas de los sub-bloques nuevos si el TOC es por sección (decisión: el TOC sigue siendo por sección top-level; los sub-bloques no entran — verificar con el filtrado de T13 que la sección media-kit crecida sigue siendo usable).
- [ ] **Step 4 — PASS + gate** → **Step 5 — commit** `feat(web): showcase demos for FilterGallery and VideoScrubPreview`

## Bloque C — componentes de impacto en la app

### Task 17: `AnimatedMetric` + métricas animadas en destacados

**Files:** Create `apps/web/src/components/ui/animated-metric/animated-metric.tsx` + test · `apps/web/src/lib/reduced-motion.ts` (helper local app) · Modify `apps/web/src/components/projects/project-card.tsx` (+test).

**Interfaces:** Produces `AnimatedMetric({ value, durationMs? })` (client): número animado 0→N al entrar en viewport, conservando prefijo/sufijo/separadores del literal (`'25+'`, `'1.000+'`); nodo animado `aria-hidden`, valor real en `<span className="sr-only">`. `ProjectCard` gana `animateMetrics?: boolean` (default false) que envuelve cada `<dd>` con `AnimatedMetric`; `FeaturedProjects` lo activa.

- [ ] **Step 1 — tests en rojo:**

```tsx
it('sin IntersectionObserver muestra el valor final', () => {
  vi.stubGlobal('IntersectionObserver', undefined);
  render(<AnimatedMetric value="25+" />);
  expect(screen.getByText('25+', { selector: '[aria-hidden]' })).toBeInTheDocument();
});

it('expone el valor real para lectores de pantalla', () => {
  render(<AnimatedMetric value="1.000+" />);
  expect(screen.getByText('1.000+', { selector: '.sr-only' })).toBeInTheDocument();
});

it('formatLike conserva separador y sufijo', () => {
  expect(formatLike('1.000+', 500)).toBe('500+');
  expect(formatLike('1.000+', 1000)).toBe('1.000+');
  expect(formatLike('40+', 12)).toBe('12+');
});
```

- [ ] **Step 2 — FAIL** → **Step 3 — implementación:**

```tsx
'use client';

import { useEffect, useRef, useState } from 'react';
import { prefersReducedMotion } from '@/lib/reduced-motion';

/** Reproduce el patrón del literal original (separador de miles + sufijo) sobre n. */
export function formatLike(original: string, n: number): string {
  const match = /^(\D*)([\d.,\s]+)(.*)$/.exec(original);
  if (!match) return original;
  const [, prefix = '', digits = '', suffix = ''] = match;
  const separator = /[.,\s]/.exec(digits)?.[0];
  let body = String(n);
  if (separator) body = body.replace(/\B(?=(\d{3})+(?!\d))/g, separator);
  return `${prefix}${body}${suffix}`;
}

type AnimatedMetricProps = { value: string; durationMs?: number };

export function AnimatedMetric({ value, durationMs = 900 }: AnimatedMetricProps) {
  const targetRef = useRef<HTMLSpanElement>(null);
  const target = Number((/[\d.,\s]+/.exec(value)?.[0] ?? '0').replace(/[.,\s]/g, ''));
  const [display, setDisplay] = useState(() =>
    typeof IntersectionObserver === 'undefined' || prefersReducedMotion() ? value : formatLike(value, 0),
  );

  useEffect(() => {
    const node = targetRef.current;
    if (!node || typeof IntersectionObserver === 'undefined' || prefersReducedMotion()) return;
    let frame: number | null = null;
    const observer = new IntersectionObserver(([entry]) => {
      if (!entry?.isIntersecting) return;
      observer.disconnect();
      const start = performance.now();
      const tick = (now: number) => {
        const t = Math.min(1, (now - start) / durationMs);
        const eased = 1 - (1 - t) ** 3;
        setDisplay(formatLike(value, Math.round(target * eased)));
        if (t < 1) frame = requestAnimationFrame(tick);
      };
      frame = requestAnimationFrame(tick);
    });
    observer.observe(node);
    return () => {
      observer.disconnect();
      if (frame != null) cancelAnimationFrame(frame);
    };
  }, [value, target, durationMs]);

  return (
    <span ref={targetRef}>
      <span aria-hidden>{display}</span>
      <span className="sr-only">{value}</span>
    </span>
  );
}
```

  - `lib/reduced-motion.ts`: mismo helper de una línea que el del paquete (no importar del paquete: es API interna).
  - `project-card.tsx`: prop `animateMetrics?: boolean`; el `<dd>` renderiza `animateMetrics ? <AnimatedMetric value={metric.value} /> : metric.value`. `featured-projects.tsx` pasa `animateMetrics` a sus ProjectCard. Tests de card: con `animateMetrics` el valor sigue accesible (sr-only).
- [ ] **Step 4 — PASS + gate** → **Step 5 — commit** `feat(web): AnimatedMetric count-up on featured project metrics`

### Task 18: `TiltCard` en destacados

**Files:** Create `apps/web/src/components/ui/tilt-card/tilt-card.tsx` + test · Modify `featured-projects.tsx` (+test).

**Interfaces:** Produces `TiltCard({ children, maxTilt?=4 })` (client): tilt 3D + glow que sigue el puntero vía CSS vars; inerte en touch/reduced-motion. `FeaturedProjects` envuelve cada card del grid (incluida MoreProjectsCard) con `<TiltCard>`.

- [ ] **Step 1 — tests en rojo:**

```tsx
it('con puntero fino, mover el ratón inclina y posiciona el glow', () => {
  stubMatchMedia({ '(hover: hover) and (pointer: fine)': true, '(prefers-reduced-motion: reduce)': false });
  render(<TiltCard><div>content</div></TiltCard>);
  const root = screen.getByText('content').parentElement!.closest('[data-tilt]')!;
  vi.spyOn(root, 'getBoundingClientRect').mockReturnValue({ left: 0, top: 0, width: 200, height: 100 } as DOMRect);
  fireEvent.pointerMove(root, { clientX: 200, clientY: 0 });
  expect((root as HTMLElement).style.getPropertyValue('--tilt-ry')).not.toBe('');
  fireEvent.pointerLeave(root);
  expect((root as HTMLElement).style.getPropertyValue('--tilt-ry')).toBe('0deg');
});

it('con reduced-motion no inclina', () => {
  stubMatchMedia({ '(hover: hover) and (pointer: fine)': true, '(prefers-reduced-motion: reduce)': true });
  render(<TiltCard><div>content</div></TiltCard>);
  const root = screen.getByText('content').closest('[data-tilt]')!;
  fireEvent.pointerMove(root, { clientX: 100, clientY: 50 });
  expect((root as HTMLElement).style.getPropertyValue('--tilt-ry')).toBe('');
});
```

- [ ] **Step 2 — FAIL** → **Step 3 — implementación:**

```tsx
'use client';

import { useEffect, useRef, useState, type PointerEvent, type ReactNode } from 'react';

type TiltCardProps = { children: ReactNode; maxTilt?: number; className?: string };

/** Tilt 3D sutil + glow que sigue el puntero. Inerte en touch y reduced-motion. */
export function TiltCard({ children, maxTilt = 4, className }: TiltCardProps) {
  const rootRef = useRef<HTMLDivElement>(null);
  const frameRef = useRef<number | null>(null);
  const [enabled, setEnabled] = useState(false);

  useEffect(() => {
    const fine = window.matchMedia?.('(hover: hover) and (pointer: fine)').matches ?? false;
    const reduced = window.matchMedia?.('(prefers-reduced-motion: reduce)').matches ?? false;
    setEnabled(fine && !reduced);
  }, []);

  function onPointerMove(event: PointerEvent<HTMLDivElement>) {
    if (!enabled) return;
    const root = rootRef.current;
    const rect = root?.getBoundingClientRect();
    if (!root || !rect || rect.width === 0) return;
    const px = (event.clientX - rect.left) / rect.width;
    const py = (event.clientY - rect.top) / rect.height;
    if (frameRef.current != null) cancelAnimationFrame(frameRef.current);
    frameRef.current = requestAnimationFrame(() => {
      frameRef.current = null;
      root.style.setProperty('--tilt-rx', `${((0.5 - py) * maxTilt * 2).toFixed(2)}deg`);
      root.style.setProperty('--tilt-ry', `${((px - 0.5) * maxTilt * 2).toFixed(2)}deg`);
      root.style.setProperty('--tilt-gx', `${(px * 100).toFixed(1)}%`);
      root.style.setProperty('--tilt-gy', `${(py * 100).toFixed(1)}%`);
    });
  }

  function onPointerLeave() {
    const root = rootRef.current;
    if (!root) return;
    if (frameRef.current != null) cancelAnimationFrame(frameRef.current);
    root.style.setProperty('--tilt-rx', '0deg');
    root.style.setProperty('--tilt-ry', '0deg');
  }

  return (
    <div
      ref={rootRef}
      data-tilt
      className={className}
      style={{
        transform: 'perspective(800px) rotateX(var(--tilt-rx, 0deg)) rotateY(var(--tilt-ry, 0deg))',
        transition: 'transform 160ms ease-out',
      }}
      onPointerMove={onPointerMove}
      onPointerLeave={onPointerLeave}
    >
      {children}
      {enabled && (
        <div
          aria-hidden
          className="pointer-events-none absolute inset-0 rounded-card opacity-0 transition-opacity duration-200 [--tilt-glow:radial-gradient(240px_circle_at_var(--tilt-gx,50%)_var(--tilt-gy,50%),var(--color-accent),transparent_70%)]"
          style={{ background: 'var(--tilt-glow)', opacity: 0.12 }}
        />
      )}
    </div>
  );
}
```

  (El glow usa `var(--color-accent)` — token, permitido. El wrapper necesita `position: relative` — añadir `relative` al className. Ajustar la implementación al estilo cva/cn del repo; la clase arbitraria del gradient puede ir a CSS si queda ilegible.)
  - `featured-projects.tsx`: `<TiltCard key={…} className="relative">…card…</TiltCard>` alrededor de cada celda (ProjectCard y MoreProjectsCard). Tests de featured: la grid sigue accesible (headings/links intactos).
- [ ] **Step 4 — PASS + gate** → **Step 5 — commit** `feat(web): TiltCard glow/tilt on featured project grid`

### Task 19: `ApiRequestPlayer` + sección showcase

**Files:** Create `apps/web/src/data/api-demo.ts` · `apps/web/src/components/showcase/api-request-player.tsx` + test · Modify `page.tsx` del showcase (sección + TOC) · `messages/{es,en}.json`.

**Interfaces:** Produces: `ApiRequestPlayer({ demo, labels })` (client) con estados `idle → pending → streaming → done`; `demo: ApiDemo` tipado desde datos; sección showcase `api-player`. Reutilizable en F4.

- [ ] **Step 1 — datos** `api-demo.ts`:

```ts
export type ApiDemo = {
  method: 'POST';
  path: string;
  request: object;
  response: object;
  status: string;
};

export const apiDemo: ApiDemo = {
  method: 'POST',
  path: '/v1/ai/text-to-image',
  request: {
    prompt: 'A serene mountain landscape at golden hour',
    aspect_ratio: 'widescreen_16_9',
    seed: 42,
  },
  response: {
    task_id: 'task_01j8…',
    status: 'COMPLETED',
    generated: ['https://cdn.example.com/result.webp'],
  },
  status: '200 OK',
};
```

- [ ] **Step 2 — tests en rojo:**

```tsx
it('idle muestra la request y el botón run', () => {
  renderPlayer();
  expect(screen.getByText(/text-to-image/)).toBeInTheDocument();
  expect(screen.getByRole('button', { name: labels.run })).toBeInTheDocument();
});

it('run → pending → done con la respuesta completa (reduced-motion: sin typing)', async () => {
  stubReducedMotion(true);
  renderPlayer();
  fireEvent.click(screen.getByRole('button', { name: labels.run }));
  expect(await screen.findByText(/COMPLETED/)).toBeInTheDocument();
  expect(screen.getByText('200 OK')).toBeInTheDocument();
});

it('el botón copy copia la respuesta', async () => {
  const writeText = vi.fn().mockResolvedValue(undefined);
  vi.stubGlobal('navigator', { ...navigator, clipboard: { writeText } });
  stubReducedMotion(true);
  renderPlayer();
  fireEvent.click(screen.getByRole('button', { name: labels.run }));
  await screen.findByText(/COMPLETED/);
  fireEvent.click(screen.getByRole('button', { name: labels.copy }));
  expect(writeText).toHaveBeenCalled();
});
```

- [ ] **Step 3 — implementación** (~140 líneas): estados con `useState<'idle' | 'pending' | 'streaming' | 'done'>`; al ejecutar: `pending` 600 ms (`setTimeout`), luego si reduced-motion → `done` directo; si no → `streaming`: rAF que añade ~14 caracteres/frame del `JSON.stringify(response, null, 2)` hasta completar → `done`. Caret CSS (`animate-pulse` en un `<span aria-hidden>▌</span>` durante streaming). Layout: dos `<pre className="rounded-control …">` (request/response) con cabecera método+path y status badge (`Badge` accent al terminar); botón Run (`Button` primary) re-ejecutable; botón copy (`Button` ghost) visible en `done`. Response `aria-live="polite"` SOLO en el contenedor de estado (no en el texto que se escribe — evitar verbosidad: anunciar "Respuesta recibida" vía `role="status"` sr-only al pasar a done). Labels desde messages (`showcase.sections.apiPlayer.*`: title, description, run, running, copy, copied, done — ENMIENDA post-review T19: anidado bajo `sections.` y `description` en vez de `intro`, como las 6 secciones existentes; el TOC sigue en `toc.apiPlayer`).
- [ ] **Step 4:** sección showcase `api-player` + TOC + keys es/en → **Step 5 — PASS + gate** → commit `feat(web): ApiRequestPlayer interactive demo`

### Task 20: `HeroCanvas`

**Files:** Create `apps/web/src/components/home/hero-canvas.tsx` + test · Modify `apps/web/src/components/home/hero.tsx`.

**Interfaces:** Produces: canvas decorativo detrás del hero (spec C4): grid de puntos reactivo al puntero, color desde `--color-accent` computado, re-lee en cambios de `data-theme`/`data-skin` (MutationObserver), rAF pausado con reduced-motion/pestaña oculta/puntero fuera. `Hero` gana `position: relative` y `<HeroCanvas />` como primer hijo.

- [ ] **Step 1 — tests en rojo:**

```tsx
it('renderiza un canvas decorativo aria-hidden', () => {
  render(<HeroCanvas />);
  const canvas = document.querySelector('canvas')!;
  expect(canvas).toHaveAttribute('aria-hidden', 'true');
});

it('con reduced-motion no arranca el bucle', () => {
  stubReducedMotion(true);
  const raf = vi.spyOn(window, 'requestAnimationFrame');
  render(<HeroCanvas />);
  expect(raf).not.toHaveBeenCalled();
});
```

- [ ] **Step 2 — FAIL** → **Step 3 — implementación** (~120 líneas, todo en un `useEffect` de montaje): leer accent con `getComputedStyle(document.documentElement).getPropertyValue('--color-accent')`; puntos en grid de 24 px, radio base 1 px, dentro del radio de 140 px del puntero el radio/opacidad sube con falloff; densidad por `devicePixelRatio` capado a 2; `ResizeObserver` del contenedor para el tamaño; listeners `pointermove`/`pointerleave` en el contenedor padre (el canvas es `pointer-events: none`); `visibilitychange` pausa; estado de reposo = un frame estático y `cancelAnimationFrame`; MutationObserver de `data-theme`/`data-skin` en `<html>` → re-leer color + repintar. Cleanup completo. Canvas: `className="pointer-events-none absolute inset-0 opacity-[0.35]"` `aria-hidden`.
  - `hero.tsx`: contenedor `relative overflow-hidden` + `<HeroCanvas />` antes del contenido (contenido con `relative`).
- [ ] **Step 4 — PASS + gate** → **Step 5 — commit** `feat(web): reactive dot-grid hero canvas`

### Task 21: cierre de fase

- [ ] Gate completo + `pnpm --filter @nicobehm/media-kit build` + builds duales export/node.
- [ ] Verificación en vivo (verification-before-completion): servir export → showcase filtra por índice, tabs sin salto (medir `document.body.scrollHeight` estable al cambiar tab), demos nuevas operativas, colorización real, tooltips con delay en lightbox, fullscreen + Espacio panea (smoke manual con Playwright), home con canvas/tilt/metrics animadas, `prefers-reduced-motion` emulado → sin animaciones.
- [ ] Design review (`agents/design-reviewer.md`) sobre showcase + home; **qa-a11y-perf** (`agents/qa-a11y-perf.md`) — primera fase con presupuesto de assets pesados (canvas + mp4): Lighthouse > 90 se verifica aquí.
- [ ] Code review final de rama (superpowers:requesting-code-review) + fixes.
- [ ] Roadmap fila 3.6 → ✅ + plan filename; STATUS.md; ledger; merge FF a main + push.

## Self-Review

1. **Cobertura de la spec:** A1→T1/T2 · A2→T3 · A3→T4/T5 · A4→T6 · A5→T7 · A6→T8 · A7→T9 · B1→T13 · B2→T12 · B3→T14 · B4→T10/T11/T15/T16 · C1→T17 · C2→T18 · C3→T19 · C4→T20 · C5→T15-T20 (secciones/i18n) · cierre→T21. Sin huecos.
2. **Placeholders:** los snippets de test usan helpers (`stubReducedMotion`, `renderPlayer`, `refOf`, `fakeElement`) que el implementador define en el propio archivo de test siguiendo los patrones existentes del repo (vitest.setup del paquete, providers de la app) — el contrato de cada helper queda definido por su uso. Ninguna tarea depende de tipos no definidos en el plan o en el código existente.
3. **Consistencia de tipos:** `CompareSliderMode` se define en T4 y se usa en T5/T9/T15; `prefersReducedMotion` del paquete en T5/T7 y su gemelo de app (`lib/reduced-motion.ts`) en T17/T18; `ApiDemo` en T19; `sections`/`toc` de T13 los construye `page.tsx` en la misma tarea.
4. **Riesgos señalados:** T7 (FLIP con render ya aplicado — las medidas "First" se toman al final de cada render previo, documentado en el código); T13 (children RSC como props de client component — patrón soportado por Next); T10 puede BLOQUEAR el bloque B si no hay generación de imagen disponible (escalar, no inventar).
