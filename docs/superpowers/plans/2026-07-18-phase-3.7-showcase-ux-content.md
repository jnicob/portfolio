# Fase 3.7 — Showcase v3 + UX global + contenido — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** media-kit `0.5.0 → 0.6.0` (fix compare retrato en fullscreen, side-by-side responsive, FilterGallery combinable + salida animada, `HoverVideo`, scrub v3), showcase con galería de 16 ejemplos REALES generados con IA (filtro por categoría + texto, fullscreen HD con preload en hover) y API player v2 estilo playground sin layout shift; navegación con página activa + hamburguesa mobile; contenido: intro de carrera + disponibilidad, skills con estrellas revisadas, CV timeline rail (Freepik/Magnific jul 2022 — jul 2026), proyectos con case study nuevo de integración por agentes IA y métricas verificadas.

**Architecture:** Bloque A = paquete (aditivo, 0 deps nuevas). Bloque B = assets IA (ORQUESTADOR, vía Magnific MCP + post-proceso ffmpeg/cwebp local). Bloques C-E = showcase app (galería como composición de FilterGallery + MediaLightbox reutilizando la infra `fullSrc` YA existente en `media-source.ts`; API player reescrito en split). Bloques F-G = nav + contenido. La verificación "E2E" es en vivo con Playwright MCP sobre el export (protocolo T21) — **no** se añade framework e2e (no existe en el repo; desviación documentada de spec §8).

**Tech Stack:** React 18+ peer (paquete), TS strict, tsup, Vitest+jsdom+Testing Library; app Next.js 16 App Router (export estático), Tailwind v4 tokens, next-intl, Zod.

**Spec:** `docs/superpowers/specs/2026-07-18-phase-3.7-showcase-ux-content-design.md` (leer ANTES de cada tarea).
**Desviaciones de spec ya decididas (mejoras DRY, anotadas aquí como enmienda):**
1. §1.6 «MediaLightbox hdSrc»: **ya existe** como `MediaSource.fullSrc` + `pickFullscreenSrc` + `preloadFullSources` (`packages/media-kit/src/media-source.ts`, fase 2.6). No se añade API nueva: la galería pasa `MediaSource` con `fullSrc` y precarga con `preloadFullSources` en hover/focus del botón ⛶. El README ya lo documenta.
2. §8 «E2E Playwright»: cobertura vía verificación en vivo Playwright MCP en el cierre (T25), como en T21 de F3.6.

## Global Constraints

- Rama: `feature/phase-3.7-showcase-ux-content` desde `main` (crearla en T1 antes de tocar nada).
- TDD estricto; gate por tarea `pnpm run lint && pnpm run typecheck && pnpm run test` antes de cada commit. Commits convencionales.
- media-kit: cero breaking (props nuevas opcionales, defaults retro-compatibles); tests existentes NO se modifican (describes nuevos al final).
- Colores hardcodeados SOLO en `packages/media-kit/src/styles.css`, `apps/web/src/app/globals.css` y assets de demo.
- `prefers-reduced-motion` en todo lo animado nuevo (HoverVideo: sin autoplay → tap/Enter). WCAG AA en los 8 combos skin×tema.
- Presupuestos de assets (spec §7): imagen display ≤ 200 kB WebP · HD ≤ 800 kB WebP · vídeo 1080p ≤ 4 MB MP4 sin audio (0 bytes pre-interacción) · poster ≤ 100 kB · audio ≤ 1.5 MB · `width/height` explícitos SIEMPRE.
- i18n: toda label nueva en `apps/web/messages/{es,en}.json` (test `messages-parity` vigila). Paquete: defaults en inglés vía props.
- NUNCA `git add apps/web/content/` (dir completo) ni `git add -A` — añadir ficheros por ruta explícita (los MDX de proyectos SÍ se commitean; `apps/web/content/cv/` JAMÁS).
- Cero contenido interno de Freepik en el repo (procedencia F3): el case study E7 se redacta clean-room y generalizado.
- Subagentes: leer skills `component-patterns`, `accessibility`, `code-conventions` antes de componentes; `tailwind-tokens` para app; `performance` para assets/lazy.
- Lighthouse SOLO con el protocolo T21: build export + `npx serve` plano + host ocioso, A/B intercalado contra main (ledger línea "GATE PERF").

## Mapa de ficheros

```
packages/media-kit/src/
├── compare-slider/compare-slider.tsx(+test)   # T1 fix retrato fullscreen · T2 side-by-side responsive
├── filter-gallery/filter-gallery.tsx(+test)   # T3 visibleIds · T4 salida animada
├── hover-video/hover-video.tsx(+test)         # T5 CREATE
├── video-scrub-preview/video-scrub-preview.tsx(+test) # T6 barra+tiempo+loadedmetadata
├── styles.css                                  # T2, T4, T5, T6 (bloques nuevos)
└── index.ts · package.json · CHANGELOG.md · README.md # T7 (0.6.0)
apps/web/public/demo/gallery/*                  # T8 (ORQUESTADOR: 16 ítems + variantes)
apps/web/public/demo/scrub.mp4 · scrub-poster.webp # T8 (re-encode GOP corto)
apps/web/src/data/schemas.ts(+test)             # T9 galleryItemSchema
apps/web/src/data/gallery.ts(+test)             # T9 CREATE (16 ítems)
apps/web/src/components/showcase/gallery-audio-tile.tsx(+test) # T10 CREATE
apps/web/src/components/showcase/gallery-demo.tsx(+test)       # T11 CREATE
apps/web/src/components/showcase/filter-gallery-demo.tsx(+test) # T12 DELETE (sustituida)
apps/web/src/app/[locale]/showcase/page.tsx     # T12 sección gallery · T13/T14 secciones
apps/web/src/components/showcase/api-request-player.tsx(+test) # T13 REWRITE split
apps/web/src/data/api-demo.ts                   # T13 + preview
apps/web/src/components/showcase/video-scrub-demo.tsx(+test)   # T14 asset/copy/ancho
apps/web/src/components/layout/nav-link.tsx(+test)  # T15 CREATE (activo + no-renav)
apps/web/src/components/layout/header.tsx(+test)    # T15, T16
apps/web/src/components/layout/mobile-menu.tsx(+test) # T16 CREATE
apps/web/src/data/skills.ts                     # T17 estrellas
apps/web/src/data/profile.ts                    # T18 intro carrera
apps/web/src/components/home/hero.tsx(+test)    # T18 disponibilidad + CTA
apps/web/src/components/cv/cv-timeline.tsx(+test) # T19 rail v2 + badge fecha
apps/web/src/components/cv/experience-entry.tsx(+test) # T19 hideDates opcional
apps/web/src/data/experience.ts                 # T19 end 2026-07 · T21/T22 naming/highlight
apps/web/src/app/[locale]/projects/[slug]/page.tsx # T20 back link
apps/web/src/lib/mdx-components.tsx(+test)      # T20 CREATE (links externos _blank)
apps/web/src/data/projects.ts                   # T20 URLs · T22 case study · T23 métricas
apps/web/content/{es,en}/projects/ai-service-integration.mdx # T22 CREATE
apps/web/content/{es,en}/projects/ai-model-onboarding.mdx    # T22 DELETE
apps/web/src/components/home/hero-canvas.tsx · projects/more-projects-card.tsx · … # T24 polish pack
apps/web/messages/{es,en}.json                  # T11-T16, T18, T24 (keys nuevas)
docs/superpowers/plans/2026-07-10-portfolio-roadmap.md · STATUS.md # T25
```

---

## Bloque A — media-kit 0.6

### Task 1: BUG F1 — compare de retrato desfasado en fullscreen

**Files:** Modify `packages/media-kit/src/compare-slider/compare-slider.tsx` y/o `packages/media-kit/src/styles.css` (según diagnóstico) · Test `compare-slider.test.tsx` o `media-lightbox.test.tsx` (describe nuevo al final) · Ledger: evidencia del diagnóstico.

**Interfaces:** Consumes: `MediaLightbox.compare` (prioridad compare > media > children), `pickFullscreenSrc` (cada lado `MediaSource` se resuelve a `fullSrc` según pantalla). Produces: las dos capas del compare comparten caja exacta con cualquier aspect ratio dentro del lightbox.

Este es un task de **systematic-debugging**: primero reproducir y medir, después test rojo con la causa real, después fix mínimo.

- [ ] **Step 0 — rama**: `git checkout -b feature/phase-3.7-showcase-ux-content` (desde `main` actualizado).
- [ ] **Step 1 — reproducir y medir**: `pnpm run build && npx serve apps/web/out` → con Playwright MCP abrir `/es/showcase#media-kit`, abrir el ejemplo de retrato en fullscreen y medir en el navegador:

```js
// browser_evaluate sobre el lightbox abierto
const before = document.querySelector('.mk-lightbox .mk-compare__before img');
const after = document.querySelector('.mk-lightbox .mk-compare__after img');
JSON.stringify({
  before: before.getBoundingClientRect(), after: after.getBoundingClientRect(),
  beforeSrc: before.currentSrc, afterSrc: after.currentSrc,
  beforeNatural: [before.naturalWidth, before.naturalHeight],
  afterNatural: [after.naturalWidth, after.naturalHeight],
});
```

- [ ] **Step 2 — confirmar hipótesis** (documentar cuál en el ledger). Las dos más probables:
  - **H1 — variantes con dimensiones distintas**: `portrait.webp` (display) y `portrait-hd.webp` no tienen exactamente el mismo aspect ratio, o solo UN lado del compare tiene `fullSrc` → `pickFullscreenSrc` sirve intrínsecos distintos y la capa `after` (absolute inset-0) se estira diferente.
  - **H2 — CSS de la capa after**: `.mk-compare__after img` no replica el sizing del `before` (p.ej. `width:100%` sin `height`) y con AR vertical dentro del contenedor `contain` del lightbox las cajas divergen.
- [ ] **Step 3 — test en rojo que captura la causa.** Para H2 (jsdom no mide layout, se asegura el contrato de estilos):

```tsx
it('side after replica exactamente el sizing del before (retrato)', () => {
  render(
    <CompareSlider
      before={{ src: '/p.webp', fullSrc: '/p-hd.webp', alt: 'Antes' }}
      after={{ src: '/p2.webp', fullSrc: '/p2-hd.webp', alt: 'Después' }}
      label="Retrato"
    />,
  );
  const [beforeImg, afterImg] = screen.getAllByRole('img');
  // Mismo tratamiento de caja: si uno declara dimensiones/estilo, el otro igual.
  expect(afterImg.getAttribute('style')).toBe(beforeImg.getAttribute('style'));
  expect(afterImg.className).toBe(beforeImg.className);
});
```

Para H1 el test va en `media-source.test.ts` / demo: ambos lados del compare deben resolver la MISMA política de variante (los dos con `fullSrc` o ninguno) — y el fix real es de datos/CSS según lo medido. Ajustar el test a la causa confirmada (el reviewer valida que el test discrimina: en rojo sin fix, verde con fix).

- [ ] **Step 4 — fix mínimo** según causa (CSS en `styles.css` para H2: `.mk-compare__after img { width:100%; height:100%; object-fit:cover; }` espejo del before; datos/demo para H1). Suite del paquete completa en verde.
- [ ] **Step 5 — verificar en vivo** (mismo protocolo del Step 1: rects idénticos ±1px) y anotar evidencia en ledger.
- [ ] **Step 6 — commit** `fix(media-kit): align compare layers in fullscreen for portrait aspect ratios`

### Task 2: side-by-side responsive (apilado) + altura normalizada

**Files:** Modify `packages/media-kit/src/compare-slider/compare-slider.tsx` · `packages/media-kit/src/styles.css` (bloque side-by-side, líneas ~248-262) · Test `compare-slider.test.tsx` (describe nuevo).

**Interfaces:** Produces: `data-stacked="true"` en `.mk-compare[data-compare-mode='side-by-side']` cuando el ancho del root < 480px (ResizeObserver; SSR-safe: sin observer → sin atributo). CSS pasa el grid a 2 filas y normaliza alturas.

- [ ] **Step 1 — test en rojo** (describe `side-by-side responsive (v0.6)`):

```tsx
function mockResizeObserver(width: number) {
  const callbacks: ResizeObserverCallback[] = [];
  vi.stubGlobal('ResizeObserver', class {
    constructor(cb: ResizeObserverCallback) { callbacks.push(cb); }
    observe = (el: Element) => callbacks.forEach((cb) =>
      cb([{ target: el, contentRect: { width } } as unknown as ResizeObserverEntry], this as never));
    unobserve = () => {};
    disconnect = () => {};
  });
}

it('marca data-stacked cuando el contenedor es estrecho', () => {
  mockResizeObserver(360);
  const { container } = render(
    <CompareSlider compareMode="side-by-side" before={beforeSrc} after={afterSrc} label="SxS" />,
  );
  expect(container.querySelector('.mk-compare')).toHaveAttribute('data-stacked', 'true');
});

it('no marca data-stacked con ancho holgado', () => {
  mockResizeObserver(900);
  const { container } = render(
    <CompareSlider compareMode="side-by-side" before={beforeSrc} after={afterSrc} label="SxS" />,
  );
  expect(container.querySelector('.mk-compare')).not.toHaveAttribute('data-stacked');
});
```

- [ ] **Step 2 — FAIL** (`pnpm --filter @nicobehm/media-kit test -- compare-slider`).
- [ ] **Step 3 — implementación**: en `compare-slider.tsx`, solo cuando `compareMode === 'side-by-side'` (usar el ref del elemento root `.mk-compare` ya existente en el componente; si el root no tiene ref hoy, añadirla):

```tsx
const STACK_BREAKPOINT = 480;
const [stacked, setStacked] = useState(false);
useEffect(() => {
  if (compareMode !== 'side-by-side') return;
  const root = rootRef.current;
  if (!root || typeof ResizeObserver === 'undefined') return;
  const observer = new ResizeObserver((entries) => {
    setStacked(entries[0].contentRect.width < STACK_BREAKPOINT);
  });
  observer.observe(root);
  return () => observer.disconnect();
}, [compareMode]);
```

`data-stacked={stacked ? 'true' : undefined}` en el root. CSS:

```css
.mk-compare[data-compare-mode='side-by-side'][data-stacked='true'] {
  grid-template-columns: 1fr;
  grid-template-rows: 1fr 1fr;
}
/* Altura normalizada (Minor T21): ambas mitades llenan su track por igual. */
.mk-compare[data-compare-mode='side-by-side'] img {
  width: 100%;
  height: 100%;
  object-fit: cover;
}
```

- [ ] **Step 4 — PASS** + suite completa (los tests v0.5 de side-by-side intactos).
- [ ] **Step 5 — commit** `feat(media-kit): stack side-by-side compare vertically in narrow containers`

### Task 3: FilterGallery — filtrado combinable (`visibleIds`)

**Files:** Modify `packages/media-kit/src/filter-gallery/filter-gallery.tsx` · Test `filter-gallery.test.tsx` (describe nuevo).

**Interfaces:** Produces: prop opcional `visibleIds?: readonly string[]` — si se pasa, un item solo es visible si ADEMÁS de pasar el filtro de categoría su `id` está en la lista. FLIP anima igual (la visibilidad cambia por render, misma mecánica que `filter`).

- [ ] **Step 1 — test en rojo** (describe `visibleIds (v0.6)`):

```tsx
const items = [
  { id: 'a', categories: ['image'], node: <span>A</span> },
  { id: 'b', categories: ['image'], node: <span>B</span> },
  { id: 'c', categories: ['video'], node: <span>C</span> },
];

it('interseca visibleIds con el filtro de categoría', () => {
  render(<FilterGallery items={items} filter="image" visibleIds={['b', 'c']} label="G" />);
  expect(screen.queryByText('A')).not.toBeInTheDocument();
  expect(screen.getByText('B')).toBeInTheDocument();
  expect(screen.queryByText('C')).not.toBeInTheDocument(); // categoría lo excluye
});

it('sin visibleIds mantiene el comportamiento actual', () => {
  render(<FilterGallery items={items} filter="image" label="G" />);
  expect(screen.getByText('A')).toBeInTheDocument();
  expect(screen.getByText('B')).toBeInTheDocument();
});
```

- [ ] **Step 2 — FAIL.**
- [ ] **Step 3 — implementación** (en `filter-gallery.tsx`, tras `activeFilter`):

```tsx
const visible = items.filter(
  (item) =>
    (activeFilter == null || item.categories.includes(activeFilter)) &&
    (visibleIds === undefined || visibleIds.includes(item.id)),
);
```

(prop nueva en `FilterGalleryProps` con TSDoc: «Restricción adicional de visibilidad; se interseca con el filtro de categoría. `undefined` = sin restricción.»). El `useLayoutEffect` FLIP no cambia — pero el efecto solo anima cuando `previousFilterRef` cambia: ampliarlo para que también anime cuando cambie `visibleIds` (guardar `previousVisibleIdsRef` y comparar por identidad de contenido `join(' ')`).

- [ ] **Step 4 — PASS** + test extra: cambiar `visibleIds` entre renders dispara `element.animate` (mismo spy que los tests FLIP existentes).
- [ ] **Step 5 — commit** `feat(media-kit): FilterGallery visibleIds prop for combinable filtering`

### Task 4: FilterGallery — animación de salida real

**Files:** Modify `packages/media-kit/src/filter-gallery/filter-gallery.tsx` · `styles.css` (nada nuevo necesario si la salida va por WAAPI) · Test `filter-gallery.test.tsx` (describe nuevo).

**Interfaces:** Produces: los ítems que dejan de ser visibles permanecen montados durante `duration` ms con fade-out (`opacity → 0, scale 0.96`) y se desmontan al terminar. `prefers-reduced-motion` o sin WAAPI: desmontado inmediato (comportamiento actual).

- [ ] **Step 1 — test en rojo** (describe `exit animation (v0.6)`; el setup del paquete ya stubbea `element.animate` en los tests FLIP — reutilizar el mismo patrón, con `onfinish` capturable):

```tsx
it('mantiene el item saliente montado hasta que termina el fade-out', () => {
  const finishers: (() => void)[] = [];
  stubAnimate((anim) => finishers.push(() => anim.onfinish?.(new Event('finish') as never)));
  const { rerender } = render(<FilterGallery items={items} filter={null} label="G" />);
  rerender(<FilterGallery items={items} filter="video" label="G" />);
  expect(screen.getByText('A')).toBeInTheDocument(); // saliendo, aún montado
  act(() => finishers.forEach((finish) => finish()));
  expect(screen.queryByText('A')).not.toBeInTheDocument();
});

it('con reduced-motion desmonta inmediatamente', () => {
  mockReducedMotion(true);
  const { rerender } = render(<FilterGallery items={items} filter={null} label="G" />);
  rerender(<FilterGallery items={items} filter="video" label="G" />);
  expect(screen.queryByText('A')).not.toBeInTheDocument();
});
```

- [ ] **Step 2 — FAIL.**
- [ ] **Step 3 — implementación** (render diferido, la extensión anticipada por la spec A5 de F3.6):

```tsx
const [exitingIds, setExitingIds] = useState<readonly string[]>([]);
const previousVisibleIdsRef = useRef<readonly string[]>([]);

// En el useLayoutEffect existente, tras calcular filterChanged:
const currentIds = visible.map((item) => item.id);
const removed = previousVisibleIdsRef.current.filter((id) => !currentIds.includes(id));
previousVisibleIdsRef.current = currentIds;
if (removed.length > 0 && !prefersReducedMotion()) {
  setExitingIds((prev) => [...prev, ...removed]);
  for (const id of removed) {
    const el = grid.querySelector<HTMLElement>(`[data-fg-id="${CSS.escape(id)}"]`);
    if (!el || typeof el.animate !== 'function') { drop(id); continue; }
    const anim = el.animate(
      [{ opacity: 1, transform: 'scale(1)' }, { opacity: 0, transform: 'scale(0.96)' }],
      { duration, easing: 'ease-out', fill: 'forwards' },
    );
    anim.onfinish = () => drop(id);
  }
}
function drop(id: string) {
  setExitingIds((prev) => prev.filter((x) => x !== id));
}
```

Y el render lista `visible` ∪ los items cuyo id esté en `exitingIds` (los salientes con `data-fg-exiting` y `aria-hidden` + `inert` para sacarlos del árbol accesible mientras se desvanecen). Si un id saliente vuelve a ser visible antes de terminar, se elimina de `exitingIds` (gana la entrada).

- [ ] **Step 4 — PASS** + suite completa (los tests v0.5 de FLIP/entrada intactos; verificar el test existente «items ocultos fuera del DOM accesible» sigue verde gracias a `aria-hidden`).
- [ ] **Step 5 — commit** `feat(media-kit): animated exit for FilterGallery items (deferred unmount)`

### Task 5: `HoverVideo` (componente nuevo)

**Files:** Create `packages/media-kit/src/hover-video/hover-video.tsx`, `packages/media-kit/src/hover-video/index.ts` · Test `hover-video.test.tsx` · Modify `styles.css` (bloque `.mk-hover-video`).

**Interfaces:** Produces:

```ts
export type HoverVideoProps = {
  src: string;            // MP4, solo se descarga tras activación (facade)
  poster: string;         // imagen visible en reposo
  label: string;          // nombre accesible del área
  delay?: number;         // ms de hover antes de activar, default 300
  width: number;          // dimensiones intrínsecas del poster (cero CLS)
  height: number;
  className?: string;
};
export function HoverVideo(props: HoverVideoProps): JSX.Element;
```

Comportamiento: en reposo renderiza SOLO `<img src={poster}>` (0 bytes de vídeo). `pointerenter` arma un timer de `delay` ms → al vencer monta `<video autoPlay muted loop playsInline src>`; `pointerleave` cancela el timer y desmonta el vídeo (vuelve al poster). Foco de teclado: `Enter`/`Espacio` alternan reproducción (toggle). `prefers-reduced-motion` o `(pointer: coarse)`: el hover NO activa; solo el toggle explícito (tap/Enter). Root `tabIndex={0}`, `role="button"`, `aria-pressed` = reproduciendo, `data-state="idle" | "playing"`.

- [ ] **Step 1 — tests en rojo** (fichero nuevo; `vi.useFakeTimers()`; el helper `mockReducedMotion` del setup existente):

```tsx
it('en reposo no monta el video (facade)', () => {
  render(<HoverVideo src="/v.mp4" poster="/p.webp" label="Demo" width={640} height={360} />);
  expect(document.querySelector('video')).not.toBeInTheDocument();
  expect(screen.getByRole('button', { name: 'Demo' })).toHaveAttribute('data-state', 'idle');
});

it('hover sostenido delay ms activa el video', () => {
  render(<HoverVideo src="/v.mp4" poster="/p.webp" label="Demo" width={640} height={360} />);
  fireEvent.pointerEnter(screen.getByRole('button', { name: 'Demo' }));
  act(() => vi.advanceTimersByTime(299));
  expect(document.querySelector('video')).not.toBeInTheDocument();
  act(() => vi.advanceTimersByTime(1));
  expect(document.querySelector('video')).toBeInTheDocument();
});

it('salir antes del delay cancela la activación', () => {
  render(<HoverVideo src="/v.mp4" poster="/p.webp" label="Demo" width={640} height={360} />);
  const root = screen.getByRole('button', { name: 'Demo' });
  fireEvent.pointerEnter(root);
  act(() => vi.advanceTimersByTime(200));
  fireEvent.pointerLeave(root);
  act(() => vi.advanceTimersByTime(500));
  expect(document.querySelector('video')).not.toBeInTheDocument();
});

it('con reduced-motion el hover no activa pero Enter sí (toggle)', () => {
  mockReducedMotion(true);
  render(<HoverVideo src="/v.mp4" poster="/p.webp" label="Demo" width={640} height={360} />);
  const root = screen.getByRole('button', { name: 'Demo' });
  fireEvent.pointerEnter(root);
  act(() => vi.advanceTimersByTime(1000));
  expect(document.querySelector('video')).not.toBeInTheDocument();
  fireEvent.keyDown(root, { key: 'Enter' });
  expect(document.querySelector('video')).toBeInTheDocument();
  expect(root).toHaveAttribute('aria-pressed', 'true');
});
```

- [ ] **Step 2 — FAIL** (módulo no existe).
- [ ] **Step 3 — implementación** (~90 líneas): estado `playing: boolean`, ref del timer; `pointerenter` solo arma timer si `!prefersReducedMotion() && matchMedia('(pointer: fine)').matches`; `pointerleave` limpia timer y `setPlaying(false)`; keydown Enter/Espacio y click alternan; cleanup del timer al desmontar. El `<img>` del poster con `width`/`height` y `loading="lazy"`; el `<video>` con `aria-hidden` (el nombre accesible vive en el root). CSS `.mk-hover-video { position: relative; }` con el vídeo `position:absolute; inset:0; width:100%; height:100%; object-fit:cover;`.
- [ ] **Step 4 — PASS** + export en `index.ts` del paquete (`export { HoverVideo } from './hover-video'; export type { HoverVideoProps } …`).
- [ ] **Step 5 — commit** `feat(media-kit): HoverVideo — poster facade with delayed hover autoplay`

### Task 6: VideoScrubPreview v3 — barra gruesa con tiempo + `loadedmetadata`

**Files:** Modify `packages/media-kit/src/video-scrub-preview/video-scrub-preview.tsx` · `styles.css` (`.mk-scrub__bar` más gruesa, `.mk-scrub__time` nuevo) · Test `video-scrub-preview.test.tsx` (describe nuevo).

**Interfaces:** Produces: (a) el scrub funciona aunque `duration` llegue tarde (estado interno `duration` seteado en `onLoadedMetadata`; hoy `scrubTo` es no-op silencioso si el metadata no está); (b) indicador visible `mm:ss / mm:ss` (`.mk-scrub__time`, `aria-hidden` — el estado accesible sigue siendo el `aria-label`); (c) barra de progreso más gruesa (6px) con track visible. API pública sin cambios.

- [ ] **Step 1 — tests en rojo** (describe `scrub v3 (v0.6)`; jsdom no implementa `duration`: definirla con `Object.defineProperty`):

```tsx
function setDuration(video: HTMLVideoElement, value: number) {
  Object.defineProperty(video, 'duration', { configurable: true, value });
}

it('el scrub se activa cuando loadedmetadata llega después del hover', async () => {
  const { container } = render(<VideoScrubPreview src="/v.mp4" label="Scrub" />);
  const root = container.querySelector('.mk-scrub') as HTMLElement;
  const video = container.querySelector('video') as HTMLVideoElement;
  vi.spyOn(root, 'getBoundingClientRect').mockReturnValue({ left: 0, width: 100 } as DOMRect);
  // Sin metadata: no-op (comportamiento actual, sin errores)
  fireEvent.pointerMove(root, { clientX: 50 });
  // Llega el metadata → el siguiente move ya scrubbea
  setDuration(video, 10);
  fireEvent.loadedMetadata(video);
  fireEvent.pointerMove(root, { clientX: 50 });
  await waitFor(() => expect(video.currentTime).toBeCloseTo(5));
});

it('muestra el tiempo actual y total', () => {
  const { container } = render(<VideoScrubPreview src="/v.mp4" label="Scrub" />);
  const video = container.querySelector('video') as HTMLVideoElement;
  setDuration(video, 90);
  fireEvent.loadedMetadata(video);
  expect(container.querySelector('.mk-scrub__time')).toHaveTextContent('0:00 / 1:30');
});
```

(el primer test necesita el rAF inmediato del setup existente de los tests de scrub — reutilizar su stub).

- [ ] **Step 2 — FAIL.**
- [ ] **Step 3 — implementación**: estado `const [duration, setDurationState] = useState<number | null>(null)`; `<video onLoadedMetadata={(e) => setDurationState(e.currentTarget.duration)} …>`; `scrubTo` usa `duration ?? video.duration` (misma guarda de finitud); helper puro `formatTime(seconds)` → `m:ss`; render:

```tsx
<div className="mk-scrub__time" aria-hidden>
  {duration != null ? `${formatTime((progress / 100) * duration)} / ${formatTime(duration)}` : ''}
</div>
```

CSS: `.mk-scrub__bar { height: 6px; }` sobre un track `.mk-scrub__track` de fondo `--mk-scrub-track` (variable pública nueva, default translúcido); `.mk-scrub__time` chip mono pequeño esquina inferior-derecha.

- [ ] **Step 4 — PASS** + suite completa (tests v0.5 del scrub intactos).
- [ ] **Step 5 — commit** `feat(media-kit): scrub v3 — thicker progress bar, time indicator, late-metadata support`

### Task 7: media-kit 0.6.0 — versión, CHANGELOG, README

**Files:** Modify `packages/media-kit/package.json` (0.5.0 → 0.6.0) · `CHANGELOG.md` · `README.md` · `src/index.ts` (verificar exports T5).

- [ ] **Step 1 — CHANGELOG** (Keep a Changelog):

```markdown
## [0.6.0] — 2026-07-XX
### Added
- `HoverVideo`: poster facade with delayed hover-autoplay (keyboard toggle, reduced-motion aware).
- `FilterGallery`: `visibleIds` prop to combine category filtering with external predicates (e.g. text search).
- `FilterGallery`: animated exit — leaving items fade out before unmounting.
- `VideoScrubPreview`: thick progress bar with time indicator; scrubbing now works when metadata loads late.
- New public CSS variable: `--mk-scrub-track`.
### Fixed
- `CompareSlider`: before/after layers no longer misalign in fullscreen with portrait aspect ratios.
- `CompareSlider` `side-by-side`: stacks vertically in narrow containers; halves keep equal height.
### Compatibility
- All changes additive; no breaking changes.
```

- [ ] **Step 2 — README**: tabla de props de `HoverVideo`; fila `visibleIds` en FilterGallery; nota del time indicator del scrub; `--mk-scrub-track` en la tabla de variables. Verificar que la sección de `MediaSource.fullSrc`/preload ya cubre el flujo HD de la galería (spec §1.6 — no hay API nueva).
- [ ] **Step 3 — gate completo** `pnpm run lint && pnpm run typecheck && pnpm run test` (raíz).
- [ ] **Step 4 — commit** `chore(media-kit): release 0.6.0 — HoverVideo, combinable filtering, scrub v3, compare fixes`

## Bloque B — assets IA (ORQUESTADOR — no subagente)

### Task 8: generar y post-procesar los 16 ítems + scrub re-encode

**Files:** Create `apps/web/public/demo/gallery/` (assets según convención abajo) · Replace `apps/web/public/demo/scrub.mp4`, `scrub-poster.webp`.

El orquestador (sesión principal, con acceso a Magnific MCP) ejecuta esta tarea. Prompts = tabla spec §7 (los 16, con su modelo y AR). Convención de nombres por `id` de ítem (ids en spec §7, kebab del título: `nbp-retrato-neon`, `seedream-ciudad-isometrica`, `veo-costa-atardecer`, `kling-retrato-cine`, `audio-lofi`…):

| Tipo | Ficheros |
| --- | --- |
| imagen | `<id>.webp` (display ≤200 kB, ~1200px) + `<id>-hd.webp` (≤800 kB, ~2560px) |
| vídeo | `<id>.mp4` (1080p ≤4 MB, sin audio) + `<id>-poster.webp` (≤100 kB) |
| audio | `<id>.mp3` (≤1.5 MB, ~30 s) + `<id>-cover.webp` + `<id>-cover-hd.webp` |

- [ ] **Step 1 — generar**: imágenes con `images_generate` (modelos Google NBP y Seedream 5 Pro — confirmar ids exactos con `images_models_list`), vídeos con `video_generate` (Veo / Kling V3 vía `video_models_list`), audio con `audio_music_generate`; carátulas con NBP/Seedream. Esperar con `creations_wait` y descargar los originales a un dir temporal (scratchpad, NO al repo).
- [ ] **Step 2 — post-proceso** (por asset; verificar tamaño tras cada uno):

```bash
# Imagen display + HD (desde el original descargado)
cwebp -q 82 -resize 1200 0 orig.png -o apps/web/public/demo/gallery/<id>.webp
cwebp -q 82 -resize 2560 0 orig.png -o apps/web/public/demo/gallery/<id>-hd.webp
# Vídeo 1080p sin audio + poster del primer frame nítido
ffmpeg -i orig.mp4 -an -vf "scale=-2:1080" -c:v libx264 -crf 26 -preset slow \
  -movflags +faststart apps/web/public/demo/gallery/<id>.mp4
ffmpeg -i apps/web/public/demo/gallery/<id>.mp4 -frames:v 1 -q:v 2 poster.png \
  && cwebp -q 80 -resize 1200 0 poster.png -o apps/web/public/demo/gallery/<id>-poster.webp
# Audio
ffmpeg -i orig.wav -codec:a libmp3lame -q:a 4 apps/web/public/demo/gallery/<id>.mp3
# Scrub: keyframes densos (GOP 4) — la clave de la fluidez del scrub
ffmpeg -i scrub-orig.mp4 -an -vf "scale=-2:720" -c:v libx264 -crf 24 \
  -x264-params keyint=4:min-keyint=4:scenecut=0 -movflags +faststart \
  apps/web/public/demo/scrub.mp4
```

- [ ] **Step 3 — verificar presupuestos**: `ls -la apps/web/public/demo/gallery/` — CADA fichero dentro de su presupuesto (si excede: bajar `-q`/`-crf` o recortar duración). Registrar `width×height` reales de cada display/poster (`ffprobe`/`webpinfo` o `identify`): los necesita T9.
- [ ] **Step 4 — curación**: revisar los 16 visualmente; regenerar los flojos (criterio: nitidez, sin artefactos obvios, composición del prompt).
- [ ] **Step 5 — commit** (rutas explícitas, nunca `-A`): `feat(web): AI-generated gallery assets (16 items, display+HD variants) and keyframe-dense scrub clip`

## Bloque C — galería «Ejemplos reales»

### Task 9: schema Zod + datos de la galería

**Files:** Modify `apps/web/src/data/schemas.ts` (+ `schemas.test.ts`) · Create `apps/web/src/data/gallery.ts` · Test `apps/web/src/data/gallery.test.ts`.

**Interfaces:** Produces:

```ts
// schemas.ts
export const galleryItemSchema = z.discriminatedUnion('type', [imageItem, videoItem, audioItem]);
export type GalleryItem = z.infer<typeof galleryItemSchema>;
// gallery.ts
export const galleryItems: GalleryItem[]; // 16 ítems validados
```

Forma (campos comunes: `id`, `model` (string libre: `'Google NBP'`…), `title: localizedStringSchema`, `width`, `height` positivos):
- `image`: `src`, `hdSrc`.
- `video`: `src`, `poster` (width/height = del poster).
- `audio`: `src`, `cover`, `coverHd` (width/height = de la carátula).

- [ ] **Step 1 — tests en rojo** (`schemas.test.ts` describe nuevo + `gallery.test.ts`):

```ts
it('galleryItemSchema image exige src y hdSrc', () => {
  expect(() =>
    galleryItemSchema.parse({ type: 'image', id: 'x', model: 'Google NBP',
      title: { es: 'X', en: 'X' }, src: '/demo/gallery/x.webp', width: 1200, height: 800 }),
  ).toThrow(); // falta hdSrc
});

// gallery.test.ts
it('hay 16 ítems: 8 imagen / 5 vídeo / 3 audio, ids únicos', () => {
  expect(galleryItems).toHaveLength(16);
  const count = (type: GalleryItem['type']) => galleryItems.filter((i) => i.type === type).length;
  expect(count('image')).toBe(8);
  expect(count('video')).toBe(5);
  expect(count('audio')).toBe(3);
  expect(new Set(galleryItems.map((i) => i.id)).size).toBe(16);
});

it('todos los ficheros referenciados existen en public/', () => {
  const files = galleryItems.flatMap((i) =>
    i.type === 'image' ? [i.src, i.hdSrc] : i.type === 'video' ? [i.src, i.poster] : [i.src, i.cover, i.coverHd]);
  for (const f of files) expect(existsSync(join(process.cwd(), 'public', f)), f).toBe(true);
});
```

- [ ] **Step 2 — FAIL.**
- [ ] **Step 3 — implementación**: schema con `z.discriminatedUnion` (patrón de los schemas existentes del fichero); `gallery.ts` con los 16 ítems (títulos es/en de la tabla spec §7; `model` en el título NO — el título compuesto «Modelo — título» lo forma la UI; dimensiones reales medidas en T8). `export const galleryItems = entries.map((e) => galleryItemSchema.parse(e));` (patrón `projects.ts`).
- [ ] **Step 4 — PASS** (los 3 tests) + gate.
- [ ] **Step 5 — commit** `feat(web): typed gallery data — 16 AI-generated items validated with Zod`

### Task 10: tile de audio con player inline

**Files:** Create `apps/web/src/components/showcase/gallery-audio-tile.tsx` · Test `gallery-audio-tile.test.tsx`.

**Interfaces:** Consumes: item `audio` de T9. Produces:

```ts
export type GalleryAudioTileLabels = { play: string; pause: string };
export function GalleryAudioTile(props: {
  cover: string; src: string; title: string; width: number; height: number;
  labels: GalleryAudioTileLabels;
}): JSX.Element;
```

Carátula `<img loading="lazy">` + botón play/pause superpuesto (círculo, icono SVG inline, `aria-label` alternante) + barra de progreso fina inferior (div con `width%`, `aria-hidden`; el `<audio>` es la fuente de verdad vía `timeupdate`). Facade: el `<audio src>` se monta en la PRIMERA pulsación de play (0 bytes antes). `Escape` no aplica; al terminar (`ended`) vuelve a estado play.

- [ ] **Step 1 — tests en rojo**:

```tsx
it('no monta el audio hasta el primer play (facade)', () => {
  render(<GalleryAudioTile cover="/c.webp" src="/a.mp3" title="Lo-fi" width={1200} height={1200}
    labels={{ play: 'Reproducir Lo-fi', pause: 'Pausar Lo-fi' }} />);
  expect(document.querySelector('audio')).not.toBeInTheDocument();
  fireEvent.click(screen.getByRole('button', { name: 'Reproducir Lo-fi' }));
  expect(document.querySelector('audio')).toBeInTheDocument();
});

it('alterna aria-label y play/pause', () => {
  const playSpy = vi.spyOn(HTMLMediaElement.prototype, 'play').mockResolvedValue();
  const pauseSpy = vi.spyOn(HTMLMediaElement.prototype, 'pause').mockImplementation(() => {});
  render(<GalleryAudioTile …labels={{ play: 'Reproducir Lo-fi', pause: 'Pausar Lo-fi' }} />);
  fireEvent.click(screen.getByRole('button', { name: 'Reproducir Lo-fi' }));
  expect(playSpy).toHaveBeenCalled();
  fireEvent.click(screen.getByRole('button', { name: 'Pausar Lo-fi' }));
  expect(pauseSpy).toHaveBeenCalled();
});
```

- [ ] **Step 2 — FAIL.** · **Step 3 — implementación** (~80 líneas, tokens semánticos, botón `size-12 rounded-full bg-surface/80 backdrop-blur` centrado). · **Step 4 — PASS + gate.** · **Step 5 — commit** `feat(web): gallery audio tile — cover with inline facade player`

### Task 11: `GalleryDemo` — composición completa

**Files:** Create `apps/web/src/components/showcase/gallery-demo.tsx` · Test `gallery-demo.test.tsx`.

**Interfaces:** Consumes: `FilterGallery` (con `visibleIds` T3), `HoverVideo` (T5), `MediaLightbox` + `preloadFullSources` (paquete), `GalleryAudioTile` (T10), `galleryItems` (T9). Produces:

```ts
export type GalleryDemoLabels = {
  filterLabel: string; allLabel: string;
  categories: { image: string; video: string; audio: string };
  searchLabel: string; searchPlaceholder: string; emptyState: string;
  fullscreen: string;              // aria-label del botón ⛶ (se interpola el título)
  audio: GalleryAudioTileLabels;
  lightbox: MediaLightboxLabels;   // passthrough
};
export function GalleryDemo(props: { locale: 'es' | 'en'; labels: GalleryDemoLabels }): JSX.Element;
```

Comportamiento:
- Título visible del ítem = `` `${item.model} — ${item.title[locale]}` `` (así la búsqueda encuentra por modelo, decisión brainstorm).
- Búsqueda: `<input type="search">` con label accesible; matching sobre el título compuesto, case-insensitive y sin diacríticos (`normalize('NFD').replace(/\p{Diacritic}/gu, '')`); `visibleIds` = ids que matchean (`undefined` si query vacía). Sin debounce artificial: el filtrado es local sobre 16 ítems (el FLIP ya suaviza).
- Categorías: botonera de `FilterGallery` con `categories={[image, video, audio]}` (labels i18n).
- Tile por tipo: image → `<img loading="lazy" width height>`; video → `<HoverVideo>`; audio → `<GalleryAudioTile>`. Cada tile en un wrapper con el título (caption) y el botón ⛶ **solo icono** (SVG inline). Las labels con `{title}` (`fullscreen`, `audio.play`, `audio.pause`) llegan como plantilla y se interpolan en el componente con un helper local `fill(template, title) => template.replace('{title}', title)` — la interpolación es por ítem, no puede hacerla `t()` en la page.
- ⛶: `onPointerEnter`/`onFocus` → `preloadFullSources([toMediaSource(item)])`; `onClick` → abre `MediaLightbox`: image → `media={{ src, fullSrc: hdSrc, alt: título }}`; audio → `media={{ src: cover, fullSrc: coverHd, alt: título }}` + player debajo (children con el mismo `GalleryAudioTile`); video → children `<video controls autoPlay muted playsInline src poster>`.
- 0 resultados → `EmptyState` del repo (patrón existente) con `labels.emptyState`.

- [ ] **Step 1 — tests en rojo** (mock de `@nicobehm/media-kit` NO: usar el real; mock solo de `preloadFullSources` vía `vi.mock` parcial):

```tsx
it('la búsqueda por modelo restringe el grid', () => {
  render(<GalleryDemo locale="es" labels={labels} />);
  fireEvent.change(screen.getByRole('searchbox', { name: labels.searchLabel }), {
    target: { value: 'seedream' },
  });
  const grid = screen.getByRole('list', { name: labels.filterLabel });
  expect(within(grid).getAllByRole('listitem')).toHaveLength(4); // 4 imágenes Seedream
});

it('búsqueda sin resultados muestra el empty state', () => {
  render(<GalleryDemo locale="es" labels={labels} />);
  fireEvent.change(screen.getByRole('searchbox', { name: labels.searchLabel }), {
    target: { value: 'zzzz' },
  });
  expect(screen.getByText(labels.emptyState)).toBeInTheDocument();
});

it('hover del botón fullscreen precarga la variante HD', () => {
  render(<GalleryDemo locale="es" labels={labels} />);
  const button = screen.getAllByRole('button', { name: /Pantalla completa/ })[0];
  fireEvent.pointerEnter(button);
  expect(preloadFullSources).toHaveBeenCalledWith([
    expect.objectContaining({ fullSrc: expect.stringContaining('-hd.webp') }),
  ]);
});

it('el botón fullscreen abre el lightbox con el ítem', () => {
  render(<GalleryDemo locale="es" labels={labels} />);
  fireEvent.click(screen.getAllByRole('button', { name: /Pantalla completa/ })[0]);
  expect(screen.getByRole('dialog')).toBeInTheDocument();
});
```

- [ ] **Step 2 — FAIL.** · **Step 3 — implementación** (~170 líneas; client component; estado `query` + `lightboxItem: GalleryItem | null`). · **Step 4 — PASS + gate.** · **Step 5 — commit** `feat(web): GalleryDemo — real AI examples with combined filtering and HD fullscreen`

### Task 12: integrar la galería en el showcase (sustituye la demo SVG)

**Files:** Modify `apps/web/src/app/[locale]/showcase/page.tsx` · Delete `apps/web/src/components/showcase/filter-gallery-demo.tsx` (+ su test) · Modify `apps/web/messages/es.json`, `en.json` · Test: `showcase-view.test.tsx` sigue verde; parity test.

**Interfaces:** Consumes: `GalleryDemo` (T11). Produces: sección top-level `gallery` con entrada de TOC propia; la sub-demo `filterGallery` de la sección media-kit desaparece (la galería ES la demo de FilterGallery).

- [ ] **Step 1 — claves i18n** (es; en equivalente — TODAS con paridad):

```json
"showcase": {
  "toc": { "gallery": "Galería de ejemplos IA" },
  "sections": {
    "gallery": {
      "title": "Galería de ejemplos generados con IA",
      "description": "Imágenes (Google NBP, Seedream 5 Pro), vídeo (Google Veo, Kling V3) y música generados con IA. Filtra por tipo, busca por modelo o título y abre cualquier ejemplo a pantalla completa en calidad HD.",
      "filterLabel": "Filtrar ejemplos",
      "all": "Todos", "image": "Imagen", "video": "Vídeo", "audio": "Audio",
      "searchLabel": "Buscar por nombre", "searchPlaceholder": "p. ej. Seedream, retrato…",
      "empty": "Sin resultados para esa búsqueda.",
      "fullscreen": "Pantalla completa: {title}",
      "audioPlay": "Reproducir {title}", "audioPause": "Pausar {title}"
    }
  }
}
```

- [ ] **Step 2 — page.tsx**: `const GalleryDemo = dynamic(() => import('…/gallery-demo').then((m) => m.GalleryDemo))` (patrón T31); entrada `toc` `{ id: 'gallery', label: t('toc.gallery') }`; sección `{ id: 'gallery', node: <Section …><GalleryDemo …/></Section> }`. Eliminar el import dinámico de `FilterGalleryDemo`, su sub-bloque en la sección media-kit y sus claves i18n antiguas (`sections.mediaKit.filterGallery.*` — borrar en ambos idiomas).
- [ ] **Step 3 — borrar** `filter-gallery-demo.tsx` + test. `pnpm run test` (parity + showcase en verde), `pnpm run build` (export OK, la sección aparece en el HTML prerenderizado).
- [ ] **Step 4 — commit** `feat(web): showcase gallery section with real AI examples (replaces SVG tile demo)`

## Bloque D — API player v2

### Task 13: split de playground sin layout shift

**Files:** Rewrite `apps/web/src/components/showcase/api-request-player.tsx` (+ test: describes nuevos; los tests del flujo streaming existentes se ADAPTAN solo en selectores, el contrato de estados idle→pending→streaming→done se conserva) · Modify `apps/web/src/data/api-demo.ts` · `apps/web/src/app/[locale]/showcase/page.tsx` (labels nuevas) · `messages/{es,en}.json`.

**Interfaces:** Consumes: `Tabs` de la app (paneles apilados sin salto, F3.6 B2), `MediaLightbox` + `MediaSource`, asset de galería (T8). Produces:

```ts
// api-demo.ts — campo nuevo
export type ApiDemo = {
  method: 'POST'; path: string; request: object; response: object; status: string;
  preview: { src: string; fullSrc: string; width: number; height: number }; // asset galería
};
// Labels nuevas añadidas a ApiRequestPlayerLabels:
//   previewTab, responseTab, previewIdle, previewAlt, fullscreen (+ las existentes)
```

Layout: `div.grid.gap-4.md:grid-cols-2` →
- **Columna request**: fila método+path (mono), `<pre>` del request (`whitespace-pre-wrap`, backlog T21) y botón Run al pie.
- **Columna visor**: fila fija de estado (SIEMPRE renderizada, `min-h-8`: idle vacío-reservado / spinner+pending / streaming / badge status + copy) + `Tabs` Preview|Response. Panel Response: `<pre class="h-64 overflow-auto whitespace-pre-wrap …">` (altura FIJA, el streaming scrollea dentro). Panel Preview: contenedor `h-64` con: idle/pending/streaming → placeholder (`labels.previewIdle`, patrón EmptyState de T30); done → `<img src={demo.preview.src} …>` `object-contain h-full` + botón ⛶ (icono solo) que precarga en hover (`preloadFullSources`) y abre `MediaLightbox` con `{ src, fullSrc, alt: labels.previewAlt }`.
- **Cero shift**: TODO estado vive dentro de cajas de altura fija (`min-h-8` la fila de estado, `h-64` ambos paneles — los paneles de `Tabs` ya se apilan en la misma celda de grid, B2). Nada se monta/desmonta fuera de ellas.

- [ ] **Step 1 — tests en rojo** (describes nuevos):

```tsx
it('la fila de estado está reservada también en idle (cero shift)', () => {
  render(<ApiRequestPlayer demo={apiDemo} labels={labels} />);
  expect(screen.getByTestId('player-status-row')).toHaveClass('min-h-8');
});

it('la respuesta escribe dentro de un panel de altura fija', async () => {
  render(<ApiRequestPlayer demo={apiDemo} labels={labels} />);
  const pane = screen.getByTestId('player-response-pane');
  expect(pane).toHaveClass('h-64');
  fireEvent.click(screen.getByRole('button', { name: labels.run }));
  await screen.findByText(labels.done, {}, { timeout: 3000 });
  expect(pane).toHaveClass('h-64'); // sin cambio de clase/tamaño reservado
});

it('done muestra la preview con fullscreen', async () => {
  render(<ApiRequestPlayer demo={apiDemo} labels={labels} />);
  fireEvent.click(screen.getByRole('button', { name: labels.run }));
  await screen.findByText(labels.done, {}, { timeout: 3000 });
  fireEvent.click(screen.getByRole('tab', { name: labels.previewTab }));
  expect(screen.getByRole('img', { name: labels.previewAlt })).toBeInTheDocument();
  fireEvent.click(screen.getByRole('button', { name: labels.fullscreen }));
  expect(screen.getByRole('dialog')).toBeInTheDocument();
});
```

(los timers del flujo pending/streaming ya se manejan en los tests existentes del player — reutilizar su patrón de fake timers/rAF).

- [ ] **Step 2 — FAIL.** · **Step 3 — implementación** (~200 líneas; conservar la máquina de estados y el aria-live actuales, que T19/T30 dejaron finos). `api-demo.ts` añade `preview` apuntando a una imagen de la galería (p.ej. `nbp-amanecer-alpino`) — coherente: la request `text-to-image` "devuelve" ese paisaje. Claves i18n nuevas bajo `sections.apiPlayer.*` (convención T19): `previewTab`, `responseTab`, `previewIdle`, `previewAlt`, `fullscreen`.
- [ ] **Step 4 — PASS + gate** (tests viejos adaptados solo en selectores + nuevos verdes).
- [ ] **Step 5 — commit** `feat(web): API player v2 — playground-style split with zero layout shift and HD preview`

## Bloque E — scrub demo v3 (app)

### Task 14: demo del scrub — asset nuevo, ancho, copy de caso de uso

**Files:** Modify `apps/web/src/components/showcase/video-scrub-demo.tsx` (+ test) · `messages/{es,en}.json` (`sections.mediaKit.scrub*`).

**Interfaces:** Consumes: `scrub.mp4` re-encodeado (T8), scrub v3 del paquete (T6). Produces: demo a ancho completo de la card (deja el `max-w` estrecho — backlog T21 «ancho scrub»), copy que explica el caso de uso, hint táctil corregido (backlog T21 «copy táctil»).

- [ ] **Step 1 — copy nuevo** (es; en equivalente): descripción de sección → «Recorre un vídeo sin reproducirlo: mueve el puntero (o usa las flechas) para saltar a cualquier fotograma, como en las miniaturas de YouTube.»; hint → es: «Desliza o usa las flechas para recorrer el vídeo» (cubre táctil y teclado sin asumir ratón).
- [ ] **Step 2 — test en rojo**: el contenedor de la demo ya no tiene la clase de ancho estrecho (`expect(container.querySelector('.mk-scrub')!.closest('[class*="max-w"]')).toBeNull()` — ajustar al selector real actual) y el hint usa la clave nueva.
- [ ] **Step 3 — implementación**: quitar el cap de ancho, actualizar poster/labels, verificar el facade intacto (el botón de activación de T27 se mantiene).
- [ ] **Step 4 — PASS + gate + verificación manual en dev** (`pnpm run dev`): el scrub va fluido con el asset GOP-4.
- [ ] **Step 5 — commit** `feat(web): scrub demo v3 — keyframe-dense clip, full width, use-case copy`

**GATE de salida (decisión spec §4):** si el design review de T25 no aprueba esta demo, ejecutar T25b (retirada) en esta misma fase.

## Bloque F — navegación

### Task 15: página activa en el menú + no-renavegación + hover

**Files:** Create `apps/web/src/components/layout/nav-link.tsx` · Test `nav-link.test.tsx` · Modify `apps/web/src/components/layout/header.tsx`.

**Interfaces:** Produces:

```ts
export function NavLink(props: { href: string; children: ReactNode }): JSX.Element;
```

Client component: `usePathname()` de `@/i18n/navigation`; activo si pathname === href o (href !== '/' y pathname empieza por `${href}/`). Activo → `aria-current="page"` + estilo (`text-fg font-semibold` + subrayado de acento `underline decoration-accent underline-offset-8`); inactivo → `text-fg-muted hover:text-fg transition-colors` (B3). Click estando activo → `event.preventDefault()` (B1: no re-navega).

- [ ] **Step 1 — tests en rojo** (mock de `usePathname` como en los tests existentes de layout):

```tsx
it('marca el enlace de la página actual', () => {
  mockPathname('/cv');
  render(<NavLink href="/cv">CV</NavLink>);
  expect(screen.getByRole('link', { name: 'CV' })).toHaveAttribute('aria-current', 'page');
});

it('clicar el enlace activo no navega', () => {
  mockPathname('/cv');
  render(<NavLink href="/cv">CV</NavLink>);
  const event = createEvent.click(screen.getByRole('link', { name: 'CV' }));
  fireEvent(screen.getByRole('link', { name: 'CV' }), event);
  expect(event.defaultPrevented).toBe(true);
});

it('el enlace inactivo no lleva aria-current y no previene', () => {
  mockPathname('/cv');
  render(<NavLink href="/projects">Proyectos</NavLink>);
  expect(screen.getByRole('link', { name: 'Proyectos' })).not.toHaveAttribute('aria-current');
});
```

- [ ] **Step 2 — FAIL.** · **Step 3 — implementación** + header.tsx usa `<NavLink>` para los 4 enlaces. · **Step 4 — PASS + gate.** · **Step 5 — commit** `feat(web): active nav state with aria-current, hover styles, no self-renavigation`

### Task 16: menú hamburguesa en mobile

**Files:** Create `apps/web/src/components/layout/mobile-menu.tsx` · Test `mobile-menu.test.tsx` · Modify `apps/web/src/components/layout/header.tsx` (+ test) · `messages/{es,en}.json` (`nav.menuOpen`, `nav.menuClose`).

**Interfaces:** Consumes: `NavLink` (T15), switchers existentes. Produces: en `<sm` la nav inline y los switchers se ocultan (`hidden sm:flex`) y aparece un botón hamburguesa (`sm:hidden`) que despliega un panel bajo el header con los 4 NavLink en columna + los 3 switchers.

Comportamiento: botón con `aria-expanded` + `aria-controls`; icono hamburguesa/X (SVG inline, `aria-hidden`; nombre accesible por `aria-label` i18n). Panel `id` correlado; `Escape` cierra y devuelve el foco al botón; clicar cualquier enlace cierra; sin focus trap (es un desplegable, no un dialog — el foco fluye, patrón disclosure WAI-ARIA).

- [ ] **Step 1 — tests en rojo**:

```tsx
it('abre y cierra con aria-expanded correcto', () => {
  render(<MobileMenu labels={{ open: 'Abrir menú', close: 'Cerrar menú' }}>{links}</MobileMenu>);
  const button = screen.getByRole('button', { name: 'Abrir menú' });
  expect(button).toHaveAttribute('aria-expanded', 'false');
  fireEvent.click(button);
  expect(screen.getByRole('button', { name: 'Cerrar menú' })).toHaveAttribute('aria-expanded', 'true');
  expect(screen.getByRole('link', { name: 'CV' })).toBeVisible();
});

it('Escape cierra y devuelve el foco al botón', () => {
  render(<MobileMenu labels={labels}>{links}</MobileMenu>);
  fireEvent.click(screen.getByRole('button', { name: 'Abrir menú' }));
  fireEvent.keyDown(document.activeElement ?? document.body, { key: 'Escape' });
  expect(screen.getByRole('button', { name: 'Abrir menú' })).toHaveFocus();
});

it('clicar un enlace cierra el panel', () => {
  render(<MobileMenu labels={labels}>{links}</MobileMenu>);
  fireEvent.click(screen.getByRole('button', { name: 'Abrir menú' }));
  fireEvent.click(screen.getByRole('link', { name: 'CV' }));
  expect(screen.getByRole('button', { name: 'Abrir menú' })).toHaveAttribute('aria-expanded', 'false');
});
```

- [ ] **Step 2 — FAIL.** · **Step 3 — implementación** (~90 líneas) + header: nav actual `hidden sm:flex`, switchers `hidden sm:flex`, `<MobileMenu>` `sm:hidden` con NavLinks + switchers dentro. · **Step 4 — PASS + gate + check visual dev 375px** (8 combos en dos: dark+light del skin default mínimo). · **Step 5 — commit** `feat(web): mobile hamburger menu with disclosure pattern`

## Bloque G — contenido

### Task 17: skills con estrellas (C2)

**Files:** Modify `apps/web/src/data/skills.ts` · los tests de datos existentes siguen verdes.

Cambios EXACTOS (decisión Nico, feedback C2 — incluye bajar niveles):
1. Añadir `{ name: 'JavaScript', level: 5, category: 'frontend', tags: ['javascript', 'es2024'] }` PRIMERO de frontend.
2. Fusionar React y Next.js → una entrada `{ name: 'React / Next.js', level: 4, category: 'frontend', tags: ['react', 'nextjs', 'ssr', 'ui'] }` (eliminar las dos entradas actuales).
3. TypeScript `level: 5` → `4`.
4. `Vue 2/3` `level: 4` → `3`.
5. El resto queda igual (la lista ya cubre PHP/Node/Docker/HL7 — revisada en brainstorm).

- [ ] **Step 1 — test en rojo** (en `apps/web/src/data/gallery.test.ts` NO — crear describe en el test de datos existente o `skills.test.ts` nuevo):

```ts
it('refleja la revisión de niveles del feedback C2', () => {
  const byName = Object.fromEntries(skills.map((s) => [s.name, s.level]));
  expect(byName['JavaScript']).toBe(5);
  expect(byName['React / Next.js']).toBe(4);
  expect(byName['TypeScript']).toBe(4);
  expect(byName['Vue 2/3']).toBe(3);
  expect(byName['React']).toBeUndefined();
  expect(byName['Next.js']).toBeUndefined();
});
```

- [ ] **Step 2 — FAIL → implementar → PASS + gate** (ojo: si algún test/snapshot contaba skills, actualizarlo — cambio de contrato documentado aquí).
- [ ] **Step 3 — commit** `feat(web): skills review — JavaScript 5★, merged React/Next.js 4★, TypeScript 4★, Vue 3★`

### Task 18: intro de carrera + disponibilidad (C1)

**Files:** Modify `apps/web/src/data/profile.ts` · `apps/web/src/components/home/hero.tsx` (+ test) · `messages/{es,en}.json` (`home.availability`, `home.availabilityCta`).

**Interfaces:** Produces: `profile.summary` reescrito (carrera, no trabajo actual) y hero con badge de disponibilidad + CTA.

- [ ] **Step 1 — copy** (propuesto; Nico veta en review de la task):

```ts
summary: {
  es: 'Ingeniero en Informática con más de 12 años construyendo software full-stack en tres mundos distintos: salud (historia clínica electrónica e interoperabilidad HL7), producto internacional para clientes de EE. UU. y plataformas públicas de APIs de IA a gran escala. Perfil de punta a punta: del spec y el backend al frontend, la documentación y el producto, con más de 1.000 PRs en los últimos cuatro años. Cómodo liderando proyectos, mentorizando y trabajando con equipos distribuidos.',
  en: 'Computer engineer with 12+ years building full-stack software across three different worlds: healthcare (EHR and HL7 interoperability), international product work for US clients, and large-scale public AI API platforms. End-to-end profile: from spec and backend to frontend, documentation and product, with 1,000+ PRs over the last four years. Comfortable leading projects, mentoring and working with distributed teams.',
},
```

Claves i18n: es `availability: "Disponible para nuevas oportunidades"`, `availabilityCta: "Contactar"`; en `"Open to new opportunities"`, `"Get in touch"`.

- [ ] **Step 2 — test en rojo** (hero test existente, describe nuevo): el hero renderiza el badge de disponibilidad y un enlace CTA a `profile.links.linkedin` con `target="_blank"` y `rel="noopener noreferrer"`.
- [ ] **Step 3 — implementación**: en `hero.tsx`, bajo el headline: `<Badge variant="accent">{t('availability')}</Badge>` + `<a href={profile.links.linkedin} …>{t('availabilityCta')}</a>` con estilo de botón (`Button` asChild o clase equivalente del repo).
- [ ] **Step 4 — PASS + gate.** · **Step 5 — commit** `feat(web): career-focused intro and visible availability CTA`

### Task 19: CV timeline rail v2 + fecha-badge + fin de etapa Freepik/Magnific

**Files:** Modify `apps/web/src/components/cv/cv-timeline.tsx` (+ `cv-views.test.tsx` describe nuevo) · `apps/web/src/components/cv/experience-entry.tsx` (+ test: prop opcional) · `apps/web/src/data/experience.ts`.

**Interfaces:** Produces: (a) `ExperienceEntryBlock` gana prop opcional `hideDates?: boolean` (default false — resto de vistas intactas); (b) la vista timeline pinta la fecha como `Badge` dentro de la card; (c) datos: entrada `freepik` con `company: 'Freepik/Magnific'` y `end: '2026-07'`.

- [ ] **Step 1 — datos**: en `experience.ts`, `company: 'Freepik'` → `'Freepik/Magnific'`, `end: null` → `'2026-07'`. (Las vistas formatean el rango desde `start/end` — desaparece el `presentLabel` para esta entrada automáticamente.)
- [ ] **Step 2 — test en rojo** (timeline):

```tsx
it('muestra la fecha en badge dentro de la card y no duplicada', () => {
  render(<CvTimeline locale="es" strings={strings} />);
  const freepikCard = screen.getByText('Freepik/Magnific').closest('li')!;
  // El Badge de rango lleva data-testid="timeline-date" (lo añade esta task)
  expect(within(freepikCard).getByTestId('timeline-date')).toHaveTextContent(/2022/);
  // ExperienceEntryBlock no vuelve a pintar el rango (hideDates)
  expect(within(freepikCard).getAllByText(/2022/)).toHaveLength(1);
});
```

(usar el helper de formateo de fechas del repo — el que use `experience-entry.tsx` hoy — para construir la aserción exacta.)

- [ ] **Step 3 — implementación**: en `cv-timeline.tsx`, estructura del rail (visual A del brainstorm): `<ol>` con línea `before` de acento degradado (`bg-gradient-to-b from-accent/60 to-accent/10`), nodo por entrada (`size-2.5 rounded-full bg-accent ring-4 ring-accent/20`), card `rounded-card border border-border p-4` conteniendo `<Badge>{rango}</Badge>` + `<ExperienceEntryBlock hideDates …>`. En `experience-entry.tsx`: envolver el render del rango en `{!hideDates && …}`.
- [ ] **Step 4 — PASS + gate + check visual dev** (ambos temas, mobile 375px: un solo layout).
- [ ] **Step 5 — commit** `feat(web): CV timeline v2 — accent rail with nodes and date badges; Freepik/Magnific stage ends 2026-07`

### Task 20: proyectos — quick fixes E1-E3 + URLs E5-E6

**Files:** Modify `apps/web/src/app/[locale]/projects/[slug]/page.tsx` (+ test si existe; si no, cubrir en `content.test.ts`/review) · Create `apps/web/src/lib/mdx-components.tsx` (+ test) · Modify `apps/web/src/lib/content.ts` (usar el map de componentes) · `apps/web/src/data/projects.ts` · `apps/web/src/components/projects/project-card.tsx` (+ test) · `messages/{es,en}.json` (`projects.backToList`).

- [ ] **Step 1 — tests en rojo**:

```tsx
// mdx-components.test.tsx
it('los enlaces externos del MDX abren en pestaña nueva', () => {
  const A = mdxComponents.a!;
  render(<A href="https://example.com">Ext</A>);
  const link = screen.getByRole('link', { name: 'Ext' });
  expect(link).toHaveAttribute('target', '_blank');
  expect(link).toHaveAttribute('rel', 'noopener noreferrer');
});
it('los enlaces internos no', () => {
  const A = mdxComponents.a!;
  render(<A href="/es/projects">Int</A>);
  expect(screen.getByRole('link', { name: 'Int' })).not.toHaveAttribute('target');
});
```

```ts
// projects.test.ts (o describe en test de datos)
it('URLs reales del feedback E5-E6', () => {
  const bySlug = Object.fromEntries(projects.map((p) => [p.slug, p]));
  expect(bySlug['freepik-api-playground'].links.live).toBe('https://www.magnific.com/api/playground');
  expect(bySlug['cadi'].links.live).toBe('https://www.cadigolf.com/');
});
```

- [ ] **Step 2 — FAIL → implementación**:
  - `mdx-components.tsx`: map `{ a: ExternalAwareLink }` (externo = `/^https?:\/\//`); estilo hover destacado para TODOS los links de prosa (E2): en `globals.css`, `.prose-portfolio a:hover { text-decoration-color: var(--color-accent); }` + `transition` (solo tokens).
  - `content.ts`: pasar `components: mdxComponents` al render de next-mdx-remote.
  - `[slug]/page.tsx`: sobre el `<article>`: `<Link href="/projects" className="text-sm text-fg-muted hover:text-fg">← {t('backToList')}</Link>` (es: «Volver a proyectos», en: "Back to projects").
  - `projects.ts`: `freepik-api-playground.links.live = 'https://www.magnific.com/api/playground'`; `cadi.links = { live: 'https://www.cadigolf.com/' }`.
  - `project-card.tsx`: links externos (live/docs) con `target="_blank" rel="noopener noreferrer"` si no lo tienen ya (E3; verificar también footer y cv — el CV ya lo hace).
- [ ] **Step 3 — PASS + gate.** · **Step 4 — commit** `feat(web): project detail back link, highlighted link hover, external links in new tab, real URLs`

### Task 21: naming — Freepik → Freepik/Magnific (E4)

**Files:** Modify según grep: `apps/web/src/data/*.ts`, `apps/web/messages/{es,en}.json`, `apps/web/content/{es,en}/projects/*.mdx` (rutas explícitas al add).

- [ ] **Step 1 — inventario**: `grep -rn "Freepik" apps/web/src/data apps/web/messages apps/web/content/es apps/web/content/en --include='*.ts' --include='*.json' --include='*.mdx' | grep -v "Freepik/Magnific"`. Criterio (decisión E4): TODA referencia a la **empresa/plataforma** pasa a «Freepik/Magnific»; se conservan «Freepik API» SOLO dentro de URLs y nombres propios de producto ya compuestos («Freepik/Flaticon content backoffice» → mantener: nombra productos, no la empresa).
- [ ] **Step 2 — aplicar** el barrido (data: `experience.ts` ya hecho en T19 para company; revisar summaries/highlights; projects.ts títulos/summaries; MDX; messages).
- [ ] **Step 3 — verificación**: re-grep = solo restos legítimos (URLs, nombres de producto); gate completo (parity, snapshots que citen el nombre).
- [ ] **Step 4 — commit** `fix(web): company naming — Freepik/Magnific everywhere (E4)`

### Task 22: case study nuevo — «Integración de servicios automatizada a través de IA» (E7)

**Files:** Modify `apps/web/src/data/projects.ts` (reemplaza entrada `ai-model-onboarding`) · Create `apps/web/content/es/projects/ai-service-integration.mdx`, `apps/web/content/en/projects/ai-service-integration.mdx` · Delete `apps/web/content/{es,en}/projects/ai-model-onboarding.mdx` · Modify `apps/web/src/data/experience.ts` (highlight 3 de freepik).

**Procedencia (regla F3, INNEGOCIABLE):** el contexto de `~/workspace/freepik/freepik-ai-context/api-b2b` puede LEERSE para entender el flujo, pero NADA interno se copia: el MDX describe el patrón en términos genéricos y públicos.

- [ ] **Step 1 — entrada en projects.ts** (sustituye a `ai-model-onboarding` completa):

```ts
{
  slug: 'ai-service-integration',
  title: {
    es: 'Integración de servicios automatizada a través de IA',
    en: 'AI-automated service integration',
  },
  summary: {
    es: 'Sistema de agentes de IA especializados que automatiza el alta de un servicio nuevo en la plataforma de APIs: a partir de la definición del servicio generan la especificación OpenAPI, la implementación del endpoint, la configuración de precios y límites, los permisos y reglas de gateway, la documentación pública y su entrada en el Playground — para cualquier servicio, no solo modelos concretos.',
    en: 'System of specialized AI agents that automates onboarding a new service into the API platform: from the service definition they generate the OpenAPI spec, the endpoint implementation, pricing and rate-limit configuration, gateway permissions and rules, public documentation and its Playground entry — for any service, not just specific models.',
  },
  role: { es: 'Ingeniero — diseño y orquestación de los agentes', en: 'Engineer — agent design and orchestration' },
  stack: ['openapi', 'fastapi', 'apisix', 'llm-agents', 'python'],
  links: { docs: 'https://docs.freepik.com' },
  metrics: [
    {
      // 6 = spec, API, precios, permisos/reglas, documentación, playground
      label: { es: 'Artefactos generados por servicio', en: 'Artifacts generated per service' },
      value: '6',
    },
  ],
  featured: true,
  caseStudy: true,
  date: '2022-07',
},
```

- [ ] **Step 2 — MDX es/en** (frontmatter con el schema `projectFrontmatterSchema` — copiar la forma de `freepik-api-platform.mdx`). Estructura (≈60 líneas por locale, redactadas de verdad, clean-room):
  1. **El problema**: dar de alta un servicio en una plataforma pública de APIs son 6+ artefactos coordinados entre repos (spec, servidor, gateway, precios, docs, playground) — proceso manual, propenso a inconsistencias.
  2. **La solución**: agentes especializados (uno por artefacto) orquestados sobre la definición del servicio; cada agente conoce las convenciones de su repo y produce el cambio listo para review humano.
  3. **Qué genera**: lista de los 6 artefactos con una línea cada uno.
  4. **Impacto**: onboarding repetible y consistente; el proceso que antes se aplicaba a mano a familias como Kling o WAN queda generalizado a cualquier servicio.
  5. **Lecciones**: los agentes rinden con convenciones estrictas y fuentes de verdad únicas (mismo principio que gobierna este portfolio).
- [ ] **Step 3 — experience.ts**: highlight «Onboarding end-to-end de modelos de IA (Kling, WAN…)…» → «Integración automatizada de servicios mediante agentes de IA: de la definición del servicio a spec, implementación, precios, permisos, documentación y playground.» (es/en).
- [ ] **Step 4 — borrar** los 2 MDX antiguos; gate + `pnpm run build` (la ruta vieja desaparece del export, la nueva prerenderiza en es/en).
- [ ] **Step 5 — commit** (añadir MDX por ruta explícita): `feat(web): replace onboarding case study with AI-automated service integration (E7)`

### Task 23: métricas reales verificadas (E8) — ORQUESTADOR

**Files:** Modify `apps/web/src/data/projects.ts` (métricas de `freepik-api-platform`).

El orquestador (acceso web) ejecuta la verificación; regla de procedencia: SOLO cifras derivables de fuentes públicas.

- [ ] **Step 1 — verificar**: contar en <https://docs.freepik.com> (API reference pública) los modelos de IA listados y los endpoints públicos documentados; contrastar con el playground público (<https://www.magnific.com/api/playground>). Guardar el conteo y la fecha en el ledger (evidencia).
- [ ] **Step 2 — actualizar** `metrics` de `freepik-api-platform` con los valores redondeados hacia abajo con `+` (p.ej. si se cuentan 63 modelos → `'60+'`; si 130 endpoints → `'130+'`). Si alguna cifra NO es derivable públicamente, se deja la actual (documentar).
- [ ] **Step 3 — gate + commit** `fix(web): platform metrics verified against public docs (E8)`

## Bloque H — polish backlog T21

### Task 24: pack de polish (design review T21, ver ledger líneas 167-168)

**Files:** Según ítem: `apps/web/src/components/home/hero-canvas.tsx` (+ test) · `apps/web/src/components/projects/more-projects-card.tsx` (+ test) · `apps/web/src/components/showcase/compare-modes-demo.tsx` · `apps/web/src/components/showcase/media-kit-demo.tsx` / `spotlight-demo.tsx` · `messages/{es,en}.json` · fichero de card afectado a 768px (localizar con el finding).

Fuente de verdad de cada finding: ledger `.superpowers/sdd/progress.md` líneas 167-168 (design review T21 M2-M7/N1-N3 + code review Minors) — leerlas ANTES. Ítems (los de showcase/galería ya cayeron en T2/T11/T13/T14):

1. **Canvas dark en reposo**: en dark, el estado de reposo del hero canvas es demasiado tenue/plano — ajustar la opacidad base de los puntos en reposo (leer `--color-accent` ya re-leído por MutationObserver; subir el mínimo de opacidad en reposo, mismo valor ambos temas si el review lo valida). Test: el valor de reposo documentado.
2. **Key i18n propia del switch blink**: el switch pausa/reanuda del modo blink reutiliza una clave — crear `sections.mediaKit.modes.blinkToggle` (es/en) y usarla (nombre accesible estable, Minor del code review).
3. **Jerarquía «y 8 más»** (`MoreProjectsCard`): el conteo/lista adelantada compite con los títulos — bajar peso tipográfico del sufijo (`text-fg-muted text-sm`) y verificar nombre accesible intacto (T30 lo arregló).
4. **Card rota/agolpada a 768px**: reproducir con el finding M del ledger, fix de grid/wrap en el breakpoint md del componente afectado.
5. **Segundo asset para compare/spotlight**: usar una imagen de la galería (T8) como segundo ejemplo (p.ej. el retrato neón en el spotlight), eliminando la sensación de «misma foto en todas las demos» (N del review). Sin assets nuevos: reutilizar.

- [ ] **Step 1** — leer findings en el ledger; para cada ítem: test en rojo si es comportamiento (2, 3), fix + verificación visual si es puramente visual (1, 4, 5).
- [ ] **Step 2** — gate + verificación en dev de los 5 (dark+light).
- [ ] **Step 3** — commit por ítem o consolidado si son de un mismo fichero: `fix(web): T21 design-review polish pack (canvas idle, blink key, hierarchy, 768px card, second asset)`

## Cierre

### Task 25: cierre de fase — gate, verificación en vivo, reviews, merge

**Files:** Modify `docs/superpowers/plans/2026-07-10-portfolio-roadmap.md` (F3.7 ✅) · `STATUS.md` · ledger.

- [ ] **Step 1 — gate completo**: `pnpm run lint && pnpm run typecheck && pnpm run test` + `pnpm run build` + `NEXT_OUTPUT_MODE=node pnpm run build` (duales verdes).
- [ ] **Step 2 — verificación en vivo** (Playwright MCP sobre `npx serve apps/web/out`, es+en, dark+light) — la cobertura "E2E" de spec §8:
  - Galería: filtro por categoría, búsqueda («seedream» → 4, «zzz» → empty), hover-autoplay de un vídeo (delay), fullscreen de imagen (verificar en Network que la HD se pidió en el hover del ⛶ ANTES del click), audio play/pause, 0 bytes de mp4/mp3 sin interacción (gate assets).
  - API player: medir `getBoundingClientRect().height` del contenedor en idle → run → done: **igual ±0px**; tabs Preview/Response; fullscreen del resultado.
  - Menú mobile (viewport 375): abrir/cerrar, Escape, foco, navegación cierra. Nav activo con `aria-current`.
  - CV timeline, back link de proyectos, case study nuevo, scrub fluido.
  - 0 console errors en todas las páginas.
- [ ] **Step 3 — Lighthouse protocolo T21**: host ocioso, `npx serve` plano, A/B intercalado main vs rama (3 runs por lado): mediana rama ≥ 90 y First Load JS ≤ el de main (comparar el `pnpm run build` output de ambos). Si la galería empeoró First Load JS → mover datos/imports al chunk dinámico hasta restaurarlo.
- [ ] **Step 4 — design review** (`agents/design-reviewer.md`): incluye el **GATE del scrub** (spec §4): veredicto explícito sobre la demo. Si NO aprueba → **T25b**: retirar la demo del showcase (quitar sección+TOC+i18n keys; el componente y su README quedan en el paquete), commit `refactor(web): retire scrub demo per design review gate`.
- [ ] **Step 5 — qa-a11y-perf** (`agents/qa-a11y-perf.md`): axe 0 violations (es/en × dark/light, home/cv/projects/showcase), teclado completo en galería/menú/player.
- [ ] **Step 6 — fix-first**: todo Critical/Important de los reviews se arregla y re-verifica antes de seguir (patrón T21/T30).
- [ ] **Step 7 — code review final** (skill `nico-code-review` / reviewer Fable) sobre el diff completo de la rama.
- [ ] **Step 8 — docs**: roadmap F3.7 ✅; STATUS.md (hecho/siguiente = F4 First Load JS); ledger con el cierre.
- [ ] **Step 9 — merge**: `superpowers:finishing-a-development-branch` (FF a main + push, patrón de fases anteriores).

---

## Inventario de features del portfolio + punteros a decisiones técnicas

*(Requisito transversal F3.7 — punteros, no duplicación. «Spec» = `docs/superpowers/specs/2026-07-10-portfolio-design.md`; «Spec 3.6» = `…2026-07-16-phase-3.6-showcase-mediakit-design.md`; «Spec 3.7» = `…2026-07-18-phase-3.7-showcase-ux-content-design.md`; «Ledger» = `.superpowers/sdd/progress.md`; «Roadmap» = `docs/superpowers/plans/2026-07-10-portfolio-roadmap.md`, que enlaza el plan de cada fase.)*

### Plataforma y arquitectura

| Feature | Diseño/arquitectura | Otras decisiones |
| --- | --- | --- |
| Monorepo pnpm (apps/web + packages/media-kit) | Spec §4 | Roadmap F0 |
| Runtime dual export/node sin reescribir componentes | Spec §4.2 · `skills/nextjs-static-dual` | `docs/architecture.md` |
| Config de agentes agnóstica (AGENTS.md + symlinks) | Spec §5 · `AGENTS.md` | `scripts/setup-agents.sh` (CI valida) |
| Pins de dependencias (Node 20) | `docs/decisions/2026-07-10-dependency-pins.md` | STATUS «Pendientes» (upgrade Node 22) |
| Deploy 3 targets + CI | Spec §7 · `docs/deploy/` | Roadmap F5 |

### Design system y UI base

| Feature | Diseño | Testing/a11y |
| --- | --- | --- |
| Tokens semánticos + dark/light + 4 skins (`data-theme`/`data-skin`) | Spec §3 · `skills/tailwind-tokens` | AA 8 combos: Spec 3.6 «Restricciones»; axe 0 violations (Ledger T30) |
| Primitivas UI con cva (Button, Badge, Tabs, Input…) | Roadmap F1 · `skills/component-patterns` | Convención cursor A2: Ledger F3.6 T25 |
| Tabs sin layout shift (paneles apilados en grid) | Spec 3.6 B2 | Cambio de contrato documentado (plan F3.6 T12) |
| Skin switcher con aria-current + hover | Spec §2.1 «Fixes UI» | Ledger F3.5 |
| Nav activa + hamburguesa mobile + hover | Spec 3.7 §5 (este plan T15-T16) | Patrón disclosure WAI-ARIA (T16) |

### Contenido y páginas

| Feature | Diseño/contenido | Procedencia/seguridad |
| --- | --- | --- |
| Contenido como datos tipados (Zod, build-validated) | Spec §4.3 | `validate-content` en CI |
| i18n es/en completo (next-intl, prerender por locale) | Spec §4.4 | Test de paridad `messages-parity` |
| SEO: metadata, OG estático, sitemap, JSON-LD | Spec §4.4 | Guard `NEXT_PUBLIC_SITE_URL` → backlog F4 |
| Home: hero + intro carrera + disponibilidad | Spec §2.1 «Home» · Spec 3.7 §6 (T18) | Copy sin datos internos |
| Skills con estrellas (niveles revisados) | Spec §2.1 «Skills» · Spec 3.7 §6 (T17) | Evidencia LinkedIn/repos (Spec §2.1) |
| CV 3 vistas + timeline rail v2 + print | Roadmap F3 · Spec 3.7 §6 (T19) | PII: `apps/web/content/cv/` gitignored (Spec §2.1) |
| Proyectos + case studies MDX (es/en) | Spec §2.1 «Proyectos» · Spec 3.7 §6 | Clean-room F3 (Spec §3); métricas solo de fuentes públicas (T23) |
| Case study «Integración de servicios por agentes IA» | Spec 3.7 §6.7 (T22) | Procedencia api-b2b sin copia (Spec 3.7 §9) |
| Métrica «+1.000 PRs» solo global | Spec §2.1 «Métrica de PRs» | — |

### Showcase y media-kit

| Feature | Diseño | Performance | Testing |
| --- | --- | --- | --- |
| Showcase índice-que-filtra + deep-link hash | Spec 3.6 B1 | `next/dynamic` en demos (Ledger T31) | `showcase-view.test` |
| Galería IA 16 ítems (filtro categoría+texto, HD fullscreen) | Spec 3.7 §2 + §0 (decisiones brainstorm) | Facade 0-bytes + chunk dinámico + presupuestos §7 | T9-T12 + verificación en vivo T25 |
| API player v2 split sin layout shift | Spec 3.7 §3 (layout companion visual) | Alturas reservadas (T13) | Test altura estable + medición en vivo |
| CompareSlider (wipe/onion/blink/side-by-side, responsive) | Spec 3.6 A3 · Spec 3.7 §1 (T1-T2) | preload fullSrc (plan 2.6) | Tests por modo (paquete) |
| MediaLightbox (zoom/pan, fullscreen, tooltips delay, fit) | Spec 3.6 A1-A2 · plan F2.5/F2.6 (Roadmap) | fullSrc según pantalla (`media-source.ts`) | Suite del paquete |
| SpotlightReveal | Spec 3.6 A4 | — | Paquete |
| FilterGallery (FLIP + visibleIds + salida animada) | Spec 3.6 A5 · Spec 3.7 §1 (T3-T4) | WAAPI, reduced-motion instantáneo | Paquete |
| VideoScrubPreview v3 (+gate de salida) | Spec 3.6 A6 · Spec 3.7 §4 | GOP-4 re-encode (T8) | Gate design review (T25) |
| HoverVideo | Spec 3.7 §1.5 (T5) | Facade 0-bytes | Paquete |
| media-kit publicable npm (0.6.0) | Spec §4.1 · CHANGELOG del paquete | 0 deps runtime | Publish manual (Spec §9) |

### Playground (F4, diseño cerrado)

| Feature | Diseño | Seguridad |
| --- | --- | --- |
| Puerto/adaptadores mock·pollinations·proxy | Spec §4.5 | Secretos SOLO en modo Node/proxy (Spec §3) |
| ApiRequestPlayer reutilizado con datos reales | Spec 3.6 C3 · Spec 3.7 §3 | — |

### Calidad transversal

| Área | Decisiones |
| --- | --- |
| Testing | TDD por tarea (Spec §6); Vitest paquete+app; verificación en vivo Playwright MCP (protocolo T21, Ledger «T21 GATE PERF»); sin framework e2e (desviación documentada Spec 3.7) |
| Performance | Lighthouse ≥90 con protocolo host-ocioso (Ledger T31/172); presupuestos de assets (Spec 3.6 §Restricciones + Spec 3.7 §7); facade para media pesada (Ledger T27); First Load JS >2× → **primera prioridad F4** (Ledger 175) |
| Accesibilidad | WCAG AA 8 combos (Spec §3); axe 0 violations + Lighthouse A11y 100 (Ledger T30); `prefers-reduced-motion` por componente (Specs 3.6/3.7) |
| Seguridad | Sin secretos en bundle (Spec §3); deps solo OSS público (Spec §3); PII gitignored; procedencia clean-room (Spec §2.1/§3); `rel="noopener noreferrer"` en externos (T20) |
| Observabilidad | Sitio estático sin telemetría runtime: la observabilidad del proyecto es CI (lint/type/test/validate-content/agents:validate) + ledger SDD (`.superpowers/sdd/progress.md`) + STATUS.md como resumen operativo |

## Backlog que este plan NO cubre (queda para F4)

First Load JS >2× presupuesto (palanca real de perf) · img «after» del paquete sin lazy/resize · `aria-describedby` FilterableList · token `surface-hover` · sweep `z.url()`/consistencia · guard `NEXT_PUBLIC_SITE_URL` · contraste borders 1.4.11 · publicación npm 0.6.0 (manual, Nico).
