# Phase 2.6 — @nicobehm/media-kit v2.1 + showcase polish — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Pulir `MediaLightbox` (pan con Espacio + cursor grab, help box de teclado `?`, toggle ojo/ojo-tachado con tooltip), añadir el ejemplo color/B-N con retrato IA y fullscreen nativo del slider, arreglar el layout-shift de `Tabs` y enriquecer el showcase — media-kit `0.2.0 → 0.3.0`, cero regresiones.

**Architecture:** Todo lo nuevo del paquete vive en `media-lightbox.tsx` (estado espacio-pan, estado `helpOpen`, precedencia de `Escape`, iconos SVG inline) + un subcomponente presentacional nuevo `LightboxHelp`. Los hooks v2 (`useZoomPan`, `useAutoHide`, `useFullscreen`) y `LightboxControls` NO cambian de API. En la app: `TabPanel` deja de desmontarse (usa `hidden`), y el showcase gana índice lateral, intros, estados y el demo del retrato.

**Tech Stack:** React 18+ (peer), TypeScript strict, tsup, Vitest + Testing Library (jsdom), CSS plano `--mk-*`; app Next.js 16 App Router (static export por defecto), Tailwind v4 con tokens semánticos.

**Spec:** `docs/superpowers/specs/2026-07-14-media-kit-v2.1-showcase-design.md` (leer antes de empezar).

## Global Constraints

- **v2.1 = v2 + capacidades, cero regresiones.** Los tests v1/v2 existentes del paquete NO se modifican (ni sus aserciones ni su comportamiento); los tests nuevos se AÑADEN al final del fichero en `describe` propios (patrón F2.5). Dos excepciones acotadas: (1) el fixture `LABELS` de `lightbox-controls.test.tsx` se COMPLETA con las claves nuevas del tipo (obligatorio para que compile; cero cambios de aserciones — Task 3); (2) 1 aserción del test v1 de `Tabs` cuyo contrato cambia el spec C1 (documentado en Task 8).
- Rama de trabajo: `feature/phase-2.6-media-kit-v2.1` (ya creada desde `main`).
- TDD estricto (test failing primero). Gate por tarea: `pnpm run lint && pnpm run typecheck && pnpm run test` en verde antes de cada commit.
- 0 dependencias runtime nuevas. Los hooks siguen internos; `index.ts` no cambia (los tipos `MediaLightboxFit`/`MediaLightboxLabels` ya se exportan).
- Idioma del paquete: defaults en inglés; todo texto nuevo vía `labels` (las teclas/glifos del help son literales no traducibles, por diseño del spec).
- Colores hardcodeados SOLO en `packages/media-kit/src/styles.css`, `apps/web/src/app/globals.css` y SVGs/assets de demo. Los SVG inline del ojo usan `currentColor` (no es color hardcodeado).
- Presupuesto de imagen (skill `performance`): retrato demo ≤ 200 kB, WebP, ~1600px de ancho, `width`/`height` explícitos (sin CLS).
- **NUNCA** `git add apps/web/content/` — los PDFs del CV contienen PII y su tratamiento es una decisión del usuario pendiente. Añadir ficheros a git siempre por ruta explícita, jamás `git add .` / `git add -A` desde la raíz.
- Los subagentes leen las skills de conocimiento ANTES de tocar su tema: `accessibility` (help/tooltip/ojo/teclado), `component-patterns` (help box/estados), `tailwind-tokens` (showcase), `performance` (imágenes), `code-conventions` (creada en Task 1; todo el código de la fase), `nextjs-static-dual` (demo con fullscreen — componente cliente).
- Versionado: `0.2.0 → 0.3.0` (Task 11). B1–B3 son cambios del paquete; B4 y C1–C2 son de la app.

## Mapa de ficheros

```
skills/code-conventions/SKILL.md                       # CREATE (Task 1)
AGENTS.md                                              # MODIFY (Task 1: fila de routing)
packages/media-kit/src/media-lightbox/
├── media-lightbox.tsx        # MODIFY: espacio-pan (T2), help + Escape (T4), ojo/tooltip (T5)
├── media-lightbox.test.tsx   # MODIFY: añadir describes v2.1 al final (T2, T4, T5)
├── lightbox-help.tsx         # CREATE (Task 3)
├── lightbox-help.test.tsx    # CREATE (Task 3)
└── (use-zoom-pan / use-auto-hide / use-fullscreen / lightbox-controls SIN CAMBIOS)
packages/media-kit/src/styles.css                      # MODIFY: cursor grab (T2), panel help (T4), tooltip (T5)
packages/media-kit/README.md · CHANGELOG.md · package.json   # MODIFY (Task 11)
apps/web/public/demo/portrait.webp                     # CREATE (Task 6, orquestador)
apps/web/src/components/showcase/portrait-compare-demo.tsx        # CREATE (Task 7)
apps/web/src/components/showcase/portrait-compare-demo.test.tsx   # CREATE (Task 7)
apps/web/src/components/showcase/media-kit-demo.tsx    # MODIFY (Task 7)
apps/web/src/components/ui/tabs/tabs.tsx               # MODIFY (Task 8)
apps/web/src/components/ui/tabs/tabs.test.tsx          # MODIFY (Task 8: 1 aserción + describe nuevo)
apps/web/src/app/showcase/page.tsx                     # MODIFY (Task 9: estructura+índice; Task 10: estados)
apps/web/src/components/ui/button/button.tsx           # MODIFY (Task 10: :active)
docs/superpowers/plans/2026-07-10-portfolio-roadmap.md # MODIFY (Task 12)
```

---

### Task 1: Skill `code-conventions` (adaptación genérica, sin contenido interno)

Adapta a este repo lo GENÉRICO de las skills internas de `fc_freepik_web` (`typescript`, `react-nextjs` y la parte no-propietaria de `fc-guild-agreements`/`testing`). PROHIBIDO incluir nombres internos de Freepik, URLs internas, paquetes `@fc/*`, comandos internos o acuerdos de gremio: este repo es público. El skill NO menciona su origen.

**Files:**

- Create: `skills/code-conventions/SKILL.md`
- Modify: `AGENTS.md` (tabla "Skills routing")

**Interfaces:**

- Consumes: nada.
- Produces: skill de conocimiento que el resto de tareas debe leer antes de escribir código.

- [ ] **Step 1: Crear `skills/code-conventions/SKILL.md`** con exactamente este contenido:

```markdown
---
name: code-conventions
description: Concrete TypeScript, React hooks and code-style conventions for this repo. Use when writing or reviewing any TypeScript/React code — types, hooks, naming, imports, component internals, test conventions.
metadata:
  auto-invoke: 'TypeScript, types, interface, generics, hooks, useEffect, useMemo, naming, imports, constants, conventions'
---

# Code conventions

Complements `code-principles` (the _why_); this skill is the _how_ — concrete rules.

## TypeScript

- `type` by default; `interface` only for declaration merging/augmentation.
- Const-object unions instead of duplicated enum-like pairs:
  `const FIT = ['contain', 'cover', 'actual'] as const; type Fit = (typeof FIT)[number];`
- Discriminated unions + type guards for multi-state data; never parallel booleans.
  Narrow with `value is X` predicates, not casts.
- No `any`. Use `unknown` + a guard; if truly unavoidable, a one-line comment says why.
- `import type { ... }` for type-only imports.
- Prefer utility types (`Pick`, `Omit`, `Partial`, `Record`) over hand-rolled shapes.
- Keep object types flat (one level); name nested shapes instead of inlining them.

## React

- Named exports; no default exports (App Router `page.tsx`/`layout.tsx` are the exception).
- Named React imports (`import { useState } from 'react'`), never `import React`.
- Component internal order: hooks → refs → state → derived values → effects →
  handlers → early returns → render. No hooks after conditionals.
- `useState`: lazy initializer for non-trivial initial values.
- `useEffect`: always return cleanup when subscribing/listening; deps complete and
  minimal — extract stable callbacks rather than silencing the linter.
- Derive during render instead of mirroring props/state in extra state + effects.
- Don't over-memoize: `useMemo`/`useCallback` only for referential stability that
  something depends on (deps, memoized children, context values).
- Context: `createContext<T | null>(null)` + accessor hook that throws when missing
  (see `apps/web/src/components/ui/tabs/tabs.tsx`).
- No `index` as `key` in `.map()` over dynamic lists.

## Style

- Functions with > 2 parameters take an options object; boolean props/params are
  named explicitly (`defaultControlsVisible`, not `visible2`).
- Module-level constants in `UPPER_SNAKE_CASE`; extract magic numbers.
- Files `kebab-case`; components `UpperCamelCase`; one component folder =
  `component.tsx` + `component.test.tsx` (+ `index.ts` only as public entry).
- No barrel files except a package/folder public entry point.
- Imports ordered: external → internal alias (`@/`) → relative. No unused exports.

## Tests

- Colocated next to the source (`foo.test.tsx`), never in `__tests__/`.
- Query priority: `getByRole` (with `name`) > `getByLabelText` > `getByText` >
  `getByTestId` (last resort). Interact with `userEvent`; `fireEvent` only for
  low-level events RTL can't produce (pointer gestures, non-bubbling events).
- Async UI: `findBy*`/`waitFor`, never arbitrary sleeps. No snapshot tests.
- Test names state behavior + condition; assert observable behavior, not internals.
```

- [ ] **Step 2: Añadir la fila de routing en `AGENTS.md`**

En la tabla "Skills routing (knowledge)", debajo de la fila de `code-principles`, añadir:

```markdown
| TypeScript, types, hooks rules, naming, imports, constants, test conventions | `code-conventions` |
```

- [ ] **Step 3: Regenerar/validar la config de agentes**

Run: `pnpm run agents:setup && pnpm run agents:validate`
Expected: exit 0 (los symlinks de `.claude/skills` etc. apuntan a `skills/`, el nuevo directorio queda visible; el fichero de Copilot se regenera si aplica).

- [ ] **Step 4: Gate y commit**

Run: `pnpm run lint && pnpm run typecheck && pnpm run test`
Expected: verde (no hay cambios de código).

```bash
git add skills/code-conventions AGENTS.md .github/copilot-instructions.md
git commit -m "docs(skills): add code-conventions skill (TS, React hooks, style, tests)"
```

(Si `agents:setup` no tocó `.github/copilot-instructions.md`, omitirlo del add.)

---

### Task 2: B1 — Pan con Espacio + cursor grab (MediaLightbox, TDD)

Leer antes: `skills/accessibility/SKILL.md`, `skills/code-conventions/SKILL.md`.

**Files:**

- Modify: `packages/media-kit/src/media-lightbox/media-lightbox.tsx`
- Modify: `packages/media-kit/src/media-lightbox/media-lightbox.test.tsx` (añadir describe al final; NO tocar tests existentes)
- Modify: `packages/media-kit/src/styles.css`

**Interfaces:**

- Consumes: `zoomPan.panBy(dx, dy)` (v2, sin cambios — clampa en vivo contra los refs).
- Produces: atributo `data-space-pan` en el root del dialog mientras Espacio está mantenido (lo usa el CSS); ningún cambio de API pública.

**Diseño (cerrado en el spec):** el estado "Espacio pulsado" vive en `media-lightbox`. Mientras está pulsado: cursor `grab` sobre el viewport y **mover el puntero panea** (sin botón; con botón pulsado ya panea el drag de `useZoomPan` — el guard `event.buttons !== 0` evita duplicar el delta). `Espacio` sobre un elemento interactivo NO entra en modo pan (sigue activando el botón). El estado se libera en `keyup` y en `blur` de la ventana.

- [ ] **Step 1: Tests failing** — añadir al FINAL de `media-lightbox.test.tsx` (reutiliza el helper `mockSizes` ya definido a nivel de módulo):

```tsx
describe('MediaLightbox v2.1 — pan con Espacio (B1)', () => {
  function openWithOverflow() {
    render(
      <MediaLightbox open onClose={() => {}} label="Vista" fit="actual">
        <img src="/big.png" alt="grande" />
      </MediaLightbox>,
    );
    const dialog = screen.getByRole('dialog');
    const viewport = document.querySelector('.mk-lightbox__viewport') as HTMLElement;
    const media = document.querySelector('.mk-lightbox__media') as HTMLElement;
    mockSizes(viewport, 800, 600);
    mockSizes(media, 1600, 1200);
    return { dialog, media };
  }

  it('con Espacio mantenido, mover el puntero panea cuando hay desborde', () => {
    const { dialog, media } = openWithOverflow();
    fireEvent.keyDown(dialog, { key: ' ' });
    expect(dialog).toHaveAttribute('data-space-pan');
    fireEvent.pointerMove(dialog, { clientX: 400, clientY: 300 });
    fireEvent.pointerMove(dialog, { clientX: 360, clientY: 300 });
    expect(media.style.transform).toContain('translate(-40px, 0px)');
  });

  it('al soltar Espacio deja de panear y retira la affordance de cursor', () => {
    const { dialog, media } = openWithOverflow();
    fireEvent.keyDown(dialog, { key: ' ' });
    fireEvent.pointerMove(dialog, { clientX: 400, clientY: 300 });
    fireEvent.keyUp(dialog, { key: ' ' });
    expect(dialog).not.toHaveAttribute('data-space-pan');
    fireEvent.pointerMove(dialog, { clientX: 300, clientY: 300 });
    expect(media.style.transform).toContain('translate(0px, 0px)');
  });

  it('Espacio con el foco en un botón NO entra en modo pan (el botón se sigue activando)', () => {
    openWithOverflow();
    const close = screen.getByRole('button', { name: 'Close' });
    close.focus();
    fireEvent.keyDown(close, { key: ' ' });
    expect(screen.getByRole('dialog')).not.toHaveAttribute('data-space-pan');
  });
});
```

- [ ] **Step 2: Run para verificar RED**

Run: `pnpm --filter @nicobehm/media-kit exec vitest run src/media-lightbox/media-lightbox`
Expected: FAIL los 3 tests nuevos (`data-space-pan` inexistente); todos los previos PASS.

- [ ] **Step 3: Implementación** — en `media-lightbox.tsx`:

3a. Ampliar el import de tipos de React (línea 3-10) añadiendo `type PointerEvent`:

```tsx
import {
  useEffect,
  useRef,
  useState,
  type KeyboardEvent,
  type MouseEvent,
  type PointerEvent,
  type ReactNode,
} from 'react';
```

3b. Dentro de `MediaLightboxContent`, junto al resto de estado (tras `const [fit, setFit] = ...`):

```tsx
// B1: Espacio mantenido = modo pan (cursor grab + mover panea). El flag vive aquí
// (no en useZoomPan) porque es una convención de teclado del dialog, no un gesto.
const [spacePan, setSpacePan] = useState(false);
const lastPointRef = useRef<{ x: number; y: number } | null>(null);
```

3c. Effect de liberación al perder la ventana el foco (para no quedar "pegado"), junto a los otros effects:

```tsx
// Si la ventana pierde el foco con Espacio pulsado, el keyup nunca llega: liberar.
useEffect(() => {
  if (!spacePan) return;
  const release = () => {
    setSpacePan(false);
    lastPointRef.current = null;
  };
  window.addEventListener('blur', release);
  return () => window.removeEventListener('blur', release);
}, [spacePan]);
```

3d. En `onKeyDown`, DESPUÉS del guard `if (target.matches('input, textarea, select, [contenteditable="true"]')) return;` y ANTES del bloque `if (event.key === '+' ...)`:

```tsx
if (event.key === ' ') {
  // Espacio sobre un elemento interactivo lo activa (convención nativa): no panear.
  if (target.closest('button, a')) return;
  event.preventDefault();
  if (!event.repeat) setSpacePan(true);
  return;
}
```

3e. Handler nuevo `onKeyUp` (junto a `onKeyDown`):

```tsx
function onKeyUp(event: KeyboardEvent<HTMLDivElement>) {
  if (event.key === ' ') {
    setSpacePan(false);
    lastPointRef.current = null;
  }
}
```

3f. Handler nuevo `onRootPointerMove` que sustituye al `onPointerMove={autoHide.poke}` del JSX:

```tsx
function onRootPointerMove(event: PointerEvent<HTMLDivElement>) {
  autoHide.poke();
  // Con botón pulsado ya panea el drag de useZoomPan sobre el viewport; este camino
  // cubre "Espacio + mover" sin botón y evita duplicar el delta del arrastre.
  if (!spacePan || event.buttons !== 0) {
    lastPointRef.current = null;
    return;
  }
  const last = lastPointRef.current;
  if (last) zoomPan.panBy(event.clientX - last.x, event.clientY - last.y);
  lastPointRef.current = { x: event.clientX, y: event.clientY };
}
```

3g. En el JSX del root del dialog: añadir `data-space-pan={spacePan ? 'true' : undefined}`, `onKeyUp={onKeyUp}`, y cambiar `onPointerMove={autoHide.poke}` por `onPointerMove={onRootPointerMove}`:

```tsx
<div
  ref={dialogRef}
  role="dialog"
  aria-modal="true"
  aria-label={label}
  tabIndex={-1}
  className="mk-lightbox"
  data-fit={fit}
  data-space-pan={spacePan ? 'true' : undefined}
  onKeyDown={onKeyDown}
  onKeyUp={onKeyUp}
  onClick={onOverlayClick}
  onPointerMove={onRootPointerMove}
>
```

3h. En `styles.css`, tras el bloque `.mk-lightbox__media :is(img, video)` (línea ~183):

```css
/* B1: Espacio mantenido = modo pan. Solo affordance de cursor; el pan lo hace JS. */
.mk-lightbox[data-space-pan] .mk-lightbox__viewport {
  cursor: grab;
}

.mk-lightbox[data-space-pan] .mk-lightbox__viewport:active {
  cursor: grabbing;
}
```

- [ ] **Step 4: Run para verificar GREEN**

Run: `pnpm --filter @nicobehm/media-kit exec vitest run src/media-lightbox/media-lightbox`
Expected: PASS todos (v1 + v2 + v2.1).

- [ ] **Step 5: Gate y commit**

Run: `pnpm run format:fix && pnpm run lint && pnpm run typecheck && pnpm run test`
Expected: verde.

```bash
git add packages/media-kit/src/media-lightbox/media-lightbox.tsx packages/media-kit/src/media-lightbox/media-lightbox.test.tsx packages/media-kit/src/styles.css
git commit -m "feat(media-kit): space-held pan mode with grab cursor affordance"
```

---

### Task 3: B2a — LightboxHelp presentacional (TDD)

Leer antes: `skills/component-patterns/SKILL.md`, `skills/accessibility/SKILL.md`, `skills/code-conventions/SKILL.md`.

**Files:**

- Create: `packages/media-kit/src/media-lightbox/lightbox-help.tsx`
- Create: `packages/media-kit/src/media-lightbox/lightbox-help.test.tsx`

**Interfaces:**

- Consumes: tipo `MediaLightboxLabels` desde `./media-lightbox` — Task 4 le añade las claves nuevas. **Task 3 y Task 4 deben ejecutarse en orden y sus tests solo compilan juntos al cerrar Task 4** → para mantener cada task verde de forma independiente, Task 3 AÑADE también las claves al tipo y a `DEFAULT_LABELS` (cambio mínimo, sin comportamiento).
- Produces: `<LightboxHelp labels={MediaLightboxLabels} />` — panel `role="group"` etiquetado con `labels.helpTitle`, `tabIndex={-1}` y atributo `data-mk-help` (Task 4 lo usa para enfocar).

- [ ] **Step 1: Ampliar el tipo y defaults en `media-lightbox.tsx`** (sin comportamiento aún):

Añadir al final de `MediaLightboxLabels` (tras `close: string;`):

```tsx
/** aria-label del botón de ayuda. */
help: string;
/** Encabezado del panel de ayuda. */
helpTitle: string;
/** Descripciones del mapa de teclado (las teclas son literales, no traducibles). */
shortcutZoom: string;
shortcutReset: string;
shortcutPanKeys: string;
shortcutPanDrag: string;
shortcutFit: string;
shortcutFullscreen: string;
shortcutControls: string;
shortcutHelp: string;
shortcutClose: string;
```

Añadir a `DEFAULT_LABELS` (tras `close: 'Close',`):

```tsx
  help: 'Keyboard shortcuts',
  helpTitle: 'Keyboard shortcuts',
  shortcutZoom: 'Zoom in / out',
  shortcutReset: 'Reset view',
  shortcutPanKeys: 'Pan',
  shortcutPanDrag: 'Hold Space and drag to pan',
  shortcutFit: 'Cycle fit mode (toolbar)',
  shortcutFullscreen: 'Toggle fullscreen',
  shortcutControls: 'Show / hide controls',
  shortcutHelp: 'Toggle this help',
  shortcutClose: 'Close',
```

**Step 1b — completar el fixture v2:** el tipo ahora exige las claves nuevas, así que el objeto `const LABELS: MediaLightboxLabels = {...}` de `lightbox-controls.test.tsx` deja de compilar. AÑADIR al final de ese objeto exactamente las 11 claves anteriores (los mismos valores en inglés). PROHIBIDO tocar cualquier otra línea de ese fichero (aserciones intactas).

- [ ] **Step 2: Test failing** — `lightbox-help.test.tsx`:

```tsx
import { render, screen } from '@testing-library/react';
import { describe, expect, it } from 'vitest';
import { LightboxHelp } from './lightbox-help';
import type { MediaLightboxLabels } from './media-lightbox';

const LABELS: MediaLightboxLabels = {
  controls: 'Controls',
  zoomIn: 'Zoom in',
  zoomOut: 'Zoom out',
  zoomLevel: 'Zoom {percent}%',
  reset: 'Reset view',
  fit: 'Fit: {current}. Switch to {next}',
  fullscreen: 'Enter fullscreen',
  exitFullscreen: 'Exit fullscreen',
  hideControls: 'Hide controls',
  showControls: 'Show controls',
  close: 'Close',
  help: 'Atajos de teclado',
  helpTitle: 'Atajos de teclado',
  shortcutZoom: 'Acercar / alejar',
  shortcutReset: 'Restablecer vista',
  shortcutPanKeys: 'Desplazar',
  shortcutPanDrag: 'Mantén Espacio y arrastra para desplazar',
  shortcutFit: 'Cambiar modo de ajuste (toolbar)',
  shortcutFullscreen: 'Pantalla completa',
  shortcutControls: 'Mostrar / ocultar controles',
  shortcutHelp: 'Mostrar esta ayuda',
  shortcutClose: 'Cerrar',
};

describe('LightboxHelp', () => {
  it('es una región group etiquetada con helpTitle, enfocable programáticamente', () => {
    render(<LightboxHelp labels={LABELS} />);
    const panel = screen.getByRole('group', { name: 'Atajos de teclado' });
    expect(panel).toHaveAttribute('data-mk-help');
    expect(panel).toHaveAttribute('tabindex', '-1');
  });

  it('renderiza una fila por atajo con la descripción de labels', () => {
    render(<LightboxHelp labels={LABELS} />);
    for (const description of [
      'Acercar / alejar',
      'Restablecer vista',
      'Desplazar',
      'Mantén Espacio y arrastra para desplazar',
      'Cambiar modo de ajuste (toolbar)',
      'Pantalla completa',
      'Mostrar / ocultar controles',
      'Mostrar esta ayuda',
      'Cerrar',
    ]) {
      expect(screen.getByText(description)).toBeInTheDocument();
    }
  });

  it('las teclas son literales en <kbd>', () => {
    render(<LightboxHelp labels={LABELS} />);
    for (const key of ['Space', 'Esc', '?', 'f', 'c', '0']) {
      expect(screen.getByText(key, { selector: 'kbd' })).toBeInTheDocument();
    }
  });
});
```

- [ ] **Step 3: Run para verificar RED**

Run: `pnpm --filter @nicobehm/media-kit exec vitest run src/media-lightbox/lightbox-help`
Expected: FAIL — módulo inexistente.

- [ ] **Step 4: Implementación** — `lightbox-help.tsx`:

```tsx
'use client';

import type { MediaLightboxLabels } from './media-lightbox';

export type LightboxHelpProps = {
  labels: MediaLightboxLabels;
};

type ShortcutLabelKey =
  | 'shortcutZoom'
  | 'shortcutReset'
  | 'shortcutPanKeys'
  | 'shortcutPanDrag'
  | 'shortcutFit'
  | 'shortcutFullscreen'
  | 'shortcutControls'
  | 'shortcutHelp'
  | 'shortcutClose';

type HelpRow = { keys: string[]; label: ShortcutLabelKey };

// Teclas/gestos literales (no traducibles, por diseño); las descripciones salen de labels.
// La fila de fit muestra el glifo del botón de la toolbar: el ciclo de ajuste no tiene tecla.
const ROWS: HelpRow[] = [
  { keys: ['+', '−', 'wheel', 'double-click'], label: 'shortcutZoom' },
  { keys: ['0'], label: 'shortcutReset' },
  { keys: ['←', '↑', '→', '↓'], label: 'shortcutPanKeys' },
  { keys: ['Space'], label: 'shortcutPanDrag' },
  { keys: ['▣'], label: 'shortcutFit' },
  { keys: ['f'], label: 'shortcutFullscreen' },
  { keys: ['c'], label: 'shortcutControls' },
  { keys: ['?'], label: 'shortcutHelp' },
  { keys: ['Esc'], label: 'shortcutClose' },
];

/**
 * Panel presentacional del mapa de teclado. La visibilidad, el foco y la
 * precedencia de Escape los gobierna media-lightbox (estado helpOpen).
 * role="group" (no dialog): ya vivimos dentro de un dialog modal.
 */
export function LightboxHelp({ labels }: LightboxHelpProps) {
  return (
    <div
      role="group"
      aria-label={labels.helpTitle}
      className="mk-lightbox__help"
      data-mk-help=""
      tabIndex={-1}
    >
      <p className="mk-lightbox__help-title" aria-hidden="true">
        {labels.helpTitle}
      </p>
      <dl>
        {ROWS.map((row) => (
          <div key={row.label} className="mk-lightbox__help-row">
            <dt>
              {row.keys.map((key) => (
                <kbd key={key}>{key}</kbd>
              ))}
            </dt>
            <dd>{labels[row.label]}</dd>
          </div>
        ))}
      </dl>
    </div>
  );
}
```

- [ ] **Step 5: Run para verificar GREEN**

Run: `pnpm --filter @nicobehm/media-kit exec vitest run src/media-lightbox`
Expected: PASS (los tests de media-lightbox existentes siguen verdes: solo ampliamos el tipo con claves nuevas y sus defaults).

- [ ] **Step 6: Gate y commit**

Run: `pnpm run format:fix && pnpm run lint && pnpm run typecheck && pnpm run test`
Expected: verde.

```bash
git add packages/media-kit/src/media-lightbox/lightbox-help.tsx packages/media-kit/src/media-lightbox/lightbox-help.test.tsx packages/media-kit/src/media-lightbox/media-lightbox.tsx
git commit -m "feat(media-kit): LightboxHelp keyboard-map panel and new help labels"
```

---

### Task 4: B2b — Integración de la ayuda: botón `?`, tecla `?`, precedencia de Escape (TDD)

Leer antes: `skills/accessibility/SKILL.md` (foco en overlays), `skills/code-conventions/SKILL.md`.

**Files:**

- Modify: `packages/media-kit/src/media-lightbox/media-lightbox.tsx`
- Modify: `packages/media-kit/src/media-lightbox/media-lightbox.test.tsx` (añadir describe al final)
- Modify: `packages/media-kit/src/styles.css`

**Interfaces:**

- Consumes: `LightboxHelp` (Task 3) y sus labels; `[data-mk-help]` para el foco.
- Produces: botón `?` en el cluster de la esquina con `aria-expanded`; tecla `?`; orden de precedencia de `Escape`: fullscreen nativo → ayuda → cerrar lightbox.

- [ ] **Step 1: Tests failing** — añadir al FINAL de `media-lightbox.test.tsx`:

```tsx
describe('MediaLightbox v2.1 — ayuda de teclado (B2)', () => {
  it("la tecla '?' abre la ayuda, el foco entra al panel, y '?' la cierra", async () => {
    render(<Harness />);
    await userEvent.click(screen.getByRole('button', { name: 'Abrir' }));
    await userEvent.keyboard('?');
    const panel = screen.getByRole('group', { name: 'Keyboard shortcuts' });
    expect(panel).toHaveFocus();
    expect(screen.getByText('Hold Space and drag to pan')).toBeInTheDocument();
    await userEvent.keyboard('?');
    expect(screen.queryByRole('group', { name: 'Keyboard shortcuts' })).not.toBeInTheDocument();
  });

  it('el botón ? abre la ayuda y refleja aria-expanded', async () => {
    render(<Harness />);
    await userEvent.click(screen.getByRole('button', { name: 'Abrir' }));
    const helpButton = screen.getByRole('button', { name: 'Keyboard shortcuts' });
    expect(helpButton).toHaveAttribute('aria-expanded', 'false');
    await userEvent.click(helpButton);
    expect(helpButton).toHaveAttribute('aria-expanded', 'true');
    expect(screen.getByRole('group', { name: 'Keyboard shortcuts' })).toBeInTheDocument();
  });

  it('Escape cierra la ayuda ANTES que el lightbox y devuelve el foco al botón ?', async () => {
    render(<Harness />);
    await userEvent.click(screen.getByRole('button', { name: 'Abrir' }));
    const helpButton = screen.getByRole('button', { name: 'Keyboard shortcuts' });
    await userEvent.click(helpButton);
    await userEvent.keyboard('{Escape}');
    expect(screen.getByRole('dialog')).toBeInTheDocument();
    expect(screen.queryByRole('group', { name: 'Keyboard shortcuts' })).not.toBeInTheDocument();
    expect(helpButton).toHaveFocus();
    await userEvent.keyboard('{Escape}');
    expect(screen.queryByRole('dialog')).not.toBeInTheDocument();
  });
});
```

- [ ] **Step 2: Run para verificar RED**

Run: `pnpm --filter @nicobehm/media-kit exec vitest run src/media-lightbox/media-lightbox`
Expected: FAIL los 3 tests nuevos (no existe el botón ni la tecla `?`).

- [ ] **Step 3: Implementación** — en `media-lightbox.tsx`:

3a. Import del panel:

```tsx
import { LightboxHelp } from './lightbox-help';
```

3b. Estado y ref (junto a `spacePan`):

```tsx
const [helpOpen, setHelpOpen] = useState(false);
const helpButtonRef = useRef<HTMLButtonElement>(null);
```

3c. Handler de cierre (junto a `cycleFit`) y effect de foco de apertura:

```tsx
// La ayuda devuelve el foco a su disparador al cerrarse (patrón de overlay).
function closeHelp() {
  setHelpOpen(false);
  helpButtonRef.current?.focus();
}
```

```tsx
// Al abrir la ayuda, el foco entra al panel (closeHelp lo devuelve al botón).
useEffect(() => {
  if (helpOpen) dialogRef.current?.querySelector<HTMLElement>('[data-mk-help]')?.focus();
}, [helpOpen]);
```

3d. En `onKeyDown`, sustituir el bloque de `Escape` por (precedencia: fullscreen nativo → ayuda → cerrar):

```tsx
if (event.key === 'Escape') {
  // Con fullscreen nativo activo el navegador sale de fullscreen con este
  // mismo Escape; el lightbox no debe cerrarse a la vez.
  if (nativeFullscreenActive()) return;
  // Evita que otros handlers React por encima reaccionen al mismo Escape.
  event.stopPropagation();
  if (helpOpen) {
    closeHelp();
    return;
  }
  onClose();
  return;
}
```

3e. En la cadena de teclas (tras el branch de `'c'`), añadir:

```tsx
} else if (event.key === '?') {
  if (helpOpen) closeHelp();
  else setHelpOpen(true);
}
```

3f. En el JSX de `.mk-lightbox__corner`, añadir el botón de ayuda ANTES del toggle de controles:

```tsx
<button
  ref={helpButtonRef}
  type="button"
  className="mk-lightbox__help-toggle"
  aria-expanded={helpOpen}
  aria-label={labels.help}
  onClick={() => (helpOpen ? closeHelp() : setHelpOpen(true))}
>
  ?
</button>
```

3g. Renderizar el panel tras el `</div>` de `.mk-lightbox__corner`:

```tsx
{
  helpOpen ? <LightboxHelp labels={labels} /> : null;
}
```

3h. En `styles.css`: añadir `.mk-lightbox__help-toggle` a las DOS reglas compartidas de la familia de la esquina (base y hover):

```css
.mk-lightbox__close,
.mk-lightbox__controls-toggle,
.mk-lightbox__help-toggle {
  /* (regla existente, solo se amplía el selector) */
}

.mk-lightbox__close:hover:not(:disabled),
.mk-lightbox__controls-toggle:hover:not(:disabled),
.mk-lightbox__help-toggle:hover:not(:disabled) {
  background: rgb(255 255 255 / 0.12);
}
```

y lo mismo en la regla `:active` compartida. Añadir después los estilos del panel:

```css
/* B2: panel de ayuda de teclado, bajo el cluster de la esquina. */
.mk-lightbox__help {
  position: absolute;
  top: 4.5rem;
  right: 1rem;
  z-index: 1;
  max-width: min(22rem, calc(100vw - 2rem));
  padding: 1rem;
  border-radius: var(--mk-radius);
  background: var(--mk-control-bg);
  color: var(--mk-control-color);
  box-shadow: 0 0 0 1px var(--mk-handle-ring);
  font-size: 0.8125rem;
}

.mk-lightbox__help-title {
  margin: 0 0 0.75rem;
  font-size: 0.875rem;
  font-weight: 600;
}

.mk-lightbox__help dl {
  display: grid;
  grid-template-columns: auto 1fr;
  gap: 0.5rem 0.75rem;
  margin: 0;
}

.mk-lightbox__help-row {
  display: contents;
}

.mk-lightbox__help dt {
  display: flex;
  flex-wrap: wrap;
  gap: 0.25rem;
}

.mk-lightbox__help dd {
  margin: 0;
  align-self: center;
}

.mk-lightbox__help kbd {
  padding: 0.125rem 0.375rem;
  border-radius: 0.25rem;
  background: rgb(255 255 255 / 0.12);
  font: inherit;
  font-size: 0.75rem;
}
```

- [ ] **Step 4: Run para verificar GREEN**

Run: `pnpm --filter @nicobehm/media-kit exec vitest run src/media-lightbox`
Expected: PASS todos. Atención especial: los tests v2 de orden de foco/trap deben seguir pasando (el botón `?` entra en el ciclo de Tab como un focusable más del corner; ninguno de esos tests enumera los botones del corner de forma cerrada — si alguno fallara, PARAR y revisar con el orquestador, NO editar tests v2).

- [ ] **Step 5: Gate y commit**

Run: `pnpm run format:fix && pnpm run lint && pnpm run typecheck && pnpm run test`
Expected: verde.

```bash
git add packages/media-kit/src/media-lightbox/media-lightbox.tsx packages/media-kit/src/media-lightbox/media-lightbox.test.tsx packages/media-kit/src/styles.css
git commit -m "feat(media-kit): '?' help panel with Escape precedence over lightbox close"
```

---

### Task 5: B3 — Toggle ojo/ojo-tachado (SVG inline) + tooltips CSS (TDD)

Leer antes: `skills/accessibility/SKILL.md` (tooltip nunca única fuente de información).

**Files:**

- Modify: `packages/media-kit/src/media-lightbox/media-lightbox.tsx`
- Modify: `packages/media-kit/src/media-lightbox/media-lightbox.test.tsx` (añadir describe al final)
- Modify: `packages/media-kit/src/styles.css`

**Interfaces:**

- Consumes: labels v2 `hideControls`/`showControls` (reutilizadas) y `help` (Task 4).
- Produces: atributo `data-mk-tooltip` en los botones ojo y `?` (el CSS pinta el tooltip con `::after`); SVGs con `data-mk-icon="eye"` / `data-mk-icon="eye-off"`.

- [ ] **Step 1: Tests failing** — añadir al FINAL de `media-lightbox.test.tsx`:

```tsx
describe('MediaLightbox v2.1 — toggle ojo y tooltips (B3)', () => {
  it('el toggle muestra ojo/ojo-tachado y su tooltip sigue al aria-label', async () => {
    render(<Harness />);
    await userEvent.click(screen.getByRole('button', { name: 'Abrir' }));
    const toggle = screen.getByRole('button', { name: 'Hide controls' });
    expect(toggle.querySelector('svg[data-mk-icon="eye"]')).toBeInTheDocument();
    expect(toggle).toHaveAttribute('data-mk-tooltip', 'Hide controls');
    await userEvent.click(toggle);
    expect(toggle).toHaveAccessibleName('Show controls');
    expect(toggle.querySelector('svg[data-mk-icon="eye-off"]')).toBeInTheDocument();
    expect(toggle).toHaveAttribute('data-mk-tooltip', 'Show controls');
  });

  it('el botón de ayuda también lleva tooltip', async () => {
    render(<Harness />);
    await userEvent.click(screen.getByRole('button', { name: 'Abrir' }));
    expect(screen.getByRole('button', { name: 'Keyboard shortcuts' })).toHaveAttribute(
      'data-mk-tooltip',
      'Keyboard shortcuts',
    );
  });
});
```

- [ ] **Step 2: Run para verificar RED**

Run: `pnpm --filter @nicobehm/media-kit exec vitest run src/media-lightbox/media-lightbox`
Expected: FAIL los 2 tests nuevos (el toggle aún muestra `⋯` y no hay `data-mk-tooltip`).

- [ ] **Step 3: Implementación** — en `media-lightbox.tsx`:

3a. Constantes de iconos a nivel de módulo (tras `FIT_ORDER`; `currentColor`, 0 deps, deterministas):

```tsx
// Iconos SVG inline (trazo currentColor): deterministas y sin dependencias.
const EYE_ICON = (
  <svg
    data-mk-icon="eye"
    viewBox="0 0 24 24"
    width="18"
    height="18"
    fill="none"
    stroke="currentColor"
    strokeWidth="2"
    strokeLinecap="round"
    strokeLinejoin="round"
    aria-hidden="true"
  >
    <path d="M1 12s4-7 11-7 11 7 11 7-4 7-11 7-11-7-11-7Z" />
    <circle cx="12" cy="12" r="3" />
  </svg>
);

const EYE_OFF_ICON = (
  <svg
    data-mk-icon="eye-off"
    viewBox="0 0 24 24"
    width="18"
    height="18"
    fill="none"
    stroke="currentColor"
    strokeWidth="2"
    strokeLinecap="round"
    strokeLinejoin="round"
    aria-hidden="true"
  >
    <path d="M17.94 17.94A10.5 10.5 0 0 1 12 19c-7 0-11-7-11-7a19.9 19.9 0 0 1 5.06-5.94" />
    <path d="M9.9 4.24A10.4 10.4 0 0 1 12 5c7 0 11 7 11 7a19.9 19.9 0 0 1-3.22 4.19" />
    <path d="M14.12 14.12a3 3 0 1 1-4.24-4.24" />
    <line x1="1" y1="1" x2="23" y2="23" />
  </svg>
);
```

3b. En el botón toggle (`.mk-lightbox__controls-toggle`): sustituir el hijo `⋯` por `{autoHide.visible ? EYE_ICON : EYE_OFF_ICON}` y añadir `data-mk-tooltip={autoHide.visible ? labels.hideControls : labels.showControls}`.

3c. En el botón `?` (Task 4): añadir `data-mk-tooltip={labels.help}`.

3d. En `styles.css`, tras las reglas del corner:

```css
/* B3: tooltip ligero CSS. Complementa (nunca sustituye) al aria-label. */
.mk-lightbox__corner button[data-mk-tooltip] {
  position: relative;
}

.mk-lightbox__corner button[data-mk-tooltip]::after {
  content: attr(data-mk-tooltip);
  position: absolute;
  top: calc(100% + 0.5rem);
  right: 0;
  padding: 0.25rem 0.5rem;
  border-radius: 0.375rem;
  background: var(--mk-control-bg);
  color: var(--mk-control-color);
  box-shadow: 0 0 0 1px var(--mk-handle-ring);
  font-size: 0.75rem;
  white-space: nowrap;
  pointer-events: none;
  opacity: 0;
}

.mk-lightbox__corner button[data-mk-tooltip]:hover::after,
.mk-lightbox__corner button[data-mk-tooltip]:focus-visible::after {
  opacity: 1;
}
```

(Sin `transition`: aparece/desaparece en seco — no hay nada que silenciar en reduced-motion.)

- [ ] **Step 4: Run para verificar GREEN**

Run: `pnpm --filter @nicobehm/media-kit exec vitest run src/media-lightbox`
Expected: PASS todos. Los tests v2 del toggle usan el aria-label (no el glifo `⋯`), así que no se rompen.

- [ ] **Step 5: Gate y commit**

Run: `pnpm run format:fix && pnpm run lint && pnpm run typecheck && pnpm run test`
Expected: verde.

```bash
git add packages/media-kit/src/media-lightbox/media-lightbox.tsx packages/media-kit/src/media-lightbox/media-lightbox.test.tsx packages/media-kit/src/styles.css
git commit -m "feat(media-kit): eye/eye-off controls toggle with CSS tooltips"
```

---

### Task 6: B4a — Generar y optimizar el retrato (ORQUESTADOR, no subagente)

Esta task la ejecuta el **orquestador** en la sesión principal (usa MCP de Magnific, que puede no estar disponible en subagentes). Leer antes: `skills/performance/SKILL.md`.

**Files:**

- Create: `apps/web/public/demo/portrait.webp`

**Interfaces:**

- Consumes: MCP `mcp__claude_ai_Magnific__images_generate` + `creations_wait`.
- Produces: `/demo/portrait.webp` — retrato dramático apaisado 16:9, ~1600px de ancho, ≤ 200 kB. Task 7 lo referencia con `width={1600} height={900}`.

- [ ] **Step 1: Generar el retrato con Magnific**

Tool: `images_generate` con prompt tipo: _"Dramatic cinematic portrait of a weathered elderly sailor with deep expressive wrinkles and piercing eyes, moody Rembrandt lighting, dark background, shallow depth of field, photorealistic, high detail"_ — aspect ratio 16:9 (apaisado). Esperar con `creations_wait` y obtener la URL final del asset.

- [ ] **Step 2: Descargar y optimizar a WebP ≤ 200 kB**

```bash
SCRATCH=<scratchpad>/portrait-src.png
curl -sL "<URL_DEL_ASSET>" -o "$SCRATCH"
cwebp -q 80 -resize 1600 0 "$SCRATCH" -o apps/web/public/demo/portrait.webp
ls -la apps/web/public/demo/portrait.webp
```

Expected: fichero ≤ 200 kB (204800 bytes). Si pesa más, bajar a `-q 70` (y si sigue, `-q 60`). Verificar dimensiones: `webpinfo apps/web/public/demo/portrait.webp | grep -i 'width\|height'` (o `file`): ancho 1600, alto ~900.

- [ ] **Step 3: Commit (solo el asset, por ruta explícita)**

```bash
git add apps/web/public/demo/portrait.webp
git commit -m "feat(web): add AI-generated portrait demo asset (webp, 16:9, <200kB)"
```

---

### Task 7: B4b — Demo color/B-N con fullscreen nativo del slider (TDD)

Leer antes: `skills/component-patterns/SKILL.md`, `skills/nextjs-static-dual/SKILL.md` (componente cliente), `skills/tailwind-tokens/SKILL.md`, `skills/code-conventions/SKILL.md`.

**Files:**

- Create: `apps/web/src/components/showcase/portrait-compare-demo.tsx`
- Create: `apps/web/src/components/showcase/portrait-compare-demo.test.tsx`
- Modify: `apps/web/src/components/showcase/media-kit-demo.tsx` (integrar el demo)

**Interfaces:**

- Consumes: `CompareSlider` (API v2 sin cambios) y `/demo/portrait.webp` (Task 6); `Button` de la app.
- Produces: `<PortraitCompareDemo />` exportado; fullscreen NATIVO sobre el contenedor del slider (no el lightbox de zoom — conflicto de gestos slider↔pan, cerrado en spec).

**Diseño:** un único bitmap; el lado "antes" (izquierda) deriva el B-N con `filter: grayscale(1)` — narrativa "colorizar" (izq B-N → der color). `alt` significativo en el "antes", `alt=""` en el "después" (el componente ya lo marca `aria-hidden`). El botón de fullscreen es un overlay HERMANO del slider (no descendiente de `.mk-compare`: sus clicks no mueven el divisor) y solo se renderiza si `document.fullscreenEnabled` (estado seteado en effect — SSR-safe).

- [ ] **Step 1: Tests failing** — `portrait-compare-demo.test.tsx`:

```tsx
import { render, screen, waitFor } from '@testing-library/react';
import userEvent from '@testing-library/user-event';
import { afterEach, describe, expect, it, vi } from 'vitest';
import { PortraitCompareDemo } from './portrait-compare-demo';

function defineFullscreen(enabled: boolean) {
  Object.defineProperty(document, 'fullscreenEnabled', {
    value: enabled,
    configurable: true,
  });
}

describe('PortraitCompareDemo', () => {
  afterEach(() => {
    defineFullscreen(false);
    vi.restoreAllMocks();
  });

  it('deriva el B-N del mismo bitmap con filter grayscale en el lado "antes"', () => {
    render(<PortraitCompareDemo />);
    const before = screen.getByAltText('Retrato en blanco y negro');
    expect(before).toHaveAttribute('src', '/demo/portrait.webp');
    expect(before.style.filter).toBe('grayscale(1)');
    expect(before).toHaveAttribute('width', '1600');
    expect(before).toHaveAttribute('height', '900');
  });

  it('sin Fullscreen API no renderiza el botón de pantalla completa', () => {
    defineFullscreen(false);
    render(<PortraitCompareDemo />);
    expect(
      screen.queryByRole('button', { name: 'Ver a pantalla completa' }),
    ).not.toBeInTheDocument();
  });

  it('el botón pide fullscreen nativo sobre el contenedor del slider', async () => {
    defineFullscreen(true);
    const request = vi.fn(() => Promise.resolve());
    Element.prototype.requestFullscreen = request;
    render(<PortraitCompareDemo />);
    const button = await screen.findByRole('button', { name: 'Ver a pantalla completa' });
    await userEvent.click(button);
    await waitFor(() => expect(request).toHaveBeenCalledOnce());
  });
});
```

- [ ] **Step 2: Run para verificar RED**

Run: `pnpm --filter web exec vitest run src/components/showcase/portrait-compare-demo`
Expected: FAIL — módulo inexistente.

- [ ] **Step 3: Implementación** — `portrait-compare-demo.tsx`:

```tsx
'use client';

import { useEffect, useRef, useState } from 'react';
import { CompareSlider } from '@nicobehm/media-kit';
import { Button } from '@/components/ui/button';

/**
 * Demo color/B-N: un único bitmap; el lado "antes" deriva el blanco y negro con
 * filter grayscale (cero peso extra). Fullscreen NATIVO sobre el contenedor del
 * slider — no el lightbox de zoom, cuyos gestos de pan chocan con el arrastre
 * del divisor.
 */
export function PortraitCompareDemo() {
  const containerRef = useRef<HTMLDivElement>(null);
  const [fullscreenSupported, setFullscreenSupported] = useState(false);
  const [fullscreenActive, setFullscreenActive] = useState(false);

  useEffect(() => {
    setFullscreenSupported(Boolean(document.fullscreenEnabled));
    const sync = () => setFullscreenActive(Boolean(document.fullscreenElement));
    document.addEventListener('fullscreenchange', sync);
    return () => document.removeEventListener('fullscreenchange', sync);
  }, []);

  function toggleFullscreen() {
    if (document.fullscreenElement) {
      void document.exitFullscreen().catch(() => {});
    } else {
      void containerRef.current?.requestFullscreen().catch(() => {});
    }
  }

  return (
    <figure className="flex flex-col gap-2">
      <div
        ref={containerRef}
        className="relative [&:fullscreen]:flex [&:fullscreen]:items-center [&:fullscreen]:justify-center [&:fullscreen]:bg-bg [&:fullscreen]:p-8"
      >
        <div className="w-full">
          <CompareSlider
            before={
              <img
                src="/demo/portrait.webp"
                alt="Retrato en blanco y negro"
                width={1600}
                height={900}
                loading="lazy"
                style={{ filter: 'grayscale(1)' }}
              />
            }
            after={
              <img src="/demo/portrait.webp" alt="" width={1600} height={900} loading="lazy" />
            }
            label="Comparar blanco y negro con color"
          />
        </div>
        {fullscreenSupported ? (
          <Button
            variant="secondary"
            size="sm"
            className="absolute right-3 top-3"
            onClick={toggleFullscreen}
          >
            {fullscreenActive ? 'Salir de pantalla completa' : 'Ver a pantalla completa'}
          </Button>
        ) : null}
      </div>
      <figcaption className="text-sm text-fg-muted">
        Colorización simulada — un único bitmap; el B-N se deriva con{' '}
        <code>filter: grayscale(1)</code>. Retrato generado con IA.
      </figcaption>
    </figure>
  );
}
```

Nota sobre `<img>` nativo: correcto aquí — export estático sin optimizador de servidor (skill `performance`); el asset ya viene pre-optimizado con dimensiones explícitas.

- [ ] **Step 4: Integrar en `media-kit-demo.tsx`** — añadir el import y renderizar el demo tras el grid de sliders SVG (antes del botón del lightbox):

```tsx
import { PortraitCompareDemo } from './portrait-compare-demo';
```

```tsx
<PortraitCompareDemo />
```

Además, ampliar el objeto `labels` del `MediaLightbox` del demo con las claves nuevas en español (ejercita la i18n del panel):

```tsx
  help: 'Atajos de teclado',
  helpTitle: 'Atajos de teclado',
  shortcutZoom: 'Acercar / alejar',
  shortcutReset: 'Restablecer vista',
  shortcutPanKeys: 'Desplazar',
  shortcutPanDrag: 'Mantén Espacio y arrastra para desplazar',
  shortcutFit: 'Cambiar modo de ajuste (toolbar)',
  shortcutFullscreen: 'Pantalla completa',
  shortcutControls: 'Mostrar / ocultar controles',
  shortcutHelp: 'Mostrar esta ayuda',
  shortcutClose: 'Cerrar',
```

- [ ] **Step 5: Run para verificar GREEN**

Run: `pnpm --filter web exec vitest run src/components/showcase`
Expected: PASS (3 tests nuevos).

- [ ] **Step 6: Gate y commit**

Run: `pnpm run format:fix && pnpm run lint && pnpm run typecheck && pnpm run test`
Expected: verde.

```bash
git add apps/web/src/components/showcase/portrait-compare-demo.tsx apps/web/src/components/showcase/portrait-compare-demo.test.tsx apps/web/src/components/showcase/media-kit-demo.tsx
git commit -m "feat(web): color/B&W portrait compare demo with native slider fullscreen"
```

---

### Task 8: C1 — Tabs sin desmontar el panel (`hidden`) (TDD)

Leer antes: `skills/accessibility/SKILL.md`, `skills/component-patterns/SKILL.md`.

**Files:**

- Modify: `apps/web/src/components/ui/tabs/tabs.tsx`
- Modify: `apps/web/src/components/ui/tabs/tabs.test.tsx`

**Interfaces:**

- Consumes: nada nuevo.
- Produces: `TabPanel` renderiza SIEMPRE su div; el inactivo lleva `hidden` (fuera del árbol de a11y y del flujo visual) → cero layout-shift al cambiar de tab.

**Cambio de contrato autorizado por el spec C1:** el test v1 «marca la pestaña activa y muestra solo su panel» asertaba `not.toBeInTheDocument()` sobre el panel inactivo; el spec ahora exige que SÍ esté en el DOM (oculto). Se cambia SOLO esa aserción a `not.toBeVisible()` — la garantía de usuario (no se ve, no es accesible) se conserva y se refuerza con tests nuevos.

- [ ] **Step 1: Tests** — en `tabs.test.tsx`, cambiar UNA aserción del primer test:

```tsx
// antes:
expect(screen.queryByText('panel-api')).not.toBeInTheDocument();
// después:
expect(screen.getByText('panel-api')).not.toBeVisible();
```

y añadir al FINAL del fichero:

```tsx
describe('Tabs v2.1 — paneles persistentes (C1)', () => {
  it('mantiene ambos paneles montados; el inactivo con hidden, fuera del árbol de a11y', () => {
    renderTabs();
    expect(screen.getByText('panel-preview')).toBeVisible();
    expect(screen.getByText('panel-api')).not.toBeVisible();
    // Solo el panel activo expone role=tabpanel (hidden lo saca del árbol de a11y).
    expect(screen.getAllByRole('tabpanel')).toHaveLength(1);
  });

  it('cambiar de tab alterna hidden sin desmontar (cero remount → cero layout-shift)', async () => {
    renderTabs();
    const api = screen.getByText('panel-api');
    await userEvent.click(screen.getByRole('tab', { name: 'API' }));
    // Mismo nodo de antes del click: no se desmontó.
    expect(api).toBeVisible();
    expect(screen.getByText('panel-preview')).not.toBeVisible();
  });
});
```

- [ ] **Step 2: Run para verificar RED**

Run: `pnpm --filter web exec vitest run src/components/ui/tabs`
Expected: FAIL — el panel inactivo no está en el DOM.

- [ ] **Step 3: Implementación** — en `tabs.tsx`, sustituir `TabPanel` completo:

```tsx
export function TabPanel({ value, children }: { value: string; children: ReactNode }) {
  const { active, baseId } = useTabs('TabPanel');
  const selected = active === value;
  // El panel inactivo queda montado y oculto con `hidden` (no se desmonta): cero
  // layout-shift al cambiar de tab, y `hidden` lo saca del árbol de accesibilidad.
  return (
    <div
      role="tabpanel"
      id={`${baseId}-panel-${value}`}
      aria-labelledby={`${baseId}-tab-${value}`}
      tabIndex={0}
      hidden={!selected}
      className="mt-3 focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-ring"
    >
      {children}
    </div>
  );
}
```

- [ ] **Step 4: Run para verificar GREEN**

Run: `pnpm --filter web exec vitest run src/components/ui/tabs`
Expected: PASS todos (v1 con la aserción actualizada + C1).

- [ ] **Step 5: Gate y commit**

Run: `pnpm run format:fix && pnpm run lint && pnpm run typecheck && pnpm run test`
Expected: verde.

```bash
git add apps/web/src/components/ui/tabs/tabs.tsx apps/web/src/components/ui/tabs/tabs.test.tsx
git commit -m "fix(web): keep inactive TabPanel mounted with hidden to avoid layout shift"
```

---

### Task 9: C2a — Showcase: estructura, intros e índice lateral

Leer antes: `skills/tailwind-tokens/SKILL.md`, `skills/accessibility/SKILL.md` (landmarks, headings), `skills/code-conventions/SKILL.md`. Espaciado por escala estándar de Tailwind (sin tokens propios, AMENDED F1). Página estática sin lógica: sin test unitario nuevo; la verificación es lint/typecheck/build + verificación visual del cierre (Task 12).

**Files:**

- Modify: `apps/web/src/app/showcase/page.tsx`

**Interfaces:**

- Consumes: primitivas UI existentes + `MediaKitDemo` (que ya integra `PortraitCompareDemo`, Task 7).
- Produces: página con índice lateral (`nav` etiquetado + anchors), `Section` con `id`, título y descripción. Task 10 añade contenido a estas secciones.

- [ ] **Step 1: Reescribir la estructura de `page.tsx`** — sustituir el componente `Section` y el layout (las secciones internas conservan su contenido actual, solo cambian de wrapper):

```tsx
const TOC = [
  { id: 'button', label: 'Button' },
  { id: 'badge', label: 'Badge' },
  { id: 'card', label: 'Card' },
  { id: 'form', label: 'Formulario' },
  { id: 'tabs', label: 'Tabs' },
  { id: 'media-kit', label: 'Media kit' },
] as const;

function Section({
  id,
  title,
  description,
  children,
}: {
  id: string;
  title: string;
  description: string;
  children: React.ReactNode;
}) {
  return (
    <section id={id} aria-labelledby={`${id}-title`} className="flex scroll-mt-24 flex-col gap-4">
      <div className="flex flex-col gap-1 border-b border-border pb-3">
        <h2 id={`${id}-title`} className="text-xl font-semibold">
          {title}
        </h2>
        <p className="text-sm text-fg-muted">{description}</p>
      </div>
      {children}
    </section>
  );
}
```

y el layout raíz:

```tsx
export default function ShowcasePage() {
  return (
    <main className="mx-auto max-w-6xl px-6 py-12 lg:grid lg:grid-cols-[10rem_minmax(0,1fr)] lg:items-start lg:gap-12">
      <nav aria-label="Índice del showcase" className="hidden lg:block">
        <ul className="sticky top-12 flex flex-col gap-2 text-sm">
          {TOC.map((entry) => (
            <li key={entry.id}>
              <a
                href={`#${entry.id}`}
                className="text-fg-muted transition-colors hover:text-fg focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-ring"
              >
                {entry.label}
              </a>
            </li>
          ))}
        </ul>
      </nav>
      <div className="flex flex-col gap-14">
        <header className="flex items-start justify-between gap-4">
          <div>
            <h1 className="text-3xl font-bold">UI primitives</h1>
            <p className="mt-1 text-fg-muted">
              Kitchen sink del design system — todas las variantes y estados, en ambos temas.
            </p>
          </div>
          <ThemeSwitcher />
        </header>
        {/* ...secciones... */}
      </div>
    </main>
  );
}
```

- [ ] **Step 2: Migrar cada sección al nuevo `Section`** con estos `id` + descripciones (contenido interior intacto):

- `id="button"` · título `Button` · descripción: `Acción principal del sistema: 4 variantes, 3 tamaños y estados deshabilitados.`
- `id="badge"` · título `Badge` · descripción: `Etiqueta compacta de estado o metadatos; el color nunca es el único canal.`
- `id="card"` · título `Card` · descripción: `Contenedor componible (header, título, contenido) para casos de estudio y resultados.`
- `id="form"` · título `Formulario (Field + Input + Select)` · descripción: `Campos accesibles: label, hint y error enlazados con aria-describedby.`
- `id="tabs"` · título `Tabs` · descripción: `Pestañas APG con roving tabindex; los paneles persisten montados (sin layout-shift).`
- `id="media-kit"` · título `@nicobehm/media-kit` · descripción: `Componentes media del paquete propio: CompareSlider (drag/hover, color/B-N) y MediaLightbox con zoom.`

- [ ] **Step 3: Verificación**

Run: `pnpm run format:fix && pnpm run lint && pnpm run typecheck && pnpm run test && pnpm --filter web build`
Expected: verde; el build estático genera `/showcase`.

- [ ] **Step 4: Commit**

```bash
git add apps/web/src/app/showcase/page.tsx
git commit -m "feat(web): showcase structure with section intros and sticky side index"
```

---

### Task 10: C2b — Showcase: estados/variantes que faltan + pulido `:active`

Leer antes: `skills/component-patterns/SKILL.md` (estados explícitos), `skills/tailwind-tokens/SKILL.md` (solo tokens semánticos). Cambios de clases/JSX estático: sin test unitario nuevo; verificación visual en Task 12.

**Files:**

- Modify: `apps/web/src/components/ui/button/button.tsx`
- Modify: `apps/web/src/app/showcase/page.tsx`

**Interfaces:**

- Consumes: `Section` de Task 9.
- Produces: feedback `:active` en Button; ejemplos de disabled en formulario; estados vacío/error en Card.

- [ ] **Step 1: `:active` en Button** — en la cadena base del `cva` de `button.tsx`, añadir `active:translate-y-px` (feedback físico sin color — válido en ambos temas, sin transición de transform):

```tsx
const buttonVariants = cva(
  'inline-flex items-center justify-center gap-2 rounded-control font-medium transition-colors focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-ring active:translate-y-px disabled:pointer-events-none disabled:opacity-50',
  // ...variants sin cambios
);
```

- [ ] **Step 2: Completar estados en el showcase** — en `page.tsx`:

2a. Sección Card — añadir al grid dos cards de estado (patrón de `component-patterns`: copy humano + affordance):

```tsx
<Card>
  <CardHeader>
    <CardTitle>Estado vacío</CardTitle>
  </CardHeader>
  <CardContent>
    <div className="flex flex-col items-start gap-3">
      <p>Sin resultados todavía. Genera tu primera imagen para verla aquí.</p>
      <Button size="sm" variant="secondary">
        Crear imagen
      </Button>
    </div>
  </CardContent>
</Card>
<Card>
  <CardHeader>
    <CardTitle>Estado error</CardTitle>
  </CardHeader>
  <CardContent>
    <div className="flex flex-col items-start gap-3">
      <p className="text-danger">No se pudo cargar el resultado. Comprueba tu conexión.</p>
      <Button size="sm" variant="secondary">
        Reintentar
      </Button>
    </div>
  </CardContent>
</Card>
```

2b. Sección Formulario — añadir tras el campo con error:

```tsx
<Field label="Deshabilitado" htmlFor="demo-disabled" hint="Campo no editable en este plan.">
  <Input id="demo-disabled" disabled defaultValue="Solo lectura" />
</Field>
<Field label="Select deshabilitado" htmlFor="demo-select-disabled">
  <Select
    id="demo-select-disabled"
    disabled
    options={[{ value: '1_1', label: '1:1' }]}
  />
</Field>
```

Si `Input`/`Select` no muestran estado disabled distinguible, añadir a sus clases base: `disabled:cursor-not-allowed disabled:opacity-50` (en `input.tsx` y `select.tsx` — solo utilidades de opacidad, sin colores nuevos).

- [ ] **Step 3: Verificación**

Run: `pnpm run format:fix && pnpm run lint && pnpm run typecheck && pnpm run test`
Expected: verde.

- [ ] **Step 4: Commit**

```bash
git add apps/web/src/components/ui/button/button.tsx apps/web/src/components/ui/input/input.tsx apps/web/src/components/ui/select/select.tsx apps/web/src/app/showcase/page.tsx
git commit -m "feat(web): showcase state coverage (empty/error cards, disabled fields) and button active feedback"
```

(Omitir del add los ficheros que no hayan cambiado.)

---

### Task 11: Documentación del paquete + bump 0.3.0

**Files:**

- Modify: `packages/media-kit/README.md`
- Modify: `packages/media-kit/CHANGELOG.md`
- Modify: `packages/media-kit/package.json`

**Interfaces:**

- Consumes: comportamiento final de Tasks 2–5.
- Produces: docs públicas al día; versión `0.3.0`.

- [ ] **Step 1: `package.json`** — `"version": "0.2.0"` → `"version": "0.3.0"`.

- [ ] **Step 2: README** —

2a. Tabla `MediaLightboxLabels`: añadir las 11 claves nuevas con sus defaults (de Task 3) y su uso (`help` = aria-label/tooltip del botón `?`; `helpTitle` = encabezado del panel; `shortcut*` = descripciones de las filas del panel).

2b. Tabla "Keyboard map": añadir filas:

```markdown
| `Space` (hold) | While held, moving the pointer pans (grab cursor). With a button pressed, regular drag-panning applies. Over a button/link, `Space` activates it instead. | A no-op when the content doesn't overflow the viewport. |
| `?` | Toggles the keyboard-shortcuts help panel. | Focus moves into the panel on open, back to the `?` button on close. |
```

y actualizar la fila de `Escape`: precedencia fullscreen nativo → panel de ayuda → cerrar dialog.

2c. Actualizar la descripción del toggle de visibilidad: de `⋯` a icono ojo/ojo-tachado con tooltip (el `aria-label` sigue siendo `hideControls`/`showControls`). Mencionar el tooltip CSS de los botones `?` y ojo.

2d. Añadir receta del ejemplo color/B-N en la sección de recipes del README:

````markdown
### Color vs. black & white from a single bitmap

Derive the "before" side with a CSS filter — one asset, no double weight:

```tsx
<CompareSlider
  before={
    <img src="/portrait.webp" alt="Black and white portrait" style={{ filter: 'grayscale(1)' }} />
  }
  after={<img src="/portrait.webp" alt="" />}
  label="Compare black & white with color"
/>
```
````

- [ ] **Step 3: CHANGELOG** — nueva entrada encima de `0.2.0`:

```markdown
## 0.3.0 — 2026-07-14

### Added

- **MediaLightbox:** hold `Space` to pan with a `grab` cursor (pointer move pans while held;
  `Space` still activates focused buttons); `?` key and corner button toggle a keyboard-shortcuts
  help panel (`Escape` closes the help before the lightbox; focus is managed in/out of the panel);
  new `labels` keys `help`, `helpTitle` and `shortcut*` for full i18n of the panel.
- **MediaLightbox:** the controls-visibility toggle now renders an inline eye / eye-off SVG
  (instead of `⋯`) and both corner buttons (`?`, eye) show a CSS-only tooltip mirroring their
  `aria-label`.

No breaking changes: all new labels default to English; no new props; v2 behavior unchanged.
```

- [ ] **Step 4: Gate y commit**

Run: `pnpm run format:fix && pnpm run lint && pnpm run typecheck && pnpm run test`
Expected: verde.

```bash
git add packages/media-kit/README.md packages/media-kit/CHANGELOG.md packages/media-kit/package.json
git commit -m "docs(media-kit): document space-pan, help panel and eye toggle; bump to 0.3.0"
```

---

### Task 12: Cierre de Fase 2.6

La ejecuta el orquestador (usa agentes de review y Playwright MCP).

- [ ] **Step 1: Gate completo + grep de colores**

```bash
pnpm run lint && pnpm run typecheck && pnpm run test
git diff main --name-only
grep -rnE '#[0-9a-fA-F]{3,8}\b|rgb\(|hsl\(' apps/web/src packages/media-kit/src \
  --include='*.tsx' --include='*.ts' | grep -v node_modules
```

Expected: gate verde; el grep solo devuelve coincidencias en ficheros permitidos (ninguna en `.tsx`/`.ts` — los SVG del ojo usan `currentColor`).

- [ ] **Step 2: Builds duales con /showcase estática**

```bash
pnpm run build && ls apps/web/out/showcase*
NEXT_OUTPUT_MODE=node pnpm run build
```

Expected: ambos builds verdes; `out/showcase.html` (o `showcase/index.html`) presente; revisar tamaños de ruta impresos (First Load JS ≤ 130 kB gzip por ruta).

- [ ] **Step 3: Verificación visual en vivo (Playwright MCP, ambos temas)**

Con `pnpm run dev` corriendo, en `/showcase` y en AMBOS temas (toggle con ThemeSwitcher):

- Índice lateral: anchors navegan; focus ring visible.
- Tabs: cambiar de tab NO produce layout-shift (medir bounding box del contenedor antes/después).
- Retrato: se ve el B-N a la izquierda, color a la derecha; fullscreen nativo del slider entra/sale; el botón no mueve el divisor.
- Lightbox — teclado completo: `+` `−` `0` `f` `c` `?` flechas `Espacio` (cursor grab + pan con desborde) `Escape` (con ayuda abierta cierra la ayuda primero; con fullscreen nativo sale del fullscreen primero).
- Ojo/ojo-tachado alterna; tooltips visibles en hover y focus; help box legible.
- `prefers-reduced-motion`: sin transiciones de transform.

- [ ] **Step 4: Design review + code review**

- Dispatch agente `design-reviewer` sobre `/showcase` (ambos temas).
- Code review de la rama completa (`git diff main`) con la skill `superpowers:requesting-code-review`.
- Triaje de hallazgos: fix inmediato si es de la fase; anotar como backlog si no.

- [ ] **Step 5: Roadmap + merge FF**

- Marcar F2.6 como hecha en `docs/superpowers/plans/2026-07-10-portfolio-roadmap.md` + commit `docs: mark phase 2.6 as done in roadmap`.
- Merge con la skill `superpowers:finishing-a-development-branch` (merge FF a `main` + push, patrón de F2.5).

---

## Backlog explícitamente diferido (anotado, NO en esta fase)

- Selector de estilos con URL, selector auto-filtrable del showcase, i18n es/en, contenido/página de CV → **Fase 3**.
- Zoom en CompareSlider / compare dentro del lightbox; galería multi-imagen → v3.
- Decisión del usuario pendiente: PII de los PDFs en `apps/web/content/cv/` (NO trackear).
