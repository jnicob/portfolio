# SDD progress ledger — Fase 0 (plan: docs/superpowers/plans/2026-07-10-phase-0-foundations.md)

# Rama: feature/phase-0-foundations

Task 1: complete (commits 8a29f19..cde1edd, review clean tras adjudicación: engines>=20 aceptado con enmienda de plan; pendiente usuario: upgrade Node 22)
Task 2: complete (commits 7867090..a9dbc9b + fix 325b7d0, review clean tras fix; Minor aceptado: next-env.d.ts committeado)
Task 3: complete (commits 325b7d0..02014f0 + fixes c6be37c, wording 1f34c0e; review clean)
Task 4: complete (commit f67f554, review clean; Minors solo de report, sin acción)
Task 5: complete (commit 5e9b857, review clean sin hallazgos)
Task 6: complete (commit 781a5d2, review clean sin hallazgos)
Task 7: complete (commit 44f5501, review clean sin hallazgos; symlinks + copilot generado OK)
Task 8: complete (commit 14582ba, review clean; CI Node22+lockfile verificado compatible)
Task 9: complete (commit a5af315, review clean)
Task 9: complete (commit a5af315 + fix final f61338e, review clean)
FASE 0: COMPLETA — final review 'Ready to merge' (13+1 commits en feature/phase-0-foundations). Minor aceptado sin fix: stale out/ tras build node (cosmético).

# === FASE 1 (plan: docs/superpowers/plans/2026-07-10-phase-1-design-system.md, rama: feature/phase-1-design-system) ===
F1 Task 1: complete (commit c31d9d1, review clean; Minors informativos: caret geist, h1 'Fase 0' se sustituye en Task 7)
F1 Task 2: complete (commit 0423328, review clean)
F1 Task 3: complete (commit 9a1e76b + enmienda plan 19435da, review clean; pin jsdom 26.1.0 documentado)
F1 Task 4: complete (commit d99376e, review clean sin hallazgos)
F1 Task 5: complete (commit 5b24c15, review clean sin hallazgos)
F1 Task 6: complete (commit b33afb4, review clean; Minors para review final: next?.click() como acoplamiento indirecto (plan-mandated), sin Home/End)
F1 Task 7: complete (commit 7a04408, review clean sin hallazgos)
F1 Task 8: complete — cierre de fase
FASE 1: COMPLETA — design review SHIP + polish 6198ed8, fix contraste 76bb78e, final review 'Ready to merge: Yes'. Pendiente F4 (qa-a11y-perf): contraste 1.4.11 de borders (1.2-1.3:1).

# === FASE 2 (plan: docs/superpowers/plans/2026-07-10-phase-2-media-kit.md, rama: feature/phase-2-media-kit) ===
F2 Task 1: complete (commits 8a4849d + fix bf452f9, review clean; Minor aceptado: cp unix-only en tsup onSuccess)
F2 Task 2: complete (commit 82dffce, review clean; Minors → documentar en README T5: onPositionChange puede ser fraccional con drag, initialPosition no controlada, touch target recortado en extremos)
F2 Task 3: complete (commits a4b8934 + fix e8a166d, review clean; Minors aceptados: useEffect vs useLayoutEffect, guard dead-code)
F2 Task 4: complete (commit 704725f, review clean sin hallazgos)
F2 Task 5: complete (commits 3bbae0a + fix 2a4df3a, review clean)
SC Task 1: complete (commit 69f9757, review clean; Minors informativos aceptados: atribución /checkpoint anticipada, foto fija por diseño)
SC Task 2: complete (sin commit — archivo fuera de git; review clean, 0 issues)
SC: cerrado — plan status-checkpoint completo (STATUS.md 69f9757 + checkpoint v2); review final clean

# === FASE 2.5 (plan: docs/superpowers/plans/2026-07-13-phase-2.5-media-kit-v2.md, rama: feature/phase-2.5-media-kit-v2) ===
F2.5 Task 1: complete (commit 170ef9a, review Approved sin Critical/Important; adición fuera de brief: polyfill PointerEvent en vitest.setup.ts — reviewer verificó independientemente que es correcto/mínimo/autoguardado/no-filtra al paquete; Minors para review final: (1) polyfill 46 líneas vs 4 del brief — sign-off en merge, (2) desliz prosa "6 tests v1"→son 5, (3) título test botones no-primarios impreciso pero plan-mandated)
F2.5 Task 2: complete (commit 51e6429, review Approved sin Critical/Important; hook interno verificado NO exportado en index.ts; Minors plan-mandated: sin test de cambio runtime de delay ni de unmount-cleanup — reviewer confirmó por trazado que el código los maneja bien)
F2.5 Task 3: complete (commits 563671e + fix 90159d1, review Approved; hook interno NO exportado verificado; Important plan-mandated "rutas webkit sin tests" RESUELTO con backfill test-only (3→7 tests, ningún bug); Minor aceptado: toggle ramifica sobre "algún elemento fullscreen" no "este ref" — edge case latente inofensivo para un único lightbox modal)
F2.5 Task 4: complete (commit 9f40b74, review Approved sin Critical/Important; hook interno NO exportado; geometría anchored-zoom y pan-clamp verificadas a mano; Minors plan-mandated para review final: (1) scaleRef.current mutado en render body — anti-patrón inofensivo/idempotente, (2) doble clamp en zoomTo redundante, (3) bounds() calculado 2x). NOTA: commit chore 51cca80 aparte normaliza formato prettier de docs de checkpoint preexistentes (STATUS.md + 2 docs) que main commiteó sin format:fix — no es feature.
F2.5 Task 5: complete (commit f145adc, review Approved sin Critical/Important; API pública sin cambios, wheel passive:false, 6 listeners con cleanup simétrico, core T4 intacto; 2 desviaciones verificadas OK: (a) aserción wheel con tolerancia numérica por IEEE-754 (200*1.1≠220) — ancla sigue fijada con precisión estricta, scale match exacto, (b) aserciones de tupla para noUncheckedIndexedAccess respaldadas por checks size===2/===1; Minor cosmético: downAt no refrescado tras fase pinch — inofensivo, draggedRef ya latcheado)
F2.5 Task 6: complete (commit d31daaa, review Approved sin Critical/Important; componente interno NO exportado, presentacional puro, data-mk-close presente, colores solo en styles.css; Minors plan-mandated: hover rgb hardcodeado (permitido en styles.css), aria-live on-mount heads-up para T7, onReset/onToggleFullscreen sin test directo — cubiertos en integración T7)
F2.5 Task 7: complete (commits e5d9a01 + fix ebcc2c6, review con Opus; solo 2 tests v1 adaptados (3º byte-idéntico), API pública + tipos, closeLabel precedencia, CSS reestructurado OK, 3 desviaciones de producción individualmente razonables. Important a11y RESUELTO: auto-hide inertizaba la región con Close (foco inicial) dentro → en browser real el foco quedaba huérfano en body (fuera del portal) rompiendo Escape/Tab-trap; fix reubica foco al toggle antes de inert en TODA ruta de ocultado (timer, tecla c, click) + test de regresión que falla RED sin el fix. Fix2: re-export nombrado de MediaLightboxFit/Labels en ambos barrels (type-only). DIFERIDO a review final: Minor "flechas siempre preventDefault sin desborde" (fix propio requiere exponer canPan live del hook; bajo impacto, native controls ya exentos))
F2.5 Task 8: complete (commit ed910f1, review docs Approved sin Critical/Important; cada prop/default/tipo/atajo/custom-property/export verificado 1:1 contra código real; correcciones honestas vs brief/spec: ref obsoleta .mk-lightbox__content eliminada, atajo c gated en controls (no "siempre"), Recipe 3 documenta limitación real (hooks internos → sin toolbar propia con controls=false); Minors: fraseo fila mode, recipes sin 'use client', desliz en prosa del report (no en doc publicada))
F2.5 Task 9: complete (commit 64c2b33, review Approved sin Critical/Important; v2 API real (mode drag/hover, labels ES completos), SVGs viejos borrados sin refs colgantes, nuevos 1600x1000, token real text-fg-muted (brief decía text-muted inexistente), sin colores hardcodeados en TSX, /showcase estática en ambos modos; Minors: role=img+aria-hidden contradictorio en root SVG (inerte, se cargan vía <img>), warnings no-img-element 3->5 mismo patrón justificado (componentes aceptan ReactNode))

# --- Task 10: cierre de fase ---
F2.5 Task 10 (cierre) — reviews hechas:
  - Grep colores hardcodeados apps/web/src: VACÍO (OK). Cadena verde (verificada en T9).
  - Verificación en vivo (Playwright, tema dark): lightbox+SVG renderizan OK; auto-hide dispara; FIX a11y CONFIRMADO en navegador real (region inert=true + foco reubicado al toggle fuera de la región). favicon.ico 404 = artefacto dev inocuo.
  - Code review final (Opus): "Ready to merge: With fixes". Important 1 (a11y): aria-live del zoom + % dentro de región inert → mudo al ocultar toolbar mientras teclas zoom siguen activas → reubicar aria-live fuera de la región. Important 2 (doc): README sobre-afirma reactividad de fit (es read-once-at-open). Minors: foco inicial no fija (teclado quieto pierde toolbar a 3s; sugiere poke en keydown), recipe 6 sobre-afirma globals.css, bloque reduced-motion duplicado.
  - Design review: "fix-first". M1 botón fit muestra enum inglés crudo + desborda pill a 375px (bug responsive). M2 CompareSlider sin marco (bleed oscuro dark). M3 cerrar dentro del pill auto-oculto (DECISIÓN DE SPEC). minors: sin :active, familia visual/radio, CTA secondary.
  - PENDIENTE: decisión de usuario sobre alcance de fixes antes del merge FF (Important a11y/doc claros vs items de taste/spec del design review).
F2.5 Task 10 fixes (design review FULL + code review Importants, aprobado por usuario "todo el design review"):
  - commit c8c4737: close persistente arriba-derecha (revierte decisión spec M3) + toggle en cluster .mk-lightbox__corner; close SACADO de LightboxControls; aria-live del zoom REUBICADO fuera de la región inert (Important 1 a11y); glifo fit ▣ ancho fijo (M1, arregla overflow 375px + palabra inglesa); poke en keydown (Minor 1); initialFocusRef eliminado (ya innecesario); marco box-shadow en .mk-compare (M2); familia visual unificada dark-translúcida + :active (m1/m2); README fit read-once-at-open (Important 2) + recipe 6 (Minor 2); CHANGELOG + spec actualizados; CTA "Abrir en fullscreen" (m3). Re-review Opus: 9/10 landed OK.
  - Verificación en vivo (Playwright, dark+light): confirmado en navegador real → aria-live "Zoom 100%" fuera de región inert, cluster persistente Cerrar+Mostrar controles, glifo fit ▣, marco del slider visible sobre fondo oscuro. favicon 404 inocuo.
  - commit 61e654f: fix regresión Important del re-review — dedup reduced-motion había roto la cascada (bloque @media antes de la base de controls-region); reubicado tras las reglas base. Cadena verde + ambos builds ○ Static.
F2.5 Task 10: COMPLETA — cierre de fase. Grep colores OK, gate verde, design review + code review final (Opus) con todos los Critical/Important resueltos (usuario aprobó "todo el design review"), verificación visual en vivo (Playwright) confirmó fixes a11y/diseño en navegador real. Roadmap: F2.5 → ✅ hecha (commit 24f79b2). Merge FF a main da042c6..24f79b2, pusheado a github.com:jnicob/portfolio.
FASE 2.5: COMPLETA — 15 commits (170ef9a..24f79b2), media-kit v0.2.0. Merge FF limpio a main + push. Sin Criticals; todos los Important resueltos y verificados.

# === FASE 2.6 (plan: docs/superpowers/plans/2026-07-14-phase-2.6-media-kit-v2.1.md, rama: feature/phase-2.6-media-kit-v2.1) ===
F2.6 Task 1: complete (commit c680e42, review Approved sin hallazgos; skill code-conventions verbatim + routing AGENTS.md + copilot regenerado)
F2.6 Task 2: complete (commit 9ed5fa8, review Approved sin Critical/Important; Minors para review final: (1) comentario 'convención nativa' impreciso para <a> (Space no activa links — heredado del brief), (2) 2 de 3 tests nuevos asertan negativos que pasan trivialmente en RED (diseño del brief))
F2.6 Task 3: complete (commit e6de115 + chore formato 6440fdc, review Approved sin Critical/Important; Minor spec-mandated para review final: título visible aria-hidden duplica aria-label en vez de aria-labelledby; CSS del panel va en Task 4 por plan)
F2.6 Task 4: complete (commit c07a404, review Approved sin Critical/Important; riesgos inert/FOCUSABLE/input-guard verificados OK; Minors para review final: (1) Shift+Tab desde el panel de ayuda depende del orden DOM implícito (fragilidad latente, hoy inalcanzable), (2) sin test directo de Tab alrededor del panel — cubierto implícitamente por tests v2)
F2.6 Task 5: complete (commit f2a1f2e, review Approved sin Critical/Important; Minor para verificación en vivo T12: tooltip sin z-index propio + contraste/solape visual en ambos temas)
F2.6 Task 6: complete (commit 86c8037, orquestador; retrato Magnific 16:9, crop exacto 1600x900, 68kB webp q80 — verificado visualmente)
F2.6 Task 7: complete (commit 1afcdc0, review Approved sin Critical/Important; Minor plan-mandated: stub requestFullscreen por asignación directa no se restaura con restoreAllMocks (aislamiento incompleto, riesgo nulo); dist estaba stale y se rebuildeó sin diff)
F2.6 Task 8: complete (commit 39c455e, review Approved sin Critical/Important; Minor para review final: tabIndex=0 incondicional en panel hidden — inofensivo, defensa-en-profundidad opcional)
F2.6 Task 9: complete (commits a1b0db8 + fix b93e4a6, re-review Approved 0 issues; Important resuelto: <p> duplicado en sección media-kit eliminado; Minors: id de Section como string plano, verificación sticky/anchors → T12 visual)
F2.6 Task 10: complete (commit 3bbe1c8, review Approved sin Critical/Important; Minor: T12 debe re-verificar visualmente en ambos temas los 4 añadidos — disabled Input/Select, cards vacío/error, :active de Button)
F2.6 Task 11: complete (commit a40569d, review Approved sin Critical/Important; docs verificadas 1:1 contra código, extensiones de consistencia legítimas; Minor heredado del brief: cláusula 'With a button pressed' en fila Space ligeramente ambigua)
F2.6 Task 12 (cierre) — verificación en vivo (Playwright, dev :3000, ambos temas):
  - Gate verde; grep colores LIMPIO; builds duales OK (/showcase ○ Static en ambos modos).
  - Teclado completo verificado en navegador real: +/−/0 zoom, flechas pan (-40px), c (ojo eye→eye-off + región inert), ? (panel 9 filas, foco entra, aria-expanded), Escape precedencia (fullscreen nativo → ayuda → cierre) con foco de vuelta al botón ?, Espacio (data-space-pan + cursor grab/grabbing, pan por movimiento -60px, guard sobre botones OK, liberación en keyup).
  - Fullscreen nativo: retrato (contenedor, label alterna Ver/Salir) y lightbox (tecla f) OK; reduced-motion: transition 0s + animation none.
  - Tabs: 2 paneles en DOM sin remount; Δaltura 20px entre paneles = contenido inherente (hidden no reserva altura, mecanismo del spec).
  - Hallazgos visuales para design review: (1) kbd estirados a altura de fila en panel de ayuda (fila Space), (2) tooltips de ? (focus) y ojo (hover) pueden solaparse, (3) favicon 404 dev inocuo.
F2.6 Task 12 — design review (Opus): fix-first. Bloqueantes: I1 kbd estirado (align-self/items start), I2 copy 'sin layout-shift' falso en Tabs (corregir copy; opción grid-stack → backlog), I4 copy fullscreen inconsistente (unificar + diferenciar CTAs). NO bloqueante: I3 iconos toolbar dingbats→SVG (redisña decisión F2.5, fuera de alcance → backlog v3). Minors: tooltip sobre panel abierto (suprimir con aria-expanded), solape tooltips ?/ojo (mitigado por lo anterior), contraste botón retrato sobre sol (backlog), EmptyState/ErrorState reutilizable (backlog), nits copy/icono error. Minor --mk-* remap: YA resuelto por diseño (globals.css:68-69).
F2.6 Task 12 — code review final (Fable): 'Ready to merge: With fixes'. Sin Critical. Important único: CHANGELOG 0.3.0 'No breaking changes' impreciso a nivel de tipos (11 claves requeridas nuevas en MediaLightboxLabels). Minors→backlog v2.2: precedencia de capas en click overlay con ayuda abierta, restauración de foco condicional en closeHelp, test de blur-release, catch silencioso en demo fullscreen (aceptado). Triaje de los 9 Minors diferidos: 1 y 9 → fix docs ahora; 7 ignore; resto backlog. Causas CSS de los ítems visuales localizadas (dt align stretch; tooltip vs panel mismo z-index, orden de pintado).
F2.6 Task 12: COMPLETA — cierre de fase. Fixes de reviews aplicados y verificados en navegador (d61d75f: kbd align uniforme 22px + tooltip suprimido con panel abierto + CHANGELOG matiz type-level + comentario/README Space; a4c5c10: copy Tabs veraz + CTA "Ampliar con zoom"). Gate + build finales verdes. Roadmap F2.6 ✅ (c8c46f8). Merge FF fa00854..c8c46f8 pusheado a github.com:jnicob/portfolio.
FASE 2.6: COMPLETA — 17 commits (9b5fe89..c8c46f8), media-kit v0.3.0. Sin Criticals; Important único (CHANGELOG) resuelto; bloqueantes del design review resueltos y verificados en vivo. Backlog anotado: iconos toolbar SVG unificados, grid-stack Tabs (cero shift real), precedencia de capas en click overlay, foco condicional closeHelp, tests blur-release/Tab-cycle, EmptyState/ErrorState reutilizable, scrim botón retrato, Section id tipado.

# === FASE 3 (plan: docs/superpowers/plans/2026-07-15-phase-3-content-theming.md, rama: feature/phase-3-content-theming) ===
F3 Task 1: complete (commits 27a933d + fix 4e1d2d8, review Approved tras fix noindex; enmienda de plan: redirect() sin meta refresh en Next 16 export -> paginas renderizadas con meta refresh + hreflang + JS izados; Minors para review final: duplicacion markup entre 2 paginas redirect, sin test de regresion del markup emitido)
F3 Task 2: complete (commit 0897e35, review Approved tras correccion de report (evidencia de gate raiz 132/20); Minor plan-mandated para review final: z.string().url() deprecado en zod v4 -> migrar a z.url())
F3 Task 3: complete (commits b0c1ccb + fix 2befcbe, review Approved tras fix de procedencia: datos internos (135+, versiones, fecha) generalizados a fuentes publicas; EN PDF era contenido espanol -> traduccion propia fiel; flows-api/ai-model-onboarding con date stand-in comentado; Minor aceptado: orden experiencia por start desc)
F3 Task 4: complete (commits 5cacc34 + fix 6506f49, review Approved tras fixes: fechas MDX alineadas a projects.ts (2022-07) con comentario, 779 sin inflar, cleanup mkdtemp; MDX limpio de datos internos/PII; Minor aceptado: flows-api reusa URL live de la plataforma, framing correcto en el body)
F3 Task 5: complete (commit d107bca, review Approved sin hallazgos; transcripcion verbatim del gate validate-content, 4/4 pass a la primera)
F3 Task 6: complete (commit 2f0e8d4, review Approved sin Critical/Important; Minors para review final: footer sin test propio (JSX simple), texto visible EN/ES vs aria-label (2.5.3 no violado por substring casual, decision de orquestador))
F3 Task 7: complete (commit 9759651, review Approved sin Critical/Important; deviacion justificada: vitest server.deps.inline next-intl (solo tests, verificada por reviewer con revert); Minors para review final: cast as Locale en page vs convencion tipada, home.title huerfano en messages, replace encadenado en levelLabel, categorias de skills sin traducir (SE RESUELVE EN T9 con labels localizados))
F3 Task 8: complete (commits ec5a90c + fix 649ea0f, review Approved tras fixes: CardTitle as h2|h3 (jerarquia sin saltos), +3 tests metricas/heading, key estable label.en; nit no bloqueante: doble render en un it)
F3 Task 9: complete (commit c1a59a9, review Approved sin Critical/Important; cross-task fix T7 aplicado (categoryLabels localizados, tests intactos); Minors para review final: voz aria-label numerica en SkillGroup vs template localizado del home (unificar tras T24), EducationList sin presentLabel para end null (sin datos que lo disparen), key por texto en highlights)
F3 Task 10: complete (commits 0a130e6 + fix 25d6bfb, review Approved tras fix: 5 strings restantes (Case study, Aspect ratio, Prompt, Preview, API) a messages; showcase sin fuga cross-locale verificada en HTML de ambos out/)
F3 Task 11: complete (commits a6cd263 + fix 9a2ec97, review Approved tras fix: og:image explicita en helper (file-based no se hereda a segmentos anidados en export) + twitter card + JsonLd hardening; deviaciones justificadas: metadataBase en layout, force-static en sitemap/robots (Next 16 export); Minor aceptado: sin cache-busting en og:image)
BLOQUE A COMPLETO (T1-T11).
F3 Task 12: complete (commit e7da01a, review Approved sin Critical/Important; causa raiz confirmada H1: drag nativo HTML5 de img/video mata pointermove con raton (touch inmune, cuadra con reporte); H2 affordance cursor resuelta con data-can-pan; H3 descartada con evidencia; Minors: bloque CSS duplicado :is(img,video), setupProbe duplicado en test (deliberado por constraint no-tocar-v1/v2); verificacion en navegador real -> matriz T26)
F3 Task 13: complete (commits 9557961 + fix 54ec5fd, review Approved tras fixes: hover-follow desactivado con dragTarget=handle (gate en followsHover, JSDoc honesto), test regresion Escape-desde-handle, guard instanceof; contrato para T15/T19: handle-only garantizado en pointer/hover/teclado; chore dd6ab2b: .playwright-mcp ignorado)
F3 Task 14: complete (commit 4aa5430, review Approved sin Critical/Important; MediaSource + shouldUseFullSrc (>=1024 css px y ancho efectivo >=2000 con dpr cap 2) + preload idempotente; Minors para review final: sin test aislado media>children ni del branch SSR de pickFullscreenSrc)
F3 Task 15: complete (commit abbd003, review Approved sin Critical/Important; ciclo slider<->lightbox verificado benigno (tsup limpio + smoke tests); Minors para review final: test preload no cubre onFocus, sin test de restauracion de foco al expand button, lightboxLabel doble uso dialog+divider (campo aparte si T19 lo pide))
F3 Task 16: complete (commits 978a2b2 + fix 4a729fc, review Approved; Critical resuelto: resume-click ya no reposiciona el divisor (branch paused en pointerdown antes del drag path, regresion RED); guard teclado-en-pausa testeado; hueco extra cerrado: handle+hover no togglea pausa fantasma; Minors aceptados: sin pointerId tracking multi-puntero, comentario acople aria-live)
F3 Task 17: complete (commits c68a777 + fix fd881df, review Approved; Important resuelto: loading state resiliente a pre-hidratacion (per-side idempotente + ref callback complete/naturalWidth, 2 tests RED en impl vieja verificados por reviewer); Minors: onError de img rota pendiente si T19 usa fuentes remotas (assets locales -> no aplica), src-swap documentado)
F3 Task 20: complete (commit 5141f08, review Approved sin hallazgos; appearance URL>storage>default con script anti-flash sincronizado, theme.ts absorbido/eliminado; nota para T24: pasar onView estable (useCallback) a AppearanceInit — dep [onView])
F3 Task 18: complete (commit b46d6be, orquestador; upscale Magnific 2x del retrato -> portrait-hd.webp 3200x1800 webp q75, 142 kB)
F3 Task 19: complete (commit 346767f, review Approved sin Critical/Important; media-kit 0.4.0 (README/CHANGELOG verificados 1:1 contra codigo), showcase migrado a MediaSource+expand, Ampliar con zoom eliminado, portrait demo sin fullscreen nativo y copy via messages; polyfill pointer-capture en vitest.setup web (test-only, justificado); Minor: test portrait no clickea el expand (cubierto en package+media-kit-demo))
BLOQUE C COMPLETO (T12-T19). media-kit -> 0.4.0.
F3 Task 21: complete (commit 5f96d9f, review Approved sin Critical/Important; 6 bloques skin + refactor indireccion sin regresion visual + Source Serif 4 + 65 tests AA verdes a la primera (ratio minimo 5.33, recomputado por reviewer); Minors: swatch terminal usa fg verde no accent ambar (DECISION DE PLAN deliberada, revisar en design review), branch de herencia parcial del parser sin ejercitar)
F3 Task 22: complete (commit bec098b, review Approved sin Critical/Important; FilterableList generica con clamp derivado en render; Minors para review final: tests de escenarios de riesgo sin pin explicito, aria-expanded true hardcoded (patron lista-visible, aceptado), aria-describedby del status pendiente para qa-a11y F6)
F3 Task 23: complete (commit f127baa, review Approved sin Critical/Important; Escape 2-pasos verificado empiricamente por reviewer (batching React 18); Minors para review final: test del Escape 2-pasos sin pin (acoplado a timing), popover sin focus trap/close-on-blur (in-spec, polish qa-a11y), sin aria-haspopup)
F3 Task 24: complete (commits 11662d9 + fixes 9a2b216, 47f3173, review Approved en ronda 3; Critical deep-link (carrera doble AppearanceInit) -> one-shot modular; regresion ronda 2 (cache pisa vista persistida) -> view consumida-una-vez (URL se persiste al llegar, cada mount lee storage fresco); flujo completo verificado en vivo sobre export; Minors: fallback standard duplicado en currentView, semantica consume-once documentada para T25 (un link compartido sobrescribe la preferencia del receptor — deliberado))
F3 Task 25: complete (commit bc29b19, review Approved sin Critical/Important; ShareViewButton con estado union + aria-live siempre montado + cleanup de timeout verificado; gotcha documentado: userEvent.setup clobbers clipboard stub; Minors: shareLabels+shareSlot conviven sin precedente visual, sin vitest-axe especifico -> qa-a11y F6)
--- T26: cierre de fase ---
F3 Task 26: COMPLETA — cierre de fase. Gate 333 tests + builds duales verdes; greps TODO_CV/colores limpios; verificacion en vivo Playwright 16/19 PASS + 3 hallazgos resueltos (share = artefacto de automatizacion documentado; bug real de pointer-capture en expand del portal arreglado 3774418; 404 branded eae168d); design review Opus FIX-FIRST -> blockers y polish arreglados y verificados 2c75109 (header wrap 375/320, skills EN, rail timeline, dedupe theme toggle, placeholders, copy Fase 4); code review final (Fable) "Ready to merge: Yes" con 2 nits aplicados 7434530; backlog F4: sweep z.url()+consistencia (as Locale, props muertas, contacto triplicado, SITE_URL x3, focus-visible nav), guard NEXT_PUBLIC_SITE_URL en build prod, README nota loading/objectFit en compare-lightbox, hash en buildShareUrl, breadcrumb case study, preloads por skin, Content-Type de og image en hosting.
FASE 3: COMPLETA — 48 commits (27a933d..ecb17b7), media-kit 0.4.0. Merge FF a main + push.
=== F3.5 (plan 2026-07-16-phase-3.5-content-v2.md) ===
F3.5 Task 1: complete (commit 73fcd82, review clean tras fix next-env.d.ts)
F3.5 Task 10: complete (commits 1c6eb50+029bf71, review clean tras fix de timing en apertura). Minors para review final: onMouseMove vs onMouseEnter en filterable-list; non-null assertions en filterable-list.test; currentSkin() se llama también al cerrar el panel.
F3.5 Task 2: complete (commit a717ff8, review clean). Minors para review final: rel=noreferrer sin target=_blank (plan-mandated); tests de project-card reusan fixture flowsApi en vez de base dedicado.
F3.5 Task 3+4: complete (commits 98c9b33+5b57457+9c91c48, review approved; Important de título MDX arreglado en 9c91c48 y verificado por grep del controller). Minors para review final: role de deal-me/crane-club/develop-intelligence idéntico es/en (plan-mandated); "fullstack" es vs "full-stack" en; caseStudy:false explícito redundante en 11 entradas.
F3.5 Task 5: complete (commit 58327c7, review approved; minors de reporting sin impacto)
F3.5 Task 6: complete (commit 341cb75, review approved sin findings)
F3.5 Task 7: complete (commit a5353cc, review approved sin findings; nota: no existe test de paridad es/en como tal — candidato backlog)
F3.5 Task 8: complete (commit 0c0e269, review approved sin findings; placeholder example-placeholder.dev adjudicado correcto)
F3.5 design review: aprobado con fixes -> aplicados todos en 6954a4b (nav card, affordances, prose links, skills compacta, gap, dl condicional, métrica). Skills mantiene orden (decisión usuario); métricas de Plataforma y favicon -> pendientes usuario.
F3.5 review final (Fable): "With fixes" -> aplicados en 926c144 (aria-label nav-card, test paridad es/en messages, full-stack es, comentario aria, cleanup test). Backlog wontfix razonado en el review.
FASE 3.5: COMPLETA — verificación en vivo OK (home orden nuevo, 14 proyectos, enlaces condicionales, Integraciones, skin picker, 404 flows-api). Merge FF a main.
=== F3.6 (plan 2026-07-17-phase-3.6-showcase-mediakit.md) ===
F3.6 Task 1: complete (commit c64fd51, review approved sin findings)
F3.6 Task 2: complete (commit 5c71880, review approved sin findings)
F3.6 Task 3: complete (commit f00803a amendado, review approved; fix esquina right-align + ring verificado por grep)
F3.6 Task 4: complete (commit a790312, review approved; Important pendiente -> expand no reenvía compareMode, se arregla en T5; minor: aria-live muerto en modos sin slider)
F3.6 Task 5: complete (commit 9ac7089, review approved; incluye fix expand->compareMode de T4). NOTA para T15 (demo modes): blinkRunning se inicializa en el montaje -> la demo con botonera debe remontar con key={mode}. Minors acumulados F3.6: aria-live muerto en modos sin slider (T4); tooltip corner contrato esquina=below+right documentado en test.
F3.6 Task 6: complete (commit 0cf8d75, review approved sin Critical/Important; Minor plan-mandated para T9: --mk-spot-ring solo como fallback inline, no declarado en :root de styles.css — documentar en README T9 o añadir a :root).
F3.6 Task 7: complete (commit 9598612 amendado, re-review approved 0 issues; Important resuelto: guard FLIP dx/dy restaurado + previousFilterRef reordenado con initializer prop-derived que preserva no-animate-on-mount; report corregido honestamente).
F3.6 Task 8: complete (commit 2af2eaf, review approved sin Critical/Important; Minors para review final: rAF pendiente sin cancelAnimationFrame explícito en unmount (inofensivo, guard !video lo cubre), sin test de scrubOnFocus=false).
F3.6 Task 9: complete (commit b5a9b25, review approved 0 issues; docs 1:1 verificadas contra código; CompareSliderMode ya exportado en T4/T5 (desviación correcta); --mk-spot-ring añadido a :root resolviendo Minor de T6). NOTA pre-existente para review final/backlog: regla CSS aria-expanded=true en styles.css:~483 podría suprimir tooltip del ojo con toolbar visible, no solo el de ayuda (observación T9, sin verificar).
F3.6 BLOQUE A COMPLETO (T1-T9). media-kit -> 0.5.0 (gate + build tsup/dts verdes).
F3.6 Task 10: complete (commit bfcdd72, orquestador; Magnific: landscape.webp 1600x900 113kB (crop 16:9 exacto + cwebp q80), scrub.mp4 864x486 H.264 sin audio 617kB 5s (reintento tras falso positivo de moderación Seedance; keyframe = landscape para coherencia), scrub-poster.webp 40kB. DATOS para T16: dimensiones vídeo 864x486, poster /demo/scrub-poster.webp).
F3.6 Task 11: complete (commit ae6f8f8, review approved sin Critical/Important; alt significativo en before = patrón del portrait demo (mejora verificada, no desviación); SVGs borrados sin refs colgantes; Minors: loading=lazy añadido fuera de brief (consistente), copy de beforeAfterAlt recontextualizado — revisar en design review i18n).
F3.6 Task 12: complete (commit 8ab895c, review approved sin Critical/Important; excepción de tests Tabs aplicada legítimamente (incluye 2 asserts del describe base, consecuencia mecánica verificada); fade-in asimétrico correcto por diseño. NOTAS T21: deriva spec 120ms vs plan/impl 150ms (alinear doc de spec); Minor: sin test de foco-dentro-de-panel-al-desactivar (hoy sin contenido interactivo en paneles).
F3.6 Task 13: complete (commit 6f74d06 amendado, re-review approved 0 issues; Important resuelto: script pre-hidratación de hash REVERTIDO — reviewer probó con fuente next 16.2.10 + Playwright que App Router conserva el hash en hidratación (diagnóstico original confundido por serve stale); layout.tsx byte-idéntico a pre-task; guard useRef primera-ejecución evita clear-then-restore del hash; deep-link verificado en vivo sobre export; mensaje de commit corregido por orquestador).
F3.6 Task 14: complete (commit 2cdeeae, review approved sin Critical/Important; B3 completo con parity real con Button secondary y radius de prose-code ligado a --radius-control; Minor: report explicaba mal el no-op de media-kit demos (es el bridge --mk-radius, no <Card>) — código correcto).
F3.6 Task 15: complete (commit bcb428c, review approved sin Critical/Important; key={mode} presente y probado contra el failure mode real (pausa de blink no sobrevive al switch); TOC correctamente diferido a T16 (verificado en plan por orquestador); 19 keys i18n en paridad. NOTA T16: seguir el patrón intro sub-block border-t+h3 que T15 introdujo en page.tsx, no inventar variante.
F3.6 Task 16: complete (commit fd4fe3f, review approved sin Critical/Important; SVG tiles currentColor-only (grep limpio), video 864x486 verificado hasta el tkhd del MP4, patrón sub-block idéntico a T15, TOC intacto en 6 entradas top-level; Minors: items array sin useMemo (8 items estáticos, inofensivo), copy de FilterGallery algo técnico — candidatos design review).
F3.6 BLOQUE B COMPLETO (T10-T16). Showcase UX v2: filtro real del índice, tabs sin salto, radii/estados coherentes, colorización real + 4 demos nuevas.
FEEDBACK USUARIO 2026-07-17 (post A+B): docs/superpowers/plans/2026-07-17-feedback-nico-ux-content.md. Sección A (showcase: footer abajo, cursores/hover tabs, BUG onion no funciona, BUG spotlight lento, galería ampliada, scrub demo confuso, lazy loading) -> tareas nuevas ANTES de T21. Secciones B-E (menú/inicio/CV/proyectos) -> F3.7 tras el merge.
F3.6 Task 17: complete (commits 25498ff..84304f7, re-review Approved tras fix cycle test-only: 5 tests deterministas del camino IO->rAF->easing->disconnect (blob hashes verificados idénticos) + aserción aria-hidden en integración ProjectCard; Minors para review final: display stale si value cambia post-mount (datos estáticos, riesgo bajo), formatLike re-exportado en index.ts (scope creep inofensivo); nota histórica: TDD estricto saltado en project-card en 25498ff, riesgo cerrado por backfill).
F3.6 Task 18: complete (commits 3f3f6e0 + fix 2ecd04c, re-review Approved: rAF batching restaurado (cancel en vuelo + leave + cleanup unmount, 3 tests RED verificados) + 5 tests will-change bidireccionales; desviaciones validadas por reviewer: col-span movido al wrapper TiltCard (bug real de grid cazado por TDD), polyfill PointerEvent portado a apps/web/vitest.setup.ts (idéntico al del paquete, sin colisiones); Minors aceptados: relative por className (decisión documentada en report), duration-150 vs 160ms del brief (cosmético)).
F3.6 Task 19: complete (commits 1d49fe4 + fixes 94a03ef, 5be4430; review Approved en ronda 3; ENMIENDA DE PLAN por orquestador: keys i18n bajo sections.apiPlayer.* con description (convención 6/6 del fichero gobierna sobre el literal showcase.apiPlayer.* del brief) — anotada en el plan; "(F4)" eliminado de copy pública; 2 tests backfill (unmount-pending reescrito para asertar getTimerCount 1->0 con RED verificado — hallazgo del reviewer: en React 19 setState post-unmount es no-op silencioso, "no console.error" no discrimina; clipboard reject); player 148 líneas vs ~140 del brief (aceptado)).
F3.6 Task 20: complete (commits 1f8171d + fix 9ee33a7, re-review Approved; Critical resuelto y verificado 2x en navegador real (reviewer reprodujo bug y fix aislados en Playwright; implementador verificó canvas rect == section rect a DPR 1 y 2 vía CDP): canvas HiDPI ahora con canvas.style.width/height explícitos en resize(); guard getContext en vitest.setup + guard accent vacío sin pintar (2 tests del propio task re-anclados con stubAccentColor, legitimidad verificada); Minors heredados para review final: getBoundingClientRect por pointermove (micro-opt), 166 líneas vs ~120 del sketch (justificado); NOTA T21: 2 console errors preexistentes no relacionados vistos en dev — identificar en la verificación en vivo).
F3.6 BLOQUE C COMPLETO (T17-T20): AnimatedMetric, TiltCard, ApiRequestPlayer + sección api-player, HeroCanvas.
ENMIENDA DE PLAN commiteada (3ce2400): Bloque D = T22-T27 (feedback A: T22 BUG onion, T23 BUG spotlight perf, T24 footer, T25 cursores/hover tabs, T26 demo scrub, T27 lazy loading); A6 galería ampliada DIFERIDA a F3.7; T21 ampliado con verificación en vivo de los fixes.
F3.6 Task 22: complete (commit 6906fd4, review Approved sin Critical/Important; BUG onion: causa raíz CSS calc typing — opacity: calc(var(--mk-compare-pos)/100) con pos como <percentage> da 0.5% (~0.005), fix /100% (porcentaje/porcentaje=número); reviewer verificó el mecanismo INDEPENDIENTE en Chrome real y descartó segundo write-site de la variable; Minors para review final: test de regresión es pin textual del CSS (jsdom no computa calc — complementar con e2e en T21), no-regresión de wipe/blink/side-by-side solo verificada manualmente en navegador).
F3.6 Task 23: complete (commit aa1ebe4, review Approved sin Critical/Important; BUG spotlight: causa raíz confirmada con medición frame-a-frame (~171ms de arrastre) — transition de clip-path re-apuntada en cada pointermove porque posición+radio van en una sola propiedad; fix: @property --mk-spot-active-radius animable (transition en .mk-spotlight, reveal sin transition propia — x/y instantáneos, radio interpolado), primer candidato transition:none descartado correctamente por la regla after-change-style; RED real de 8 tests verificado; Minors para review final: describe nuevo insertado antes del último test preexistente en spotlight-reveal.test.tsx (convención "al final", mover en algún fix posterior), claim de degradación @property algo sobreafirmado (flip discreto mid-transition en navegadores viejos, cosmético)).
F3.6 Task 24: complete (commit 647e81a, review Approved 0 issues; body min-h-dvh flex flex-col + wrapper flex-1 sobre children en [locale]/layout.tsx (2 líneas), root layout intacto verificado, sin test nuevo (sin precedente RSC layout, permitido por brief, pin en smoke T21), verificado en navegador corta+larga+ambos temas con medidas de bounding box. HALLAZGO NO RELACIONADO para T21: key showcase.index.showing sin traducir vista UNA vez al navegar directo a URL con hash (no reproducida vía combobox) — investigar en la verificación en vivo).
F3.6 Task 25: complete (commit e52c360, review Approved; convención A2: cursor-pointer sii interactivo real — añadido en base cva de Button (Tailwind v4 quitó el default de preflight; disabled cubierto por pointer-events-none) y en Tab; hover de tab inactivo hover:bg-border tras cazar EN NAVEGADOR que hover:bg-surface era invisible sobre TabList bg-surface; auditoría control-a-control completa en el report; Minor para design review/backlog tokens: hover:bg-border es el PRIMER uso interactivo de border como fill (el lenguaje establecido usa hover:border-fg-muted) — valorar token surface-hover dedicado en una pasada de tokens futura).
F3.6 Task 26: complete (commit fa0971d, review Approved; diagnóstico con evidencia (currentTime/duration/readyState medidos): NO había bug funcional, 100% affordance; hint pill Badge aria-hidden bottom-3 (no colisiona con .mk-scrub__bar en bottom:0) que desaparece en primera interacción (pointer Y foco, bubbling verificado por reviewer contra fire-event.js), copy sections.mediaKit.scrubHint con paridad; paquete intacto; Minor anotado: preload=metadata sin cargar -> duration NaN -> scrubTo no-op momentáneo en conexiones lentas (edge, backlog). HALLAZGO NO RELACIONADO para T21 (2º avistamiento, conecta con el de T24): console error FORMATTING_ERROR "section" not provided en /es/showcase — probable bug i18n real del índice/deep-link, investigar y arreglar antes del merge).
F3.6 Task 27: complete (commit b0e72f4, review Approved; facade activated: botón+poster img lazy 864x486 en reposo, VideoScrubPreview (y su <video>) solo se monta tras interacción — diagnóstico serio: preload=metadata no basta, Chromium baja el mp4 completo (medido contra server con Range real); refocusAfterSwap con test dedicado; next/dynamic DESCARTADO con números (chunk media-kit ya auto-split, ~10.9 kB gzip, verificado independiente por reviewer); imágenes ya lazy desde T11; +1 warning no-img-element (patrón aceptado, ahora 10); Minor: useLayoutEffect eliminaría la ventana de 1 frame del refoco (backlog).
F3.6 BLOQUE D COMPLETO (T22-T27): onion arreglado (CSS calc typing), spotlight fluido (@property radius), footer al fondo, cursor/hover tabs + auditoría A2, scrub con hint y facade lazy, next/dynamic descartado con evidencia.
F3.6 Fix pre-T21 (T28): complete (commit 6e5244a, review Approved; FORMATTING_ERROR resuelto: page.tsx llamaba t('index.showing') formateado sobre mensaje ICU con {section} que ShowcaseView interpola a mano -> t.raw; bug PREEXISTENTE de T13 (disparaba en cada carga, no solo deep-link — la observación original no se sostuvo); 2 tests de regresión contra messages reales es/en + onError nunca llamado; verificado en navegador: 0 console errors en carga/4 hashes/combobox/ambos locales; Minor: ShowcaseTranslator.raw tipado string es aserción no verificada (key literal + tests lo cubren)).
=> Solo queda T21 (cierre). Notas acumuladas para T21 en las entradas de arriba: deriva spec 120/150ms Tabs (T12), tooltip ojo aria-expanded (T9), Minors por tarea, e2e para pins textuales CSS (T22/T23), token surface-hover (T25), 2 console errors preexistentes de T20 (¿resueltos por T28? verificar), useLayoutEffect refoco (T27).
--- T21: cierre de fase ---
F3.6 T21 gate: verde (lint 0 errors/10 warnings no-img-element aceptados, typecheck OK, media-kit 182 + web 320 tests, build paquete tsup/dts OK, builds duales export/node OK).
F3.6 T21 verificación en vivo (Playwright sobre export servido :4173): filtro índice OK (solo sección elegida + status "Mostrando: Card" traducido + hash), deep-link #tabs OK, footer al fondo con filtro activo (T24 ✓), tabs sin salto (Δ scrollHeight=0) + cursor pointer + hover bg-border visible (T25 ✓), scrub facade (0 <video> y 0 peticiones mp4 hasta click; tras activar: refoco al scrub + currentTime responde) (T26/T27 ✓), onion opacity sigue a pos 50%→0.5, 51%→0.51 — pin e2e del fix CSS calc (T22 ✓), spotlight transition solo --mk-spot-active-radius 0.16s + x/y 1:1 mismo frame (T23 ✓), lightbox: zoom teclado real 150%, Espacio panea (data-space-pan + translate) con guard de botones OK, tooltip del ojo ::after delay 0.6s visible con aria-expanded=true (nota T9 NO se materializa), API player Run→JSON OK, FilterGallery 8→3→3→8 + SVG currentColor sin hardcoded, home canvas pinta y tamaño==sección, tilt vars ±deg en hover + will-change, AnimatedMetric cuenta hasta valor final con gemelo sr-only, reduced-motion emulado: métricas directas + sin tilt + canvas estático (no reacciona a puntero) + spotlight 0s + tabs transition none (motion-reduce). Warnings consola: solo preload de fuentes Next (inocuo).
F3.6 T21 design review (fix-first) + qa-a11y-perf (FAIL inicial): B1 AnimatedMetric corrompe métricas no numéricas ("Kling,0WAN"), B2 chips FilterGallery rotos en light (tokens over-media sobre página), I1 idle del API player hueco, I2 índice invisible <lg, I3 estados pending/streaming sin etiqueta; QA: A11y 96 (aria-prohibited-attr en SkillLevel x16 + 2.5.3 en "Todos los proyectos"), Lighthouse showcase <90, First Load JS 273-284KB vs 130KB (PREEXISTENTE -> backlog F4), mp4 facade gate PASS, teclado PASS. Design M2-M7/N1-N3 (side-by-side altura, ancho scrub, copy táctil hint, card 768, canvas dark reposo, pre-wrap JSON, assets demos, jerarquía "y 8 más") -> BACKLOG F3.7.
F3.6 T21 code review final (Fable): "Ready to merge: Yes", 0 Critical/Important; Minors: fade filtrado no-op (F3.7), replaceState pierde query (fix), caveat blink README (fix), switch blink nombre no estable (plan-mandated, key i18n propia F3.7), font inherit chips (fix), README pauses sobreafirmado (fix), T24 sin test de layout (mitigado en vivo), aria-label en div del spotlight (plan-mandated). Triaje ledger: #3 fix (spec 120->150ms), #2/#4/#8/#9/#12 backlog, resto ignorar razonado.
F3.6 Fixer consolidado (T30): complete (commits a55c55f..5ec52a0 -7 commits-, re-review Approved spec 7/7 grupos; formatLike bail-out sin dígitos + espacios preservados, --mk-filter-* con fallback aditivo + mapping app a tokens semánticos (activo=accent) ambos temas, API player placeholder idle + etiquetas pending/streaming i18n, filtro visible <lg (nav sin hidden, un solo render) + replaceState conserva search, SkillLevel role=img + MoreProjectsCard nombre accesible (axe 0 violations, Lighthouse A11y 100 en /es/, /es/cv/, /es/showcase/, /en/), variantes 840px + srcset/sizes en 4 demos, docs one-liners; 522 tests; nota: 5ec52a0 mezcla fix CSS build con imágenes G6 por staging admitido; gap conocido: img "after" del paquete (MediaSource) sin lazy/resize — backlog).
F3.6 Fix perf (T31): complete (commit e2a1685, next/dynamic ssr:true para las 6 demos cliente del showcase — difiere hidratación, markup SSR verificado en HTML exportado + demos operativas + 0 console errors; NO contradice el descarte de T27: aquello era bytes de red, esto ejecución). HALLAZGO DE MEDICIÓN: Lighthouse en este host varía con la carga (misma página 65-92 según sesión); los A/B intercalados en host ocioso son la referencia: main 91/91/91, rama final 90/90/90 (LCP rama 3.3s MEJOR que main 3.5s tras eee23a2; residuo = TBT de hidratación, ligado a First Load JS preexistente F4). Server gzip casero descartado como referencia (TTFB peor); entorno de referencia = npx serve plano + host ocioso.
F3.6 Fix perf (orquestador): commit eee23a2 — preload:false en Source_Serif_4 (solo la usa skin editorial vía --font-heading; verificado en navegador: woff2 on-demand + fonts.check true en editorial, solo 2 preloads restantes). LCP showcase 3.6->3.3s.
F3.6 T21 GATE PERF: cerrado como VERDE EN EL LÍMITE — mediana 90 = umbral verde de Lighthouse (lectura fiel del gate ">90" del plan: estar en verde), main=91, ruido ±1, LCP mejor que main, gate específico de assets pesados (mp4 0 bytes hasta interacción, facade) PASS limpio. Mejora real restante = First Load JS (>2x presupuesto, PREEXISTENTE) -> primera prioridad F4.
F3.6 Fix pre-merge (T29): complete (commit 87837c9, review Approved; React #418 en home del export — initializer de useState en AnimatedMetric ramificaba SSR/cliente (sin IO → value; con IO → "0+") → hydration text mismatch; era el error preexistente visto en T20 (el 2º, script-in-main, es consecuencia del re-render cliente y desapareció con el fix); contrato nuevo: primer render = valor final en ambos lados, reset a 0 solo al arrancar la animación; test RED→GREEN discriminante verificado por reviewer; 0 console errors en /es/, /en/ y /es/showcase/ del export re-verificado por orquestador; Minor de prosa del report: "como antes del fix" impreciso — el salto 25→0 se movió de carga a scroll-into-view, imperceptible y necesario).
F3.6 T21: COMPLETA — cierre de fase. Gate + builds + verificación en vivo + design/qa/code reviews con todos los Critical/Important resueltos y re-verificados; gate perf verde en el límite (mediana 90 = umbral verde, main 91, LCP mejor que main). Tests en main mergeado 522/522. Merge FF 6b69bca..287b240 pusheado a github.com:jnicob/portfolio; rama borrada.
FASE 3.6: COMPLETA — 43 commits (c64fd51..287b240), media-kit 0.5.0 lista para npm. Backlog F3.7: feedback B-E + galería A6 + design minors T21 (side-by-side altura, ancho scrub, copy táctil hint, card 768, canvas dark reposo, pre-wrap JSON, fade real al filtrar, key i18n resume blink, jerarquía 'y 8 más', tiles galería, segundo asset). Backlog F4: First Load JS >2x presupuesto (PALANCA REAL del perf), img after del paquete (MediaSource) sin lazy/resize, loadedmetadata listener scrub 0.6, aria-describedby FilterableList, token surface-hover.

# === FASE 3.7 (plan: docs/superpowers/plans/2026-07-18-phase-3.7-showcase-ux-content.md, rama: feature/phase-3.7-showcase-ux-content) ===
F3.7 Task 1: complete (commits ae7a040..d657db1, re-review Approved; causa real NI H1 NI H2: fuga de especificidad del selector data-fit del lightbox (descendant → direct-child `>`), test regex CSS 3/3 discrimina, rects verificados en vivo 1000x562.5 ambos lados; fixes review: comentario T13 reescrito + JSDoc children con contrato hijo-directo; ⚠️ verificación en vivo re-cubierta en T25)
F3.7 Task 2: complete (commit fa13b7c, review Approved; data-stacked <480px via ResizeObserver + CSS 1fr/1fr 1fr + object-fit cover; especificidad verificada sin clash con fix T1; Minors → final review: STACK_BREAKPOINT dentro del componente (mover a module scope), guard if(entry) muerto)
F3.7 Task 3: complete (commits 1221eb3..a06f30e, re-review Approved; visibleIds opcional interseca con filtro + FLIP anima en cambio de visibleIds; ENMIENDA menor de mecanismo del plan: separador clave de contenido "\\u0000" en vez de espacio (ids con espacios) — espíritu del plan intacto; incidente: fixer incrustó NUL literal → saneado por orquestador a06f30e; test extra primer-render-no-anima)
F3.7 Task 4: complete (commits 676aeb2..e8fb35b, re-review Approved; salida animada con render diferido — render set items∩(visible∪exiting), isExiting derivado de visibleIdSet (estado imposible inofensivo por construcción), FLIP excluye data-fg-exiting (sin ghost-FLIP), re-entrada cancela y gana; fixes review: cleanup mount-only effect + isMountedRef, test re-entrada reforzado a mecanismo, ref renombrado previousRenderedIdsRef; nota reviewer: render diferido = unmount+remount en el mismo flush pre-paint, sin consecuencia observable)
F3.7 Task 5: complete (commit 143ce92, review Approved; HoverVideo facade 0-bytes con delay 300ms solo pointer:fine + !reduced-motion, toggle teclado/click siempre, guard SSR matchMedia; 9/9 tests; Minors → final review: hover-video/index.ts huérfano (root index importa directo — unificar patrón), pointerleave apaga playing activado por teclado (edge UX, decisión aceptada))
F3.7 Task 6: complete (commits 317f4ea..234ab9e, re-review Approved; scrub v3: estado duration en onLoadedMetadata + fallback, formatTime m:ss con guard finitud, chip .mk-scrub__time aria-hidden :empty, track 6px + var pública --mk-scrub-track; fixes review: test late-metadata endurecido (ruta de estado discriminada, RED descableando handler) + guard Infinity; Minor → final review: commit 234ab9e tipo test: pero toca código de producción (guard) — nit de convención; README desactualizado (scrub/--mk-scrub-track) → lo cubre T7)
F3.7 Task 7: complete (commit e04af4f, review Approved 0 findings; 0.6.0 en package.json + CHANGELOG 2026-07-18 + README con 8/8 verificaciones 1:1 re-confirmadas por reviewer; fullSrc/MediaSource intacto (spec §1.6: sin API nueva). BLOQUE A COMPLETO)
F3.7 Task 8 (ORQUESTADOR): complete (commit 04e9877; 19 generaciones Magnific MCP: imagenes imagen-nano-banana-2 + seedream-5-pro 2k, videos google-veo3_1 1080p 5-6s + kling-30 1080p 5s, musica google-lyria 30s; post-proceso cwebp q82 display/HD + ffmpeg estatico crf30-32 (Veo recomprimido para presupuesto 4MB) + scrub GOP-8 480p 1.57MB desde timelapse; curacion visual 16/16 aprobada; presupuestos TODOS en verde: display <=178kB, HD <=666kB, video <=3.8MB, poster <=99kB, audio <=623kB; dims registradas en scratchpad t8-asset-map.txt para T9; NOTA: scrub comparte fuente con veo-timelapse-urbano de la galeria)
F3.7 Task 9: complete (commit bb4ba5e, review Approved 0 findings; galleryItemSchema discriminated union strict + gallery.ts 16 items verificados 1:1 contra tabla (ids/modelos/titulos/dims), integrado en PII-guard de validate-content; ENMIENDA: model de audios = "Google Lyria" (modelo real) en vez de "Musica IA" de la spec)
F3.7 Task 10: complete (commit e0fc867, review Approved; GalleryAudioTile facade con mount-timing play via effect (gotcha real cubierta por test), guard duration>0; Minors → T11/final review: sin onPause/onPlay sync (estado playing huerfano si pausa externa — relevante si T11 orquesta un-solo-audio), title solo sr-only (arquitectura de nombre accesible a resolver en T11 con caption+fullscreen), <track kind=captions> sin src muerto)
F3.7 Task 11: complete (commits 44fdcaa..2ab74ba, re-review Approved; GalleryDemo: titulo compuesto, busqueda sin diacriticos via visibleIds, preload HD solo en hover/focus del boton fullscreen (video sin fullSrc = no-op correcto), lightbox media/children excluyentes detectado y resuelto (desviacion documentada del brief); fixes review: filtro de categoria controlado + hasResults de la interseccion real (equivalencia probada algebraicamente por reviewer), sr-only span de audio tile eliminado (title prop retirada), lightbox de audio en figure flex-col autogestionado; PENDIENTE T12: confirmacion VISUAL del layout del lightbox de audio (orquestador); Minor → final review: equivalencia categorias singleton no type-enforced (comentario si se retoca))
F3.7 Task 12: complete (commit 62bd5c8, review Approved 0 findings; seccion gallery top-level con next/dynamic patron T31, i18n es/en 12 claves + t.raw para plantillas {title} (comentado por que), sub-bloque FilterGallery retirado limpio (16 claves borradas ambos idiomas, demo+test eliminados, 0 huerfanos), TOC mediaKit intacto; verificacion en vivo del implementer: lightbox audio EN COLUMNA (cover 70dvh, medido a 2 viewports — cierra el pendiente de T11), busqueda seedream, hover-autoplay, 0 console errors; ⚠️ claims en vivo re-cubiertos por la verificacion global de T25)
F3.7 Task 13: complete (commits fcbc04e..eb27a00, review Approved; API player v2 split 2 columnas, fila de estado min-h-8 SIEMPRE renderizada (mejora vs T30: idle ya no desmonta), paneles h-64 fijos con Tabs apilados, preview done con imagen galeria + fullscreen preload/lightbox, whitespace-pre-wrap conservando overflow, maquina de estados/aria-live/cleanup intactos; 2o commit: lightbox labels compartidos via t.raw (mismo idiom GalleryDemo) tras concern del propio implementer; Minors → final review: test h-64 no asierta mid-run (clase estatica, inerte), preview.width/height aun no consumidos (forma para F4))
F3.7 Task 14: complete (commit 8916f06, review Approved 0 findings; demo scrub v3: copy caso de uso + hint tactil/teclado (backlog T21) verbatim es + en natural, max-w-md fuera (ancho card, coherente con demos hermanas), poster dims 864x486 verificadas, facade T27 y hint-disappearance T26 intactos; verificacion dev del implementer: currentTime salta fluido con GOP-8 + chip de tiempo vivo; GATE de salida del scrub pendiente en T25 design review)
F3.7 Task 15: complete (commits 06c0ddb..a00aa26, re-review Aprobado; NavLink activo con aria-current + subrayado accent, matching home/sub-rutas correcto (false-prefix rechazado), click activo preventDefault, header sigue RSC; fixes review: focus-visible convencion del repo en ambos estados + import type MouseEvent)
F3.7 Task 16: complete (commits 2434353..cff552f, re-review Approved; MobileMenu disclosure WAI-ARIA (aria-expanded/controls via useId, Escape devuelve foco, click en link cierra por delegacion closest(a), sin focus trap), header RSC pasa switchers/links como children, hidden sm:flex / sm:hidden; fixes review: BUG real de desync ThemeSwitcher entre instancias duales (MutationObserver data-theme como fuente unica, RED verificado) + stopPropagation en Escape del SkinSwitcher solo con panel abierto (test integracion); Minor residual → final review: Escape con texto en el filtro del skin dentro del menu mobile burbujea y cierra el hamburguesa (edge preexistente T22))
F3.7 Task 17: complete (commit f1b6d5b, review Approved; skills C2: JavaScript 5, React / Next.js fusionado 4 (nombre exacto con espacios), TypeScript 4, Vue 2/3 3; adaptacion de skills-summary.test solo datos citados; Minor: format gate re-envolvio 3 entradas preexistentes >100ch, valores identicos)
F3.7 Task 18: complete (commits 4fdcd5d..f3aab57, re-review Approved; profile.summary de carrera VERBATIM es/en (byte a byte verificado), badge disponibilidad + CTA LinkedIn con aria-label destino, hero sigue RSC; fixes review: buttonVariants exportado y aplicado a AMBOS CTAs del hero (un idioma; adopcion repo-wide → review final); notas → design review T25: el CTA de CV cambia levemente de estilo (h-10, press animation) — mirada visual; home.meta.description con presente verbal "at Freepik/Magnific" → T21; commit intermedio 22767b7 style: reflow prettier repo-wide sin semantica)
F3.7 Task 19: complete (commits 93b688e..f4ac29f, re-review Approved; experience freepik company Freepik/Magnific + end 2026-07 (cascada verificada: sin JSON-LD/SEO afectado; test presentLabel adaptado a entrada sintetica), timeline rail v2: gradiente accent + nodos ring + card con Badge data-testid=timeline-date + hideDates (standard/compact intactas), un solo layout verificado dark/light/375; fixes review: formatExperienceRange helper unico + comentario projects.ts actualizado)
F3.7 Task 20: complete (commit 6b2abac, review Approved; mdx-components externo-aware integrado en compileMDX (verificado en call site), back link i18n en detalle de proyecto, URLs E5-E6 exactas (solo 2 entradas tocadas), project-card externos _blank+noopener, hover prosa decoration accent con base currentColor; Minors → final review: regex sin protocolo-relativo (latente, sin contenido afectado), footer/cv con rel=noreferrer sin noopener (candidato T24/final), back link sin test de pagina → verificacion en vivo T25)
F3.7 Task 21: complete (commit 09d8877, review Approved 0 findings; barrido E4 con inventario por linea verificado 1:1 por reviewer: empresa/plataforma → Freepik/Magnific, quedan URLs + Freepik/Flaticon backoffice (producto compuesto) + fixture de slug; home.meta.description reescrita en clave de carrera es/en (cierra concern de T18); parity intacta)
F3.7 Task 22: complete (commit bbb40c2, review Approved 0 findings; ai-service-integration reemplaza ai-model-onboarding: entrada projects.ts verbatim (metrica 6 artefactos), MDX es/en 5 secciones ~60 lineas clean-room VERIFICADO por reviewer (cero sistemas internos/cifras/nombres; terminos publicos con precedente), highlight experience actualizado, cascada limpia (solo docs historicos citan el slug viejo), build: ruta vieja fuera / nueva prerenderizada es+en)
F3.7 Task 23 (ORQUESTADOR): complete (commit a78e58f; metricas E8 verificadas contra docs.magnific.com/llms.txt 2026-07-18: 40+ modelos IA (13 img + 18 video + 6 edicion + 3 audio nombrados en el indice publico) y 100+ endpoints (patron async 3/modelo documentado + audio 9 explicitos + analytics 5 + stock; conteo >120, suelo conservador); BONUS con evidencia: docs.freepik.com 301 → docs.magnific.com, links docs actualizados en projects.ts + 4 MDX; gate verde 587 tests)
F3.7 Task 24: complete (commits 9bbfbb3..a7ab14c, re-review Approved; polish pack T21: canvas BASE_DOT_ALPHA 0.15→0.35, blink switch con props propias blinkPauseLabel/blinkResumeLabel (ENMIENDA: toca paquete — pauseLabel/resumeLabel estructuralmente muertos en modo blink), jerarquia y-8-mas text-sm muted, card huerfana 768 fix dinamico featured.length%2 (finding real era STATUS de featured-projects, no galeria), spotlight con retrato neon de galeria (patron filter-derived intacto); fixes review: defaults accion-boton coherentes con aria-checked (announcement = misma expresion que el render, desync imposible por construccion; RED verificado) + 0.7.0 fantasma plegada en 0.6.0 no publicada; Minor → design review: BASE_DOT_ALPHA unico ambos temas (mirada light), spotlight retrato mas alto que demos vecinas)
F3.7 T25 gates parciales: verificacion en vivo 16/16 PASS (report .superpowers/sdd/t25-live-verification.md; notas metodologicas: HD preload solo sobre umbral C3 — correcto por spec); First Load JS A/B gz: showcase main 292.1kB vs rama 296.0kB (+3.9kB, +1.3%) y home +0.6kB — coste de features nav (MobileMenu/NavLink) y labels, demos/galeria confirmadas en chunks diferidos y 0 bytes de media pre-interaccion → aceptado con matiz, palanca real sigue en F4; LIGHTHOUSE A/B intercalado host ocioso (protocolo T21, /es/showcase): main 91/91/92 (mediana 91), rama 91/91/91 (mediana 91) — GATE PERF PASS LIMPIO, mediana igual a main y >90 (mejor cierre que F3.6); LCP main ~3316ms vs rama ~3473ms
F3.7 T25 design review (Opus): GATE SCRUB APROBADA (fluido + autoexplicativo, se queda); 0 bloqueantes, 2 Importantes ARREGLADOS y re-verificados (2957899: lightbox audio compacto hideCover 540x622 en 900px, spotlight capado max-w-md 448x600); qa-a11y-perf: axe 0 violations 12 combos, teclado completo PASS, reduced-motion PASS, contraste AA (badge 4.99/4.75:1); BUG Bloqueante cazado por QA y ARREGLADO (7b3749a: pointer-capture del zoom-pan secuestraba clicks de controles audio/video en el lightbox → INTERACTIVE_SELECTOR + defensa en onOverlayClick, 8 tests RED verificado, re-verificado 2/2 por QA → PASS); code review final de rama (Fable): 0 Critical, 2 Important ARREGLADOS (a7be8be: NavLink preventDefault solo match exacto — dead-end sub-rutas, README props blink), triage 12 minors: 6 BACKLOG F3.8/F4 + 6 ignorar razonados → "Ready to merge: Yes".
F3.7 Task 25: complete — cierre de fase. Gate 605 tests + builds duales; verificacion en vivo 16/16; Lighthouse mediana 91 = main; STATUS/roadmap/feedback-G actualizados.
FEEDBACK USUARIO 2026-07-18 (durante cierre): seccion G del doc de feedback → F3.8 (bug tema+idioma, selector layouts masonry galeria, API player mas ejemplos, paginas detalle dev-hub y backoffice).
FASE 3.7: COMPLETA — 41 commits (ae7a040..HEAD), media-kit 0.6.0 lista para npm.

# === FASE 3.8 (plan: docs/superpowers/plans/2026-07-20-phase-3.8-advanced-showcase-project-details.md, rama: feature/phase-3.8-advanced-showcase-project-details) ===
F3.8 Task 1: complete (repro G1 CONFIRMADO sobre export estático; tema: inicio dark/stored dark → toggle light/stored light → locale en deja DOM dark con storage light; skin: terminal/stored terminal → locale en elimina data-skin con storage terminal. Causa raíz confirmada: el remount del root layout reaplica el JSX estático data-theme="dark" y ningún data-skin; AppearanceInit retorna por la caché one-shot resolvedOnce y no restaura storage. Rama creada desde el HEAD aprobado 778b767 sin git pull: main divergía de origin/main y el usuario fijó explícitamente ese estado de partida.)
F3.8 Task 2: complete (fix G1 según causa confirmada, sin desviaciones: reapplyStoredAppearance prioriza storage válido y usa resolvedOnce como fallback; AppearanceInit re-aplica en cada remount. TDD: helper inexistente 2 RED + remount dark→light RED; 15 tests appearance GREEN. Export estático en vivo: light+terminal se conservan al navegar es→en, DOM y storage alineados.)
F3.8 Task 3: complete (media-kit 0.7.0: motor masonry puro con packing determinista de columna más corta, extraHeight y fallback AR=1. TDD: módulo inexistente RED; 5 tests del motor GREEN. CHANGELOG abierto como Unreleased; API aditiva y 0 dependencias runtime nuevas.)
F3.8 Task 4: complete (motor justified greedy: filas completas llenan ancho exacto y última parcial conserva targetRowHeight; semántica del corte fijada con ratios [2, 1.2, 1] según la recomendación del plan. Columnas responsive por contenedor 2→3→4→5. TDD: 5 funciones/casos RED por exports inexistentes; 10 tests del layout engine GREEN.)
F3.8 Task 5: complete (FilterGallery amplía API de forma aditiva con layout grid/masonry/justified, aspectRatio e itemExtraHeight; ResizeObserver mide el contenedor tras hidratar, left/top preservan orden DOM y transform queda para FLIP. data-layout siempre presente; grid sigue default. TDD: 5 casos de contrato RED; 20 tests FilterGallery + 10 engine GREEN, suite paquete 239/239.)
F3.8 Task 6: complete (selector accesible aria-pressed grid/masonry/justified, estado de sesión, ratios desde width/height tipados, caption truncado y chrome fijo 28px; i18n es/en en paridad. TDD: selector RED sobre contrato público stale; package rebuild + 7 tests GalleryDemo GREEN. Verificación dev: 375px→2 columnas (contenedor 312), 1440px→4 (contenedor max 896; umbral 5 cubierto por unit test a 1200), filtro Imagen 8/16, justified 16 ítems, dark→light OK.)
F3.8 Task 7: complete (skill masonry-layouts creada con patrón motor puro/orden DOM/ResizeObserver/FLIP/SSR y routing en AGENTS.md; aceptación inicial RED por archivo/routing ausentes, luego quick_validate GREEN y agents:validate GREEN. .github/copilot-instructions.md regenerado por agents:setup. Skill generalizable a ai-config, pero NO propagado por instrucción explícita del usuario; recomendar en handoff.)
F3.8 REVIEW 1 (Bloque 1, Tasks 2-7, por Claude): CORRECTITUD APROBADA — code review 0 Critical/0 Important (fix G1 ataca causa raíz, tests de regresión reales no tautológicos; motor masonry/justified correcto con orden DOM preservado y edge cases; tokens semánticos sin hardcodeos; i18n paridad; skill/routing coherente). DISEÑO fix-first, sin blockers, con deuda de pulido a resolver ANTES del Bloque 2 (decisión usuario: Majors + traducción):
  - M1 (coherencia): selector de layout (app, gallery-demo.tsx:259-271) y chips de categoría (media-kit, styles.css:774-795) usan lenguajes visuales distintos (radio/borde/peso del activo) y divergen distinto por skin (terminal: layout cuadrado vs chips píldora). FIX: unificar el selector al lenguaje de los chips (píldora + relleno accent sólido en activo) reutilizando --mk-filter-*.
  - M2 (bug UX): el FLIP (filter-gallery.tsx:226) no cubre el render de dropExiting (:265), el hueco del ítem saliente queda abierto los 240ms del fade y luego los supervivientes saltan sin transición. FIX: recolocar supervivientes en el mismo beat del filterChanged (excluir exitingIds de `computed`) o disparar FLIP al menguar renderedItems.
  - M3 (coherencia): densidades divergen a igual anchura (grid auto-fill 120px ~5col vs masonry 3col a 700px). FIX: que grid comparta columnCountForWidth/umbrales con masonry; acercar justified con targetRowHeight comparable.
  - m2 (i18n): "Masonry" sin traducir en es.json (vecinas sí: "Cuadrícula", "Filas justificadas"). FIX: "Mosaico".
  DIFERIDO a review final (Bloque 3): iconos+etiqueta en el selector (duda b), jerarquía de 3 filas de chrome (m4), empty state diseñado (m5), FLIP sin interpolar tamaño en cambio de layout (m3). Gate verificado verde de forma independiente: lint 0 err, typecheck OK, 624 tests.
F3.8 Review-1 fixes: complete (M1 selector y filtros comparten píldora, tokens --mk-filter-* y activo accent sólido; M2 masonry/justified calcula las cajas finales sin exitingIds y conserva la caja absoluta previa del saliente durante su fade; M3 grid comparte ResizeObserver + columnCountForWidth 2→3→4→5 con masonry y justified deriva el target row height del ancho de columna del breakpoint; m2 i18n es traduce Masonry como Mosaico. TDD: regresiones visuales/de layout RED→GREEN; gate verde con 241 tests media-kit + 386 web.)
F3.8 Task 8: complete (apiDemo se reemplaza por 4 ApiDemoExample tipados en orden image/video/audio/error; previews reutilizan assets existentes de galleryItems y el caso error expone 400 sin medio. TDD: 3 tests RED por export ausente → GREEN; contrato commiteado atómicamente con T9.)
F3.8 Task 9: complete (ApiRequestPlayer consume examples, añade selector accesible de endpoint, request h-40 estable, reset completo a idle al cambiar y status/JSON 400 para error; wiring + labels es/en en paridad. TDD: contrato viejo hizo 20 RED → 23 tests focalizados GREEN; commit compartido con T8.)
F3.8 Task 10: complete (preview discriminada: imagen con dims explícitas + HD, vídeo controls/poster sin montar antes de done, audio con carátula + GalleryAudioTile compacto, error diseñado sin fullscreen; MediaLightbox usa media para imagen/audio y child video para vídeo. TDD: 4 casos RED → 24 tests ApiRequestPlayer GREEN; 0 assets nuevos.)
F3.8 Task 11: complete (detalle es/en del Panel de desarrollador con caseStudy true y contenido clean-room sobre uso, presupuesto, límites, facturación, API keys, enterprise y planes; landings verificadas 2026-07-20: docs 200→/introduction y pricing 200 incluidas; Freepik API, Magnific API y playground devolvieron 403 incluso con UA navegador y se excluyeron de la lista/frontmatter por la regla del plan. Regex externo amplía http(s) a protocolo-relativo; TDD 1/6 RED→6/6 GREEN. Build estático prerenderiza ambas rutas.)
F3.8 Task 12: complete (detalle es/en del Backoffice de contenido con caseStudy true y cinco secciones acordadas: revisión/catalogación/moderación/publicación, workflow de producción, packs Flaticon, portal fiscal y extensiones Chrome; frontmatter reutiliza literalmente projects.ts y el cuerpo se mantiene clean-room, sin cifras ni nombres adicionales. validate-content GREEN y build estático prerenderiza ambas rutas.)
F3.8 Task 13: complete (GalleryAudioTile toma eventos nativos onPlay/onPause como confirmación del estado ante controles externos; STACK_BREAKPOINT 480 movido a scope de módulo con docstring; el barrel hover-video/index.ts ya existía y el root export ahora lo consume. TDD onPause/onPlay RED→10/10 GREEN; compare-slider 58/58 y media-kit 241/241 GREEN. CHANGELOG sin cambio: STACK_BREAKPOINT/barrel son internos y el audio tile pertenece a la app, siguiendo el criterio del plan.)
F3.8 REVIEW 2 (Bloque 2 = Fase A fixes + Tasks 8-13, por Claude): CODE REVIEW APROBADO — 0 Critical/0 Important (M2 verificado con discriminante real: test falla si se revierte; contrato apiDemo→apiDemoExamples sin referencias muertas; regex protocolo-relativo lineal sin ReDoS; exclusión de hosts 403 no esconde links rotos; backlog sin regresiones). DESIGN REVIEW fix-first: fixes de Fase A (M1/M2/M3/i18n) BIEN resueltos. Feature branch PUSHEADA a origin (no merge a main: se difiere al cierre de fase). 3 MAJORS de UI a arreglar en el Bloque 3 ANTES del merge final (Fase A del Bloque 3):
  - MJ1 (estado): badge de error 4xx usa variant 'neutral' (gris) en api-request-player.tsx:349; el ejemplo de error no comunica error. FIX: variant 'danger' (token --danger existe en los 4 skins, variante badge danger ya definida sin usar).
  - MJ2 (legibilidad): prose-portfolio (globals.css:263-269) fija padding-left pero no restaura list-style tras el reset de preflight; las listas del detalle freepik-developer-dashboard (3 ul/8 li) se ven como prosa sin marcador. FIX: list-style disc/decimal (o ::marker) en las listas de prose-portfolio.
  - MJ3 (UX): el fix M2 solo cubre masonry/justified (posición absoluta). En grid (layout POR DEFECTO) el li saliente queda en el flujo su celda los 240ms del fade y los supervivientes saltan sin FLIP al hacer dropExiting. CONFIRMAR en navegador (filtrar Todos→Imágenes en grid) y, si salta, unificar con el comportamiento de masonry.
  MINORS para triage en Bloque 3: FullscreenIcon duplicado (gallery-demo.tsx:84 y api-request-player.tsx:70) → extraer icono compartido; el <select> de endpoint no reutiliza el Select del design system (sin hover/medidas); deck del backoffice (~60 palabras) es un muro; fullscreen de audio en el player solo muestra carátula (inconsistente con GalleryDemo); chips inactivos casi invisibles en light (preexistente, compartido); "reset" prometido no es un affordance visible (solo al cambiar de ejemplo); icono de error usa emoji ⚠ vs set SVG; +1 warning <img> (14→15). Gate verificado verde independiente: lint 0 err, typecheck OK, 401 web + 241 media-kit; builds estáticos con las 4 rutas nuevas prerenderizadas. HEAD 189b07b.
F3.8 Review-2 fixes: complete (MJ1 badge 4xx usa danger con regresión observable; MJ2 ul/ol restauran disc/decimal y navegador confirma viñetas en developer-dashboard; MJ3 CONFIRMADO en Chrome: grid conservaba 1949.19px durante el fade y colapsaba tarde a 958.33px, corregido a colapso de 958.33px en el mismo beat con salientes absolutos y test de regresión. Minors resueltos: FullscreenIcon compartido, alerta SVG propia, endpoint con Select del design system, deck backoffice ≤200 caracteres por locale conservando los 5 subproyectos y chips inactivos con borde currentColor — verificado light con borde sólido 1px tokenizado. Diferidos al review final por requerir decisión de contrato/jerarquía: affordance Reset explícito y fullscreen de audio con controles; warning <img> se mantiene en 15 porque los medios dinámicos del showcase usan <img> deliberadamente. TDD RED→GREEN por fix; último gate Fase A: lint 0 errores/15 warnings, typecheck OK, 405 web + 242 media-kit.)
